---
phase: 05-verification-upstream-pr
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - build/build.ts
  - package.json
  - .github/workflows/testBuild.yml
  - electron-builder.ts
autonomous: true
requirements: [UPST-02]

must_haves:
  truths:
    - "The wasapi addon now ships via one optionalDependencies line (github:thomas-quant/wasapi-loopback), mirroring patchcord — the GOOFCORD_WASAPI_LOOPBACK_PATH env override and the in-CI Rust build are gone (SC#3)"
    - "The host-agnostic copy machinery (copyNativeAddonsToOutDir + ts-out/native runtime load) is PRESERVED — it is the permanent fix for Bun's Windows-host file-loader bug, NOT scaffolding (RESEARCH Pitfall 3)"
    - "The getPlatformString fix is made as its OWN separate, clearly-bounded change so it can be kept OUT of the echo PR diff (D-12)"
    - "CI diagnostics dump is removed but a lean 2-line packaging presence assertion survives (RESEARCH A2 / PITFALLS V4 safeguard)"
  artifacts:
    - path: "package.json"
      provides: "The single optionalDependencies github-ref line for the addon"
      contains: "github:thomas-quant/wasapi-loopback"
    - path: "build/build.ts"
      provides: "Prebuild-only wasapi entry (envPath dropped) + preserved copyNativeAddonsToOutDir"
      contains: "copyNativeAddonsToOutDir"
  key_links:
    - from: "package.json optionalDependencies"
      to: "node_modules/wasapi-loopback/prebuilds/windows-x86_64/wasapi-loopback-win32-x64.node"
      via: "bun clones the github ref; the committed prebuild lands at the path build.ts:207 reads"
      pattern: "github:thomas-quant/wasapi-loopback"
    - from: "build/build.ts copyNativeModules"
      to: "ts-out/native/wasapi-loopback-win32-x64.node"
      via: "copyNativeAddonsToOutDir host-agnostic fs copy (KEEP)"
      pattern: "copyNativeAddonsToOutDir"
---

## Phase Goal

**As a** GoofCord Windows user, **I want to** receive the #46 echo fix as a verified, instrumentation-free, upstream-merged change, **so that** sharing system audio never echoes the call back to viewers — proven on the actual shipped build, not a debug spike.

<objective>
D-08 + D-12 + D-13 (SC#3): Flip the addon from the build-time env override to a published `optionalDependencies` github ref, strip the in-CI Rust build + CI diagnostics, and fix the `electron-builder.ts` `getPlatformString` bug — but keep the latter as its OWN bounded change so it stays out of the echo PR diff. CRITICAL: preserve the host-agnostic native-copy machinery despite the misleading `PHASE-5 REMOVAL` comments.

Purpose: Reduce the GoofCord-side delivery to the surgical SC#3 surface (one optionalDependency line + the prebuild entry build.ts needs), matching how patchcord/venbind ship.

Output: build.ts (prebuild-only), package.json (+1 optionalDependency), testBuild.yml (Rust + diagnostics stripped, lean assertion kept), electron-builder.ts (getPlatformString fix as a separable change).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-verification-upstream-pr/05-RESEARCH.md
@.planning/phases/05-verification-upstream-pr/05-PATTERNS.md
@build/build.ts
@electron-builder.ts
@.github/workflows/testBuild.yml
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop the env override in build.ts (keep the copy machinery) and add the optionalDependencies line</name>
  <files>build/build.ts, package.json</files>
  <read_first>
    - build/build.ts — the wasapi entry (lines 194-208): `envPath: process.env.GOOFCORD_WASAPI_LOOPBACK_PATH` (206, DELETE) + the `prebuilds: [{ src: ["wasapi-loopback","prebuilds","windows-x86_64","wasapi-loopback-win32-x64.node"], ... }]` (207, KEEP); the `PHASE-5 REMOVAL` comments (195-196, 260, MISLEADING — reword); `copyNativeAddonsToOutDir()` (call at 85, def at 261-282, KEEP — Pitfall 3); `copyNativeModules()` prebuild map (~237-243)
    - package.json lines 49-52 — the optionalDependencies block (patchcord github ref + venbind)
    - 05-RESEARCH.md Q1 (committed-prebuilt delivery; do NOT strip copyNativeAddonsToOutDir or the ts-out/native runtime load; the os/cpu fields make bun skip off-Windows) + Pitfall 3; 05-PATTERNS.md "build/build.ts" + "package.json (root)" sections
  </read_first>
  <action>
    In build.ts delete ONLY the `envPath:` line in the wasapi-loopback entry; KEEP the `prebuilds` entry (the committed prebuild from the addon repo lands at exactly that path). KEEP `copyNativeAddonsToOutDir()` and its call, and the `ts-out/native` runtime load in wasapiLoopback.ts — reword the misleading `PHASE-5 REMOVAL` comments to "Phase 5: env override removed; prebuild-only (host-agnostic copy stays — Bun's file-loader emits zero .node on a Windows build host)". In root package.json add ONE line to optionalDependencies mirroring the patchcord github-ref shape: `"wasapi-loopback": "github:thomas-quant/wasapi-loopback"`.
  </action>
  <acceptance_criteria>
    - `grep -c "GOOFCORD_WASAPI_LOOPBACK_PATH" build/build.ts` is 0
    - `grep -q "copyNativeAddonsToOutDir" build/build.ts` (machinery preserved)
    - `grep -q "github:thomas-quant/wasapi-loopback" package.json`; `node -e "JSON.parse(require('fs').readFileSync('package.json'))"` succeeds (valid JSON)
    - The `prebuilds` entry for wasapi-loopback still references `windows-x86_64/wasapi-loopback-win32-x64.node`
  </acceptance_criteria>
  <verify>
    <automated>! grep -q "GOOFCORD_WASAPI_LOOPBACK_PATH" build/build.ts && grep -q "copyNativeAddonsToOutDir" build/build.ts && grep -q "github:thomas-quant/wasapi-loopback" package.json && node -e "JSON.parse(require('fs').readFileSync('package.json'))"</automated>
  </verify>
  <done>The addon is consumed as a published optionalDependency; the env override is gone; the load-bearing host-agnostic copy is preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Strip the in-CI Rust build + diagnostics dump from testBuild.yml, keeping a lean packaging assertion</name>
  <files>.github/workflows/testBuild.yml</files>
  <read_first>
    - .github/workflows/testBuild.yml — the `Install Rust toolchain` step (51-56) + `Build wasapi-loopback addon` step (57-72, includes the `GOOFCORD_WASAPI_LOOPBACK_PATH` GITHUB_ENV export at 72); the `Assert wasapi addon packaged` step (86+) with the `DIAGNOSTICS (remove once green)` block (90-101) and the real presence check using `$name = "wasapi-loopback-win32-x64.node"` (102+)
    - 05-RESEARCH.md Q5 ("CI") + A2 (keep a lean 2-line presence assertion — the V4 safeguard; strip only the verbose diagnostics); D-13 (strip the temp DIAGNOSTICS); 05-PATTERNS.md Strip-Seams testBuild.yml row
  </read_first>
  <action>
    Remove the `Install Rust toolchain (wasapi-loopback addon)` and `Build wasapi-loopback addon (.node)` steps (51-72) including the `GOOFCORD_WASAPI_LOOPBACK_PATH` GITHUB_ENV export — the addon now arrives via the optionalDependency. Remove the `DIAGNOSTICS (remove once green)` block (90-101). KEEP a lean presence assertion in the `Assert wasapi addon packaged` step: assert `wasapi-loopback-win32-x64.node` is present in `ts-out/native` and in the packaged app (the 2-line presence check around line 102) — this is the deliberate V4 safeguard, not diagnostics. Reword the surrounding comments to reflect optionalDependency delivery.
  </action>
  <acceptance_criteria>
    - `grep -c "GOOFCORD_WASAPI_LOOPBACK_PATH" .github/workflows/testBuild.yml` is 0
    - `grep -cE "Install Rust toolchain|napi build" .github/workflows/testBuild.yml` is 0
    - `grep -c "DIAGNOSTICS" .github/workflows/testBuild.yml` is 0
    - The lean assertion remains: `grep -q "wasapi-loopback-win32-x64.node" .github/workflows/testBuild.yml`
  </acceptance_criteria>
  <verify>
    <automated>! grep -qE "GOOFCORD_WASAPI_LOOPBACK_PATH|Install Rust toolchain|napi build|DIAGNOSTICS" .github/workflows/testBuild.yml && grep -q "wasapi-loopback-win32-x64.node" .github/workflows/testBuild.yml</automated>
  </verify>
  <done>CI no longer compiles Rust or exports the env path and the verbose diagnostics are gone, while a minimal packaging presence assertion (V4 safeguard) survives.</done>
</task>

<task type="auto">
  <name>Task 3: Fix electron-builder.ts getPlatformString as a separate, bounded change (D-12 — kept out of the echo PR)</name>
  <files>electron-builder.ts</files>
  <read_first>
    - electron-builder.ts — `getPlatformString(context.packager.platform)` (88), `function getPlatformString(platform: Platform)` (97) with `case Platform.WINDOWS` (99); `asarUnpack: ["**/*.node"]` (73, already present from Phase 4 — KEEP, do NOT re-add)
    - 04-03-SUMMARY.md follow-up #3 (the bug: `context.packager.platform` doesn't `===` `Platform.WINDOWS` in beforePack → ships a misnamed `…-linux-x64.node` in the Windows package + breaks venbind's build-time loader; resolve via `context.electronPlatformName`)
    - 05-RESEARCH.md Q4 commit boundary table row 4 (this is its OWN commit, may become its own upstream PR) + D-12; 05-CONTEXT.md D-12
  </read_first>
  <action>
    Fix the platform resolution in the `beforePack`/copy logic to use `context.electronPlatformName` (the reliable signal) instead of comparing `context.packager.platform` to `Platform.WINDOWS`, so the Windows package gets the correctly-named `wasapi-loopback-win32-x64.node` and venbind's build-time loader resolves. Make this change self-contained and atomic so it can be committed separately and excluded from the echo PR's logical commits (D-12). Do NOT touch `asarUnpack` (already correct from Phase 4 — it stays in the echo surface). Leave a brief code comment noting this is a separable build fix (not part of the echo feature).
  </action>
  <acceptance_criteria>
    - `grep -q "context.electronPlatformName" electron-builder.ts`
    - `asarUnpack: ["**/*.node"]` still present (unchanged)
    - The change is confined to the getPlatformString resolution path (a single logical hunk), enabling a separate commit
  </acceptance_criteria>
  <verify>
    <automated>grep -q "context.electronPlatformName" electron-builder.ts && grep -q "asarUnpack" electron-builder.ts</automated>
  </verify>
  <done>The packaging platform is resolved via electronPlatformName as a separable change, ready to be committed apart from the echo PR per D-12.</done>
</task>

</tasks>

<verification>
- `package.json` carries one new optionalDependencies github ref; build.ts is prebuild-only with the host-agnostic copy preserved.
- testBuild.yml has no Rust build / env export / diagnostics dump, but keeps the lean packaging assertion.
- The getPlatformString fix is present and isolated for a separate commit (D-12).
- Note: on the Linux/WSL dev box `bun install` skips the win32 optionalDependency (os:["win32"]) so the addon `.node` is absent locally; the addon-present Windows build is verified in 05-05 after the addon repo is pushed.
</verification>

<success_criteria>
SC#3 (delivery half): native code ships via the prebuilt-`.node` optionalDependency pattern; the in-repo footprint is the additive gate + wasapiLoopback.ts + one build-script entry + one optionalDependencies line + regenerated IPC. D-12 getPlatformString fix kept separable.
</success_criteria>

<output>
Create `.planning/phases/05-verification-upstream-pr/05-04-SUMMARY.md` when done.
</output>
