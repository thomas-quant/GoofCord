---
phase: 05-verification-upstream-pr
plan: 06
type: execute
wave: 4
depends_on: ["05-05"]
files_modified:
  - .planning/phases/05-verification-upstream-pr/05-VERIFICATION.md
autonomous: false
requirements: [UPST-02]

must_haves:
  truths:
    - "On the FINAL stripped/dependency-packaged Windows build, a second-device viewer hears desktop audio AND no call echo — re-confirms ECHO-01 on the shipping shape (SC#1)"
    - "The D-11 ECHO-03 guard is proven: on the un-flagged 19045 run (injected but unsupported → activation fails → activePort unset) the viewer hears audio (not silence), no crash; and --no-wasapi falls through to loopback with the one fallback log line (D-15)"
    - "Linux patchcord path is byte-identical (win32 gate unreachable) and the optionalDependency no-ops on the Linux install; macOS untouched by code inspection, stated plainly with no faked hardware (D-16)"
    - "The completed verification report records the detected build number, native-branch-activated line, excluded root PID + Audio Service PID in subtree, and all the above (SC#1)"
  artifacts:
    - path: ".planning/phases/05-verification-upstream-pr/05-VERIFICATION.md"
      provides: "Completed verification report (rich + final shipping-shape + fallback + Linux/macOS non-regression)"
      contains: "win32-wasapi-exclude-tree"
  key_links:
    - from: "05-VERIFICATION.md final section"
      to: "screenshare-debug.log latest run"
      via: "AI reads PID/activation/chunk lines; human supplies viewer-audible + no-echo ground truth"
      pattern: "path=win32-wasapi-exclude-tree"
---

## Phase Goal

**As a** GoofCord Windows user, **I want to** receive the #46 echo fix as a verified, instrumentation-free, upstream-merged change, **so that** sharing system audio never echoes the call back to viewers — proven on the actual shipped build, not a debug spike.

<objective>
D-14 STEP 3 + D-15 + D-16 (SC#1): Re-confirm the #46 echo fix end-to-end, viewer-side, on the FINAL stripped/dependency-packaged artifact (05-05); prove the ECHO-03 fallback fix and the graceful-fallback path; verify Linux/macOS non-regression; and complete the verification report.

Purpose: Verification is a first-class deliverable. The streamer cannot self-verify (Electron mutes local echo) and idle loopback is silent, so the human supplies the two-device ground truth on the actual shipped shape while the AI reads the WSL-accessible log and writes the report.

Output: completed `05-VERIFICATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-verification-upstream-pr/05-RESEARCH.md
@.planning/phases/05-verification-upstream-pr/05-VERIFICATION.md
@.planning/research/PITFALLS.md
@src/windows/screenshare/screenshare.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Final two-device viewer re-confirm + fallback checks on the shipping artifact</name>
  <files>none (manual on-hardware verification)</files>
  <what-built>The FINAL Windows x64 CI artifact (05-05): all instrumentation stripped, the native addon arriving via the `github:thomas-quant/wasapi-loopback` optionalDependency, with the ECHO-03 fallback guard in place.</what-built>
  <read_first>
    - .planning/research/PITFALLS.md — V1 (name the build# + branch taken), V2 (no echo), V3 (non-call audio playing), V4 (packaging)
    - 05-RESEARCH.md Q7 (the D-11 nuance: `--no-wasapi` proves no-injection fallback; the UN-FLAGGED 19045 run proves the injected-but-unsupported guard — both worth capturing, the un-flagged run is the dispositive D-11 test)
  </read_first>
  <action>
    The user performs the three manual runs in how-to-verify on the FINAL 05-05 artifact (Claude cannot supply viewer-side audio ground truth). Claude pauses until the user reports each result.
  </action>
  <how-to-verify>
    Install/run the FINAL CI artifact from 05-05 on the Windows 19045 dev box. With a SECOND device/account as viewer and non-call audio actively playing:
    1. (Supported path) Start a screenshare WITH audio. Confirm viewer: (a) HEARS desktop audio, (b) hears NO call echo. No crash.
    2. (D-11 dispositive) Run UN-FLAGGED on 19045 where the per-process API path may be unsupported/forced to fail activation: confirm the viewer still HEARS audio (the loopback fallback track is kept — NOT silence), no crash.
    3. (D-15) Launch with `--no-wasapi`: confirm the stream falls through to `"loopback"`, viewer hears audio, no crash, and the single `[Screenshare] WASAPI process-loopback unsupported on this build, using loopback fallback` log line fires.
    4. Report each result + the OS build number shown.
  </how-to-verify>
  <acceptance_criteria>
    - Supported path: viewer hears desktop audio AND no echo (re-confirms ECHO-01 on the shipping shape)
    - Fallback paths (un-flagged-unsupported and `--no-wasapi`): viewer hears audio not silence, no crash, the one fallback log line fires
  </acceptance_criteria>
  <verify>
    <human-check>User reports the three results (supported path audio+no-echo; un-flagged-unsupported audio-not-silence; --no-wasapi audio + fallback log) and the OS build number.</human-check>
  </verify>
  <resume-signal>Type "reconfirmed" with the three results and the OS build number, or describe the failure.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Read the log, complete the verification report, and document Linux/macOS non-regression</name>
  <files>.planning/phases/05-verification-upstream-pr/05-VERIFICATION.md</files>
  <read_first>
    - /mnt/c/Users/Christ/AppData/Roaming/goofcord/screenshare-debug.log (latest run; verify `path=win32-wasapi-exclude-tree`, NOT `spike-synthetic`; cumulative log — use timeline)
    - 05-RESEARCH.md Q7 (Linux: win32 gate at screenshare.ts:101 makes the wasapi branch structurally unreachable → patchcord byte-identical; os:["win32"] makes bun skip the addon install; proof shape = Linux build + `git diff upstream/main -- src/windows/screenshare/screenshare.ts` Linux branch unchanged + bun install log showing the skip) and macOS (no hardware — state plainly; code-gate + os-skip + electron-builder files exclude the win32/linux .node)
    - src/windows/screenshare/screenshare.ts — the win32 gate (the non-regression guarantee)
    - 05-VERIFICATION.md — the rich section (05-01) + the final-section placeholder
  </read_first>
  <action>
    Complete the "Final shipping-shape re-confirm" section of `05-VERIFICATION.md` with: detected build number, the `audio=none path=win32-wasapi-exclude-tree` activation line, excluded root PID + Audio Service PID in its `app.getAppMetrics()` subtree, chunk counts (latest run), and the human-supplied (a)/(b) ground truth + the D-11/D-15 fallback results, tied to log signatures and the 05-05 CI run id. Add a "Non-regression (D-16)" section: Linux — run `bun install` on the WSL/Linux dev box and capture that the wasapi optionalDependency is SKIPPED (os:["win32"]); run a Linux build and capture `git diff upstream/main -- src/windows/screenshare/screenshare.ts` showing the Linux branch unchanged (patchcord byte-identical). macOS — code-level proof only (win32 gate + os-skip + electron-builder files exclude the win32/linux `.node`), explicitly labeled "verified by code inspection, no Mac hardware — not faked." Add a one-line note that the stale dev-box `screenshare-debug.log` can be deleted manually (no migration needed).
  </action>
  <acceptance_criteria>
    - `05-VERIFICATION.md` final section is complete: build#, native-path line, both PIDs, viewer ground truth, D-11 + D-15 fallback results, 05-05 run id — each citing a log signature
    - The Non-regression section documents the Linux optionalDependency skip + unchanged Linux branch diff, and the macOS code-level proof labeled as no-hardware/not-faked
    - `grep -q "win32-wasapi-exclude-tree" .planning/phases/05-verification-upstream-pr/05-VERIFICATION.md`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "win32-wasapi-exclude-tree" .planning/phases/05-verification-upstream-pr/05-VERIFICATION.md && grep -iq "no Mac hardware" .planning/phases/05-verification-upstream-pr/05-VERIFICATION.md</automated>
  </verify>
  <done>The verification report is complete and honest — ECHO-01 re-confirmed on the shipping shape, fallback proven, Linux/macOS non-regression documented without faked hardware.</done>
</task>

</tasks>

<verification>
- The shipping-shape build is viewer-verified (hears audio, no echo); the ECHO-03 fallback fix is proven on the un-flagged 19045 + `--no-wasapi` runs; Linux/macOS non-regression is documented.
- The report names the build number and the native branch taken (PITFALLS V1/V2/V3).
</verification>

<success_criteria>
SC#1: a completed verification report records, on a real Windows build, the build number + native-branch line, the excluded root PID with the Audio Service PID in subtree, the second-device viewer hearing desktop audio AND no echo, the graceful-fallback check, and Linux patchcord + macOS non-regression.
</success_criteria>

<output>
Create `.planning/phases/05-verification-upstream-pr/05-06-SUMMARY.md` when done.
</output>
