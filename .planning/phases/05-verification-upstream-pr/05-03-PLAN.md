---
phase: 05-verification-upstream-pr
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/modules/native/wasapiLoopback.ts
  - src/modules/screenshareDebug.ts
  - src/windows/main/preload/deliverySpike.ts
  - src/windows/main/preload/bridge.ts
  - src/windows/main/preload/preload.mts
  - src/windows/main/preload/wasapiTransport.ts
  - src/windows/screenshare/screenshare.ts
  - src/ipc/gen.ts
  - src/ipc/types.ts
autonomous: true
requirements: [UPST-02]

must_haves:
  truths:
    - "No diagnostic instrumentation remains in the shipped source — zero appendScreenshareDebug / screenshare-debug.log / syncCrumb / deliverySpike / screenshareDebug references in src/ (SC#2)"
    - "Exactly ONE conventional fallback console.log survives, on the screenshare.ts fallback branch, matching patchcord/venbind logging style (D-13)"
    - "On an unsupported build / --no-wasapi / failed activation the renderer leaves the original loopback track in place (viewer hears audio, not a silent gen track) — the ECHO-03 fallback-gap fix (D-11)"
    - "The load-bearing shouldInjectWasapiTransport getter survives (relocated to wasapiLoopback.ts, simplified) and the real fix (load model, MessageChannelMain transport, PID resolution, before-quit teardown, MSTG feeder, swap seam) is preserved"
  artifacts:
    - path: "src/windows/main/preload/wasapiTransport.ts"
      provides: "Stripped feeder + the D-11 activePort-gated getDisplayMedia swap seam"
      contains: "activePort"
    - path: "src/windows/screenshare/screenshare.ts"
      provides: "The additive 3-way gate + the one surviving fallback log"
      contains: "[Screenshare]"
    - path: "src/ipc/gen.ts"
      provides: "Regenerated IPC without the screenshareDebug:* channels; keeps wasapiLoopback:* getters"
      contains: "wasapiLoopback:shouldInjectWasapiTransport"
  key_links:
    - from: "src/windows/main/preload/wasapiTransport.ts"
      to: "getDisplayMedia swap"
      via: "if (!activePort) return stream; guard before the remove+addTrack(gen)"
      pattern: "if \\(!activePort\\)"
    - from: "src/windows/main/preload/preload.mts"
      to: "wasapiLoopback:shouldInjectWasapiTransport"
      via: "sendSync gate that decides feeder injection"
      pattern: "wasapiLoopback:shouldInjectWasapiTransport"
---

## Phase Goal

**As a** GoofCord Windows user, **I want to** receive the #46 echo fix as a verified, instrumentation-free, upstream-merged change, **so that** sharing system audio never echoes the call back to viewers — proven on the actual shipped build, not a debug spike.

<objective>
D-13 + D-11 (SC#2, ECHO-03): Strip ALL diagnostic scaffolding from the shipped source and fix the ECHO-03 silent-fallback gap — as a surgical RELOCATE-then-delete (NOT a blind file-delete). The two files headed `THROWAWAY` (`wasapiTransport.ts`, `wasapiLoopback.ts`) are the real hardware-verified shipping code; `screenshareDebug.ts` exports the load-bearing `shouldInjectWasapiTransport`. Read each file's BODY, not its header.

Purpose: Reduce the diff to the surgical SC#3 surface and make the unsupported path honestly fall back to Chromium `"loopback"` (viewer hears audio) instead of a silent generated track. The build must stay green at the end of this plan.

Output: stripped + ECHO-03-fixed source; regenerated IPC; two files deleted (`deliverySpike.ts`, `screenshareDebug.ts`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-verification-upstream-pr/05-RESEARCH.md
@.planning/phases/05-verification-upstream-pr/05-PATTERNS.md
@src/modules/native/wasapiLoopback.ts
@src/modules/screenshareDebug.ts
@src/windows/main/preload/wasapiTransport.ts
@src/windows/main/preload/bridge.ts
@src/windows/main/preload/preload.mts
@src/windows/screenshare/screenshare.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate shouldInjectWasapiTransport, strip wasapiLoopback.ts diagnostics, delete screenshareDebug.ts, regenerate IPC</name>
  <files>src/modules/native/wasapiLoopback.ts, src/modules/screenshareDebug.ts, src/windows/main/preload/preload.mts, src/ipc/gen.ts, src/ipc/types.ts</files>
  <read_first>
    - src/modules/screenshareDebug.ts (the whole file; `shouldInjectWasapiTransport<IPCOn>()` at lines 35-38 is load-bearing — read by preload.mts:54)
    - src/modules/native/wasapiLoopback.ts — `syncCrumb()` (~64-75), all `[sync]`/`appendFileSync` (~64-83,162,191), all `appendScreenshareDebug(...)` calls (13), `chunkCount`/`logCount` audit (~106-108,156,177-183,193,235), `wasapi smoke`/`wasapiPath resolved?` lines, the `appendScreenshareDebug` import (28), the rich build= line added in 05-01, LOG_PREFIX at :37, the load model / MessageChannelMain / PID resolution / before-quit teardown / stopWasapiLoopback (KEEP)
    - preload.mts:50-54 (the gate `sendSync("screenshareDebug:shouldInjectWasapiTransport")`)
    - 05-RESEARCH.md Q5 ("RELOCATE" + "STRIP diagnostics in-place" + "Regenerate IPC"), Pitfall 1 & 2; 05-PATTERNS.md Strip-Seams table rows for wasapiLoopback.ts + the relocate
    - CLAUDE.md "IPC codegen" constraint — never hand-edit gen.ts/types.ts; re-run the generator
  </read_first>
  <action>
    Move `shouldInjectWasapiTransport` from screenshareDebug.ts into wasapiLoopback.ts as an `<IPCOn>` getter, SIMPLIFIED to `process.platform === "win32" && !process.argv.includes("--no-wasapi")` (drop the `isTransportSpikeEnabled()` OR-branch). Update preload.mts:54 to call the new channel `wasapiLoopback:shouldInjectWasapiTransport` (and the comment at :50). In wasapiLoopback.ts, remove `syncCrumb()` and every `[sync]`/`appendFileSync` reference, all 13 `appendScreenshareDebug(...)` calls, the per-chunk `chunkCount`/`logCount` audit logging, the `wasapi smoke`/`wasapiPath resolved?` lines, the `appendScreenshareDebug` import, and the 05-01 `build=` breadcrumb; reframe the header comment to describe the real fix (no "THROWAWAY"/spike wording). KEEP the load model, MessageChannelMain transport, PID resolution, before-quit teardown, and `stopWasapiLoopback`. Then DELETE `src/modules/screenshareDebug.ts`. Regenerate IPC with `bun run build --onlyGenerators` so gen.ts/types.ts drop the 3 `screenshareDebug:*` getters + the `appendScreenshareDebug` handler and gain `wasapiLoopback:shouldInjectWasapiTransport` (the `wasapiLoopback:stopWasapiLoopback` handler stays).
  </action>
  <acceptance_criteria>
    - `test ! -f src/modules/screenshareDebug.ts` (file deleted)
    - `grep -rn "screenshareDebug" src/ | grep -v '^\\s*//' | wc -l` is 0 (no references to the deleted module, including in regenerated IPC)
    - `grep -n "shouldInjectWasapiTransport" src/modules/native/wasapiLoopback.ts` shows the relocated, simplified getter
    - `grep -q "wasapiLoopback:shouldInjectWasapiTransport" src/windows/main/preload/preload.mts` and `... src/ipc/gen.ts`
    - `bun run build --onlyGenerators` exits 0; `grep -c "appendScreenshareDebug" src/modules/native/wasapiLoopback.ts` is 0
  </acceptance_criteria>
  <verify>
    <automated>bun run build --onlyGenerators && test ! -f src/modules/screenshareDebug.ts && ! grep -rq "screenshareDebug" src/ && grep -q "wasapiLoopback:shouldInjectWasapiTransport" src/ipc/gen.ts</automated>
  </verify>
  <done>The load-bearing gate is relocated + simplified, wasapiLoopback.ts is diagnostic-free, screenshareDebug.ts is gone, and IPC is regenerated cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Delete deliverySpike.ts, strip bridge.ts + preload.mts spike wiring, strip wasapiTransport.ts and add the ECHO-03 (D-11) swap guard</name>
  <files>src/windows/main/preload/deliverySpike.ts, src/windows/main/preload/bridge.ts, src/windows/main/preload/preload.mts, src/windows/main/preload/wasapiTransport.ts</files>
  <read_first>
    - src/windows/main/preload/deliverySpike.ts (genuine Phase-3 throwaway — delete outright)
    - src/windows/main/preload/bridge.ts — `deliverySpike` (46), `appendScreenshareDebug` (47), `feedWasapiChunk` (53, dead: main never sends `wasapi:pcm-chunk`), `stopWasapiLoopback` (50, KEEP — real STREAM_CLOSE teardown despite its wrong THROWAWAY comment)
    - src/windows/main/preload/preload.mts — `deliverySpike` import (9), `injectDeliverySpike()` (23, 35-43); KEEP `injectWasapiTransport()`
    - src/windows/main/preload/wasapiTransport.ts — `log()` helper (~45-51) + call sites, `chunks/dropped/underrun` counters, `__goofcordWasapiFeedChunk` fallback (~180-182), the `appendScreenshareDebug` field on `TransportBridge` (~40), the `GOOFCORD_TRANSPORT_SPIKE` header framing; `activePort` (declared ~88, set only on real port arrival ~168); the MessagePort receiver (~164-174), the readiness handshake (~224), and the getDisplayMedia swap (~194-218 — the remove+addTrack(gen) at ~200-204). KEEP feeder/receiver/handshake/swap.
    - 05-RESEARCH.md Q6 (the gap mechanism + the minimal `if (!activePort) return stream;` guard; race note: port forwarded synchronously before getDisplayMedia resolves on the supported path) and A3 (feedWasapiChunk dead); 05-PATTERNS.md "wasapiTransport.ts (ECHO-03 guard)" section + Strip-Seams table
  </read_first>
  <action>
    Delete `deliverySpike.ts`. In bridge.ts remove the `deliverySpike`, `appendScreenshareDebug`, and `feedWasapiChunk` fields; KEEP `stopWasapiLoopback`. In preload.mts remove the `deliverySpike` import and the `injectDeliverySpike()` call/definition; keep `injectWasapiTransport()`. In wasapiTransport.ts remove the `appendScreenshareDebug`-backed `log()` helper and all its call sites, the `chunks/dropped/underrun` logging counters, the `__goofcordWasapiFeedChunk` structured-clone fallback, and the `appendScreenshareDebug` field from `TransportBridge`; reframe/rename the header to drop the `GOOFCORD_TRANSPORT_SPIKE`/spike wording (this is the real feeder). Add the ECHO-03 guard at the TOP of the getDisplayMedia swap, before the remove+addTrack: when `activePort` is unset (unsupported build / `--no-wasapi` / activation returned false), `return stream` unchanged so the original Chromium `"loopback"` track stays and the viewer hears audio; only when `activePort` is set perform the existing remove-tracks + `addTrack(gen)` swap (the hardware-verified happy path, unchanged). KEEP the MSTG feeder, the MessagePort receiver, and the readiness handshake.
  </action>
  <acceptance_criteria>
    - `test ! -f src/windows/main/preload/deliverySpike.ts`
    - `grep -rn "deliverySpike\\|feedWasapiChunk\\|wasapi:pcm-chunk\\|__goofcordWasapiFeedChunk\\|GOOFCORD_TRANSPORT_SPIKE" src/ | wc -l` is 0
    - `grep -q "stopWasapiLoopback" src/windows/main/preload/bridge.ts` (kept)
    - `grep -q "if (!activePort) return stream" src/windows/main/preload/wasapiTransport.ts` (the D-11 guard precedes the swap)
    - `grep -c "appendScreenshareDebug" src/windows/main/preload/wasapiTransport.ts` is 0
  </acceptance_criteria>
  <verify>
    <automated>test ! -f src/windows/main/preload/deliverySpike.ts && ! grep -rq "deliverySpike\|feedWasapiChunk\|GOOFCORD_TRANSPORT_SPIKE" src/ && grep -q "activePort" src/windows/main/preload/wasapiTransport.ts</automated>
  </verify>
  <done>The Phase-3 throwaway is gone, the bridge/preload spike wiring is stripped, the feeder is diagnostic-free, and the unsupported path no longer swaps a silent track (ECHO-03 fixed) while the supported path is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Strip screenshare.ts, add the one surviving fallback log, and prove a green build + strip completeness</name>
  <files>src/windows/screenshare/screenshare.ts</files>
  <read_first>
    - src/windows/screenshare/screenshare.ts — the `appendScreenshareDebug` import (7), the 2 call sites (111, 114), the 3-way gate (94-116) where `tryStartWasapiLoopback()` returns false at ~101 → the `else` sets `result.audio = "loopback"` (KEEP the gate); current imports (it does NOT import `picocolors`)
    - 05-RESEARCH.md Q5 ("screenshare.ts" strip + the exact surviving line text) and D-13; 05-PATTERNS.md "screenshare.ts (the ONE surviving log)" — `pc.cyan("[Screenshare]")` convention from patchcord.ts/venbind.ts, mirrored by wasapiLoopback.ts:37
  </read_first>
  <action>
    In screenshare.ts remove the `appendScreenshareDebug` import (line 7) and both diagnostic call sites (111, 114). On the fallback (`else`) branch, add EXACTLY ONE conventional log: `console.log(pc.cyan("[Screenshare]"), "WASAPI process-loopback unsupported on this build, using loopback fallback");` (add `import pc from "picocolors";`). Delete the `path=win32-wasapi-exclude-tree` debug line at 111 with no replacement (the happy path stays quiet). KEEP the 3-way gate logic byte-for-byte otherwise. Then run the full type check and build, and the whole-`src/` strip-completeness grep.
  </action>
  <acceptance_criteria>
    - `grep -rn "appendScreenshareDebug\|screenshare-debug.log\|syncCrumb\|deliverySpike\|screenshareDebug" src/` returns 0 lines
    - Exactly ONE `console.log` exists on the wasapi fallback branch in screenshare.ts, prefixed `pc.cyan("[Screenshare]")`
    - `bun run check` passes (tsgo, strict, noEmit); `bun run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>! grep -rq "appendScreenshareDebug\|screenshare-debug.log\|syncCrumb\|deliverySpike\|screenshareDebug" src/ && bun run check && bun run build</automated>
  </verify>
  <done>The source carries exactly one conventional fallback log line, all diagnostic scaffolding is gone from src/, and the project type-checks and builds green.</done>
</task>

</tasks>

<verification>
- `grep -rn "appendScreenshareDebug\|screenshare-debug.log\|syncCrumb\|deliverySpike\|screenshareDebug" src/` → 0 (SC#2).
- The ECHO-03 guard is present and gates the swap on `activePort`; the supported happy-path swap is unchanged.
- `bun run build --onlyGenerators`, `bun run check`, and `bun run build` all green.
- The CI / build-script strip (.github/testBuild.yml, build.ts envPath) is owned by 05-04 (parallel, same wave); the whole-tree strip-completeness assertion lands in 05-05 once both complete.
</verification>

<success_criteria>
SC#2 (source half): all diagnostic instrumentation stripped from shipped source; exactly one conventional fallback log line. ECHO-03 honest fallback (D-11) in place without regressing the verified happy path.
</success_criteria>

<output>
Create `.planning/phases/05-verification-upstream-pr/05-03-SUMMARY.md` when done.
</output>
