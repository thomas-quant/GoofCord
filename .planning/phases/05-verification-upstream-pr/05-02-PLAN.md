---
phase: 05-verification-upstream-pr
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - wasapi-loopback-repo/package.json
  - wasapi-loopback-repo/LICENSE
  - wasapi-loopback-repo/NOTICE
  - wasapi-loopback-repo/.gitignore
  - wasapi-loopback-repo/README.md
  - wasapi-loopback-repo/.github/workflows/build.yml
  - wasapi-loopback-repo/Cargo.toml
  - wasapi-loopback-repo/Cargo.lock
  - wasapi-loopback-repo/build.rs
  - wasapi-loopback-repo/src/lib.rs
autonomous: true
requirements: [UPST-02]

must_haves:
  truths:
    - "A ready-to-push standalone addon repo dir exists, de-private-d, MIT-licensed, clean-room (MS NOTICE retained), so the user can create thomas-quant/wasapi-loopback and push it (SC#4)"
    - "The repo's own windows-latest CI builds the prebuilt .node and commits it to the EXACT path build.ts already reads: prebuilds/windows-x86_64/wasapi-loopback-win32-x64.node (SC#4)"
    - "The package.json os/cpu fields make bun skip the install entirely on Linux/macOS (free D-16 no-op)"
  artifacts:
    - path: "wasapi-loopback-repo/package.json"
      provides: "Publishable addon manifest (no private, os/cpu/files/repository, napi.name wasapi-loopback)"
      contains: "wasapi-loopback"
    - path: "wasapi-loopback-repo/.github/workflows/build.yml"
      provides: "windows-latest napi build + normalize + commit-back CI"
      contains: "windows-latest"
    - path: "wasapi-loopback-repo/LICENSE"
      provides: "MIT license body (addon author copyright), coexisting with the retained MS NOTICE (ECHO-04)"
      contains: "MIT License"
  key_links:
    - from: "wasapi-loopback-repo/.github/workflows/build.yml"
      to: "wasapi-loopback-repo/prebuilds/windows-x86_64/wasapi-loopback-win32-x64.node"
      via: "napi build → pwsh normalize → git add -f commit-back"
      pattern: "prebuilds/windows-x86_64/wasapi-loopback-win32-x64\\.node"
---

## Phase Goal

**As a** GoofCord Windows user, **I want to** receive the #46 echo fix as a verified, instrumentation-free, upstream-merged change, **so that** sharing system audio never echoes the call back to viewers — proven on the actual shipped build, not a debug spike.

<objective>
D-09/D-10/SC#4: Prepare a ready-to-push standalone addon repo directory (`wasapi-loopback-repo/`) extracted from the in-tree `native/wasapi-loopback/` crate, with the publish-deltas applied and its own `windows-latest` napi CI that commits the prebuilt `.node`. The USER creates the GitHub repo and pushes (05-05 checkpoint); Claude only prepares the dir.

Purpose: The GoofCord-side echo fix consumes the addon via one `optionalDependencies` line `github:thomas-quant/wasapi-loopback` (D-08), mirroring patchcord. That ref must resolve to a repo whose committed prebuild lands at the path `build.ts:207` already reads.

Output: `wasapi-loopback-repo/` staging dir (UNTRACKED in GoofCord — it becomes its own repo; do not commit it into GoofCord history).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-verification-upstream-pr/05-RESEARCH.md
@.planning/phases/05-verification-upstream-pr/05-PATTERNS.md
@native/wasapi-loopback/package.json
@native/wasapi-loopback/NOTICE
@native/wasapi-loopback/README.md
@native/wasapi-loopback/.gitignore
@.github/workflows/testBuild.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy the crate to a staging repo dir and apply the package.json / LICENSE / .gitignore publish-deltas</name>
  <files>wasapi-loopback-repo/package.json, wasapi-loopback-repo/LICENSE, wasapi-loopback-repo/NOTICE, wasapi-loopback-repo/.gitignore, wasapi-loopback-repo/Cargo.toml, wasapi-loopback-repo/Cargo.lock, wasapi-loopback-repo/build.rs, wasapi-loopback-repo/src/lib.rs</files>
  <read_first>
    - native/wasapi-loopback/ (package.json, NOTICE lines 18-38 = the MIT body, .gitignore, Cargo.toml, Cargo.lock, build.rs, src/lib.rs) — the extraction base
    - 05-RESEARCH.md Q3 (the exact publish delta table) and Package Legitimacy Audit (first-party, MIT, approved)
    - 05-PATTERNS.md sections "wasapi-loopback/package.json", "wasapi-loopback/LICENSE" (anti-analog: do NOT copy root /LICENSE which is OSL-3.0), "wasapi-loopback/.gitignore"
    - node_modules/venbind/package.json (the os/cpu pattern), node_modules/patchcord/package.json (the files-array shape, no postinstall/prepare)
  </read_first>
  <action>
    Copy the full `native/wasapi-loopback/` tree to a new top-level `wasapi-loopback-repo/` dir (keep `Cargo.lock` for reproducible builds; do not copy `target/`). In `package.json`: DROP `"private": true`; ADD `"os": ["win32"], "cpu": ["x64"]` (makes bun skip install off-Windows — D-16); ADD `"files": ["prebuilds","src","Cargo.toml","Cargo.lock","build.rs","NOTICE","README.md","LICENSE"]`; ADD `"repository"` and `"homepage"` pointing at `thomas-quant/wasapi-loopback`; KEEP `napi.name = "wasapi-loopback"`; do NOT add any `postinstall`/`prepare` (patchcord/venbind have none — delivery is a committed binary). Create `LICENSE` using the MIT body verbatim from `NOTICE` lines 18-38 but with the addon author's copyright line (the user's name/year); KEEP `NOTICE` (Microsoft MIT) alongside it for ECHO-04 clean-room provenance. Edit `.gitignore` so the committed prebuild is TRACKED — add `!prebuilds/**/*.node` exception under the `*.node` ignore (CI's `git add -f` is the backstop).
  </action>
  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('wasapi-loopback-repo/package.json'))"` succeeds and the parsed object has NO `private`, has `os:["win32"]`, `cpu:["x64"]`, a `files` array, and `napi.name === "wasapi-loopback"`
    - `wasapi-loopback-repo/LICENSE` exists and starts with `MIT License` with a non-Microsoft copyright line; `wasapi-loopback-repo/NOTICE` still contains the Microsoft MIT copyright
    - `grep -n "prebuilds" wasapi-loopback-repo/.gitignore` shows the un-ignore exception
    - `wasapi-loopback-repo/` is NOT staged in GoofCord git (`git status --porcelain wasapi-loopback-repo` shows untracked or it is gitignored)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const p=require('./wasapi-loopback-repo/package.json'); if(p.private||!p.os||!p.files||p.napi.name!=='wasapi-loopback')process.exit(1)" && grep -q "MIT License" wasapi-loopback-repo/LICENSE</automated>
  </verify>
  <done>The crate is staged as a de-private-d, MIT, off-Windows-skipping publishable package with the MS NOTICE retained.</done>
</task>

<task type="auto">
  <name>Task 2: Add the windows-latest napi CI and rewrite the README for optionalDependency consumption</name>
  <files>wasapi-loopback-repo/.github/workflows/build.yml, wasapi-loopback-repo/README.md</files>
  <read_first>
    - .github/workflows/testBuild.yml lines 29-72 — the proven setup-bun + Rust toolchain + `napi build --release --target x86_64-pc-windows-msvc` + pwsh normalize recipe (the load-bearing block to lift)
    - 05-RESEARCH.md Q2 (the full standalone build.yml skeleton: workflow_dispatch, permissions contents:write, the commit-back step; `git add -f` required because .gitignore ignores *.node; normalize name MUST contain BOTH win32 AND x64)
    - 05-PATTERNS.md "wasapi-loopback/.github/workflows/build.yml" (what changes vs the analog: drop matrix/GoofCord build/upload steps; replace the GITHUB_ENV export with the prebuilds/ write + commit-back)
    - native/wasapi-loopback/README.md lines 13-19 (Clean-room provenance — KEEP verbatim, load-bearing for ECHO-04), lines 51-70 (Building/Status — edit)
  </read_first>
  <action>
    Create `.github/workflows/build.yml` for the addon repo on `windows-latest`: `workflow_dispatch` trigger, `permissions: contents: write`, steps = checkout, setup-bun, dtolnay/rust-toolchain stable with target x86_64-pc-windows-msvc, then a pwsh step that runs `bun install`, `bun x napi build --release --target x86_64-pc-windows-msvc`, finds the produced `*.node`, errors if none, and copies it to `prebuilds/windows-x86_64/wasapi-loopback-win32-x64.node` (the EXACT path build.ts reads; name MUST contain both `win32` and `x64`), then a commit-back step (`git add -f prebuilds/windows-x86_64/wasapi-loopback-win32-x64.node`, commit + push only if changed). Single target only (D-10). In `README.md`: KEEP the Clean-room provenance section verbatim; DROP the `GOOFCORD_WASAPI_LOOPBACK_PATH` env-override note and the "In-repo during Phase 4 … slated to move" Status section; replace with: consume via `github:thomas-quant/wasapi-loopback` in `optionalDependencies`; CI builds + commits `prebuilds/windows-x86_64/wasapi-loopback-win32-x64.node`.
  </action>
  <acceptance_criteria>
    - `grep -c "windows-latest" wasapi-loopback-repo/.github/workflows/build.yml` is ≥ 1
    - The workflow references the exact normalized path `prebuilds/windows-x86_64/wasapi-loopback-win32-x64.node` and includes a `git add -f` commit-back step
    - `README.md` no longer mentions `GOOFCORD_WASAPI_LOOPBACK_PATH`; it documents the `github:thomas-quant/wasapi-loopback` optionalDependency consumption; the Clean-room provenance section is intact
  </acceptance_criteria>
  <verify>
    <automated>grep -q "windows-latest" wasapi-loopback-repo/.github/workflows/build.yml && grep -q "prebuilds/windows-x86_64/wasapi-loopback-win32-x64.node" wasapi-loopback-repo/.github/workflows/build.yml && ! grep -q "GOOFCORD_WASAPI_LOOPBACK_PATH" wasapi-loopback-repo/README.md</automated>
  </verify>
  <done>The staging repo has a windows-latest CI that produces + commits the correctly-named prebuilt, and a README that documents optionalDependency consumption while retaining clean-room provenance.</done>
</task>

</tasks>

<verification>
- `wasapi-loopback-repo/` is a complete, ready-to-push package: manifest valid, MIT LICENSE + MS NOTICE, off-Windows-skip fields, single-target windows-latest CI writing the exact glob-matched prebuild path.
- No postinstall/prepare (matches patchcord/venbind committed-binary delivery).
</verification>

<success_criteria>
SC#4: the separate native addon repo is shaped for publication — clean-room (MIT + retained MS NOTICE, no Discord symbols), builds its prebuilt `.node` on windows-latest, ships `wasapi-loopback-win32-x64.node` named to match the `nativeModulePlugin` glob.
</success_criteria>

<output>
Create `.planning/phases/05-verification-upstream-pr/05-02-SUMMARY.md` when done.
</output>
