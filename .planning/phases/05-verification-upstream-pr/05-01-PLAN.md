---
phase: 05-verification-upstream-pr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/native/wasapiLoopback.ts
  - .planning/phases/05-verification-upstream-pr/05-VERIFICATION.md
autonomous: false
requirements: [UPST-02]

must_haves:
  truths:
    - "On the instrumented Windows CI build, a second-device viewer hears the shared desktop audio AND hears no Discord-call echo (re-confirms ECHO-01 on the rich build, SC#1)"
    - "The detected Windows build number is captured in the verification report (was previously unlogged anywhere — RESEARCH Q7 gap)"
    - "The excluded root PID with the Audio Service PID inside its app.getAppMetrics() subtree, the native-branch-activated log line, and the streamed chunk counts are recorded from the WSL-readable debug log"
  artifacts:
    - path: ".planning/phases/05-verification-upstream-pr/05-VERIFICATION.md"
      provides: "Rich-build verification section (build#, PIDs, activation line, viewer-audible + no-echo ground truth)"
      contains: "build="
  key_links:
    - from: "src/modules/native/wasapiLoopback.ts"
      to: "screenshare-debug.log"
      via: "os.release() build-number breadcrumb at activation (rich build only; stripped in 05-03)"
      pattern: "os\\.release\\(\\)"
---

## Phase Goal

**As a** GoofCord Windows user, **I want to** receive the #46 echo fix as a verified, instrumentation-free, upstream-merged change, **so that** sharing system audio never echoes the call back to viewers — proven on the actual shipped build, not a debug spike.

> Note: the ROADMAP Phase 5 `**Goal:**` line is prose, not user-story form. This story is derived from the milestone Core Value (the Windows user's echo-free screenshare). Phase 5 ships that fix verified + clean.

<objective>
D-14 STEP 1 (verify rich): Capture the FULL structured verification report on a build that STILL has instrumentation, BEFORE any strip. Add the one missing evidence line (Windows build number), trigger the Windows x64 CI build, have the user run the two-device viewer test, then read the WSL-accessible debug log and write the rich-build section of the report.

Purpose: D-14 resolves the "stripping removes the evidence logging" tension by capturing rich evidence first. SC#1 + PITFALLS V1 require the detected build number, which is not logged anywhere today (RESEARCH Q7 / Open Question 2).

Output: `05-VERIFICATION.md` rich-build section with build#, excluded-root/Audio-Service PIDs, activation line, chunk counts, and the human-supplied viewer-audible + no-echo ground truth.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-verification-upstream-pr/05-CONTEXT.md
@.planning/phases/05-verification-upstream-pr/05-RESEARCH.md
@.planning/phases/04-native-clean-room-exclude-tree-addon-integration/04-03-SUMMARY.md
@src/modules/native/wasapiLoopback.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the build-number breadcrumb to the rich build, push, and trigger the Windows CI build</name>
  <files>src/modules/native/wasapiLoopback.ts</files>
  <read_first>
    - src/modules/native/wasapiLoopback.ts — the activation success path (where it already logs `wasapi exclude-root=<PID> audioService=<PID>` and `[sync] start() returned ok=true`); confirm the existing `import os` / add it if absent, and the existing `appendScreenshareDebug(...)` discipline (this is the RICH build — KEEP the instrumentation, do not strip yet)
    - 05-RESEARCH.md Q7 (the build-number gap; `os.release()` on Windows returns `"10.0.<build>"`) and Open Question 2
    - .github/workflows/testBuild.yml — the `workflow_dispatch` trigger and the win x64 job; confirm the in-CI Rust build step still present (it MUST still be present for this rich build)
  </read_first>
  <action>
    Add ONE diagnostic breadcrumb at the activation point in wasapiLoopback.ts that logs `os.release()` (Windows → `"10.0.<build>"`) alongside the existing exclude-root PID line, using the SAME `appendScreenshareDebug(...)` discipline already in the file (e.g. an `appendScreenshareDebug` call carrying `wasapi build=<os.release()>`). This is the rich build only — it is stripped in 05-03 along with all other instrumentation. Do NOT add `--no-wasapi` handling or touch the gate. Commit on the working branch `fix/windows-screenshare-cancel-restart`, push to `origin`, then trigger the Windows x64 CI build via `gh workflow run` on `testBuild.yml` for that ref (or push to trigger if the workflow runs on push). Record the resulting run id/URL for the human handoff.
  </action>
  <acceptance_criteria>
    - `grep -n "os.release()" src/modules/native/wasapiLoopback.ts` returns the new breadcrumb line
    - The breadcrumb routes through the existing `appendScreenshareDebug`/`[sync]` path (it must reach `screenshare-debug.log`)
    - `gh run list --workflow testBuild.yml` shows a new run for the pushed ref; the run completes green and produces the `win-artifacts` download
  </acceptance_criteria>
  <verify>
    <automated>grep -n "os.release()" src/modules/native/wasapiLoopback.ts && gh run list --workflow testBuild.yml --limit 1</automated>
  </verify>
  <done>The instrumented build adds a Windows build-number line and a green Windows x64 CI artifact is available for the human to install.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Two-device viewer verification of the instrumented (rich) build</name>
  <files>none (manual on-hardware verification)</files>
  <what-built>An instrumented Windows x64 CI artifact (the rich build) with the native WASAPI EXCLUDE-tree path active and a new build-number breadcrumb.</what-built>
  <read_first>
    - .planning/research/PITFALLS.md — the two-device viewer-side protocol (V1 build# + branch, V2 no echo, V3 non-call audio playing)
    - 05-RESEARCH.md Q7 (human-supplied ground truth: viewer hears desktop audio; viewer hears no echo)
  </read_first>
  <action>
    The user performs the manual two-device test described in how-to-verify (Claude cannot supply viewer-side audio ground truth — the streamer cannot self-verify because Electron mutes local echo). Claude pauses here until the user reports results.
  </action>
  <how-to-verify>
    1. Download the `win-artifacts` from the CI run recorded in Task 1; install/run it on the Windows 19045 dev box (run with NO transport-spike env, the normal path).
    2. Join a Discord call from a SECOND device/account (the viewer). Keep non-call audio actively playing on the streamer box (e.g. Spotify) — WASAPI loopback is silent on idle (PITFALLS V3).
    3. Start a screenshare WITH audio. On the viewer device confirm: (a) you HEAR the shared desktop audio, and (b) you do NOT hear the Discord call echoed back.
    4. (D-15 / D-11 nuance) Also do an un-flagged 19045 run where the native path is unavailable/forced-fail if practical, and a `--no-wasapi` run, noting whether the viewer hears audio (not silence) on the fallback — this seeds the post-strip re-confirm; the dispositive D-11 test happens in 05-06 after the guard lands.
    5. Report: build number shown by the OS, viewer-audible (yes/no), echo-heard (yes/no), any crash.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms viewer HEARS desktop audio (capture works, not silence)
    - Human confirms viewer hears NO call echo (exclude-tree works)
    - No crash on share
  </acceptance_criteria>
  <verify>
    <human-check>User reports: OS build number, viewer-audible (yes), echo-heard (no), no crash.</human-check>
  </verify>
  <resume-signal>Type "verified" with the viewer-audible + no-echo results and the OS build number, or describe the failure.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Read the debug log from WSL and write the rich-build verification section</name>
  <files>.planning/phases/05-verification-upstream-pr/05-VERIFICATION.md</files>
  <read_first>
    - /mnt/c/Users/Christ/AppData/Roaming/goofcord/screenshare-debug.log (WSL-readable) — the log-signature table in 05-RESEARCH.md Q7 (addon-require-ok, start() ok=true, exclude-root/audioService PIDs, path=win32-wasapi-exclude-tree, chunks=N→…, mechanism=MSTG, AND the new build= line)
    - Project memory: the log is append-only/cumulative across runs and contains historical Phase-3/Phase-4 spike lines — verify by signature + TIMELINE (latest run), confirm `path=win32-wasapi-exclude-tree` (real) NOT `spike-synthetic`
    - 04-03-SUMMARY.md — the prior verified signature for cross-reference (CI 27044504559)
  </read_first>
  <action>
    Read the latest run's lines from the WSL path. Create `05-VERIFICATION.md` with a "Rich build (instrumented, pre-strip)" section recording: detected Windows build number (from the new `build=` line), the native-branch-activated line (`audio=none path=win32-wasapi-exclude-tree`), excluded root PID + Audio Service PID confirmed within its subtree, chunk counts, the N-API smoke line, AND the human-supplied (a) viewer-hears-audio / (b) no-echo ground truth with the CI run id. Tie each datum to the exact log signature. Leave a "Final shipping-shape re-confirm" section as a placeholder to be completed in 05-06.
  </action>
  <acceptance_criteria>
    - `05-VERIFICATION.md` exists with a populated rich-build section
    - The section names the detected build number, the excluded root PID, the Audio Service PID in subtree, and the native-path log line — all citing the log signature
    - The human viewer-audible + no-echo result and the CI run id are recorded
    - The placeholder for the final re-confirm section is present
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/05-verification-upstream-pr/05-VERIFICATION.md && grep -iq "build" .planning/phases/05-verification-upstream-pr/05-VERIFICATION.md</automated>
  </verify>
  <done>The rich-build verification evidence is captured in writing before any strip removes the logging.</done>
</task>

</tasks>

<verification>
- Rich-build section of 05-VERIFICATION.md is complete and grounded in the latest-run log signature (not recollection, not a stale spike run).
- Build number is present (closes the RESEARCH Q7 gap).
</verification>

<success_criteria>
SC#1 (partial — rich half): the instrumented build's build#, activation line, PIDs, chunk counts, and the viewer-side ground truth are recorded. The final shipping-shape re-confirm (D-14 step 3) is completed in 05-06.
</success_criteria>

<output>
Create `.planning/phases/05-verification-upstream-pr/05-01-SUMMARY.md` when done.
</output>
