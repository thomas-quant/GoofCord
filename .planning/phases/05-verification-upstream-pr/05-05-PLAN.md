---
phase: 05-verification-upstream-pr
plan: 05
type: execute
wave: 3
depends_on: ["05-02", "05-03", "05-04"]
files_modified:
  - bun.lock
autonomous: false
requirements: [UPST-02]

must_haves:
  truths:
    - "The thomas-quant/wasapi-loopback GitHub repo exists, pushed by the user, with its windows-latest CI having committed prebuilds/windows-x86_64/wasapi-loopback-win32-x64.node (D-09 user-identity checkpoint)"
    - "bun install on the GoofCord branch resolves github:thomas-quant/wasapi-loopback and drops the committed prebuild into node_modules at the exact path build.ts reads"
    - "A green Windows x64 CI build of the STRIPPED + dependency-packaged shape ships the .node (packaging assertion passes; app.asar grep finds the fork code + loadable addon) with NO in-CI Rust build"
    - "The latest-run debug log shows path=win32-wasapi-exclude-tree (real), NOT spike-synthetic — and the whole-tree strip is complete"
  artifacts:
    - path: "bun.lock"
      provides: "Lockfile resolving the new optionalDependency github ref"
      contains: "wasapi-loopback"
  key_links:
    - from: "package.json optionalDependencies github:thomas-quant/wasapi-loopback"
      to: "node_modules/wasapi-loopback/prebuilds/windows-x86_64/wasapi-loopback-win32-x64.node"
      via: "bun install clones the pushed repo with its CI-committed prebuild"
      pattern: "wasapi-loopback"
---

## Phase Goal

**As a** GoofCord Windows user, **I want to** receive the #46 echo fix as a verified, instrumentation-free, upstream-merged change, **so that** sharing system audio never echoes the call back to viewers — proven on the actual shipped build, not a debug spike.

<objective>
D-14 STEP 3 setup (HARD external dependency): The USER creates + pushes the `thomas-quant/wasapi-loopback` repo (their GitHub identity — Claude prepared the dir in 05-02) and lets its CI commit the prebuilt. Then Claude installs the optionalDependency on the GoofCord branch and produces a green Windows x64 CI build of the FINAL stripped/dependency-packaged shape.

Purpose: The final viewer-side re-confirm (05-06) must run against the ACTUAL shipped shape — addon arriving via the optionalDependency, all instrumentation stripped — not the instrumented Phase-4/05-01 build. This step is blocked until the user pushes the repo (RESEARCH "Missing dependency that gates the final step").

Output: a pushed addon repo with a committed prebuild; a GoofCord Windows CI artifact of the stripped/dependency-packaged build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-verification-upstream-pr/05-RESEARCH.md
@.planning/phases/05-verification-upstream-pr/05-02-PLAN.md
@.github/workflows/testBuild.yml
@package.json
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: User creates + pushes thomas-quant/wasapi-loopback and runs its prebuild CI</name>
  <files>none (user-identity GitHub action — Claude prepared the dir in 05-02)</files>
  <what-built>Claude prepared the ready-to-push standalone addon repo dir at `wasapi-loopback-repo/` (05-02): de-private-d manifest, MIT LICENSE + retained MS NOTICE, off-Windows-skip os/cpu fields, and a windows-latest napi CI that commits the prebuilt.</what-built>
  <read_first>
    - wasapi-loopback-repo/ (the prepared dir — confirm package.json, LICENSE, NOTICE, .gitignore, README.md, .github/workflows/build.yml, src/, Cargo.toml/lock are present)
    - 05-RESEARCH.md D-09 / Q2 (the user creates the GitHub repo + pushes; its CI builds + commits the prebuild) and Environment Availability (this BLOCKS the final re-confirm until pushed)
  </read_first>
  <action>
    The user creates the GitHub repo and pushes (this requires the user's GitHub identity — Claude does not create/push outward-facing repos). Claude pauses until the user confirms the repo exists with the CI-committed prebuilt on `main`.
  </action>
  <how-to-verify>
    1. Create a new GitHub repo `thomas-quant/wasapi-loopback` (public, MIT).
    2. From the prepared `wasapi-loopback-repo/` dir: `git init`, commit all files, add the remote, and push to `main`.
    3. In the repo's Actions tab, run the `Build prebuilt` (`workflow_dispatch`) workflow on `windows-latest`. Wait for it to finish and confirm it committed `prebuilds/windows-x86_64/wasapi-loopback-win32-x64.node` back to the repo.
    4. Confirm the prebuilt file is present in the repo tree on `main`.
  </how-to-verify>
  <acceptance_criteria>
    - `thomas-quant/wasapi-loopback` exists on GitHub and is reachable by `github:` ref
    - Its `main` contains `prebuilds/windows-x86_64/wasapi-loopback-win32-x64.node` (CI-committed)
  </acceptance_criteria>
  <verify>
    <human-check>User confirms the repo exists and the prebuilt `.node` is committed on `main` (pastes the repo URL).</human-check>
  </verify>
  <resume-signal>Type "pushed" once the repo exists and the prebuilt `.node` is committed on `main` (paste the repo URL).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install the optionalDependency, build the stripped/dependency-packaged Windows artifact, assert packaging</name>
  <files>bun.lock</files>
  <read_first>
    - package.json (the optionalDependencies github ref added in 05-04); build/build.ts copyNativeModules → copyNativeAddonsToOutDir (the path that consumes node_modules/wasapi-loopback/prebuilds/...)
    - .github/workflows/testBuild.yml (now Rust-free; the lean packaging assertion)
    - 05-RESEARCH.md Q1 (bun clones the github ref; committed prebuild lands at the read path) + Q7 log discipline (verify latest run path=win32-wasapi-exclude-tree, NOT spike-synthetic — the log is cumulative); project memory "grep app.asar for the marker before any CI test"
  </read_first>
  <action>
    On the GoofCord working branch run `bun install` so the new `github:thomas-quant/wasapi-loopback` optionalDependency resolves and the committed prebuild lands at `node_modules/wasapi-loopback/prebuilds/windows-x86_64/wasapi-loopback-win32-x64.node`. Commit the updated `bun.lock`. Push and trigger the Windows x64 CI build (`testBuild.yml`) for the branch. When green, confirm the lean packaging assertion passed (the `.node` present in `ts-out/native` + the packaged app) and grep the built `app.asar` for the fork code marker + the loadable `wasapi-loopback-win32-x64.node`. Also run the whole-tree strip-completeness grep across `src/ build/ .github/` to confirm only the single intentional fallback `console.log` survives and no diagnostic tokens remain (both 05-03 and 05-04 are complete by this wave). Record the CI run id for the 05-06 re-confirm.
  </action>
  <acceptance_criteria>
    - `bun install` resolves `wasapi-loopback`; `node_modules/wasapi-loopback/prebuilds/windows-x86_64/wasapi-loopback-win32-x64.node` exists after install on a Windows host (on Linux/WSL it is correctly SKIPPED via os:["win32"])
    - The Windows CI run is green and the packaging assertion passes; `app.asar` contains `wasapi-loopback-win32-x64.node`
    - `grep -rn "appendScreenshareDebug\|screenshare-debug.log\|syncCrumb\|deliverySpike\|screenshareDebug\|GOOFCORD_WASAPI_LOOPBACK_PATH\|GOOFCORD_TRANSPORT_SPIKE\|DIAGNOSTICS" src/ build/ .github/` returns 0 lines
    - The latest debug-log run shows `path=win32-wasapi-exclude-tree` (not `spike-synthetic`)
  </acceptance_criteria>
  <verify>
    <automated>! grep -rqE "appendScreenshareDebug|screenshare-debug.log|syncCrumb|deliverySpike|screenshareDebug|GOOFCORD_WASAPI_LOOPBACK_PATH|GOOFCORD_TRANSPORT_SPIKE|DIAGNOSTICS" src/ build/ .github/ && grep -q "wasapi-loopback" bun.lock</automated>
  </verify>
  <done>The optionalDependency resolves, the stripped/dependency-packaged Windows artifact ships the addon and passes the packaging assertion, and the whole tree is strip-clean.</done>
</task>

</tasks>

<verification>
- The addon repo is live and its CI-committed prebuild resolves via the optionalDependency.
- A green Windows artifact of the stripped/dependency-packaged shape ships the `.node`; whole-tree strip is complete; latest log run is the real native path.
</verification>

<success_criteria>
Sets up SC#1 final re-confirm (05-06) and confirms SC#2/SC#3 on the actual shipped shape (instrumentation stripped, native via optionalDependency, addon present in artifact).
</success_criteria>

<output>
Create `.planning/phases/05-verification-upstream-pr/05-05-SUMMARY.md` when done.
</output>
