---
phase: 05-verification-upstream-pr
plan: 07
type: execute
wave: 5
depends_on: ["05-06"]
files_modified:
  - .planning/phases/05-verification-upstream-pr/05-PR-DESCRIPTION.md
autonomous: false
requirements: [UPST-02]

must_haves:
  truths:
    - "A clean PR branch pr/fix-windows-screenshare-echo exists off upstream/main with ZERO .planning/ and ci-artifacts/ files, mirroring PR #210's clean-branch precedent (D-03)"
    - "The branch is staged into a few logical commits (not the 92-commit trail, not one squash) limited to the 9-item surgical surface; the getPlatformString fix is kept OUT of the echo PR (D-04, D-12)"
    - "A ready-to-paste PR description exists: no AI mention, no Summary/Why/Testing skeleton, authentic verification-first engineer voice closing #46 (D-05, D-06)"
    - "The user opens the PR against Milkshiift/GoofCord on their own identity; PR #210 is left untouched (D-02, D-07)"
  artifacts:
    - path: ".planning/phases/05-verification-upstream-pr/05-PR-DESCRIPTION.md"
      provides: "The ready-to-paste, verification-first PR body (Closes #46)"
      contains: "Closes #46"
  key_links:
    - from: "pr/fix-windows-screenshare-echo"
      to: "upstream/main"
      via: "branched off upstream/main with only the 9-item code surface, 0 .planning/"
      pattern: "pr/fix-windows-screenshare-echo"
---

## Phase Goal

**As a** GoofCord Windows user, **I want to** receive the #46 echo fix as a verified, instrumentation-free, upstream-merged change, **so that** sharing system audio never echoes the call back to viewers — proven on the actual shipped build, not a debug spike.

<objective>
D-01..D-07 + D-12 (SC#3): Shape the surgical, upstream-PR-ready echo fix — a clean branch off `upstream/main` with the 9-item diff surface staged into a few logical commits and a ready-to-paste, verification-first description that closes #46 — then hand it to the USER to open (D-02; opening against Milkshiift/GoofCord is outward-facing on the user's identity).

Purpose: Win on substance with the AI-averse maintainer (rejected #200/#148/#193 for slop/no-verification). The PR leads with real hardware verification, the mechanism, and the genuine debugging war-stories; no AI mention, no slop template.

Output: `pr/fix-windows-screenshare-echo` branch + `05-PR-DESCRIPTION.md`; the user opens the PR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/workflows/pr-branch.md
</execution_context>

<context>
@.planning/phases/05-verification-upstream-pr/05-RESEARCH.md
@.planning/phases/05-verification-upstream-pr/05-CONTEXT.md
@.planning/phases/05-verification-upstream-pr/05-VERIFICATION.md
@.planning/phases/04-native-clean-room-exclude-tree-addon-integration/04-03-SUMMARY.md
@.planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-FINDINGS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the clean echo-fix PR branch off upstream/main (9-item surface, a few logical commits, 0 .planning/)</name>
  <files>(git) pr/fix-windows-screenshare-echo</files>
  <read_first>
    - 05-RESEARCH.md Q4 (the full clean-branch flow: branch off `upstream/main`; bring over only the net code diff; stage ~4 logical commits; the #210 precedent = 3 clean commits, 0 `.planning/`, manually crafted not gsd-pr-branch-named) + the "Final Surgical Diff Surface" 9-item list + Pitfall 4 + A5 (remove native/wasapi-loopback/ from the PR branch)
    - D-03 (gsd-pr-branch as the `.planning/`-filter mechanism, but it does NOT squash and DOES keep structural `.planning/` — for an upstream PR do the manual #210-style recraft), D-04 (a few logical commits), D-12 (getPlatformString OUT of this PR)
    - git: `upstream/main` (eebb15d) fetched; `pr/fix-screenshare-cancel-restart` precedent branch; current branch `fix/windows-screenshare-cancel-restart`
  </read_first>
  <action>
    Create `pr/fix-windows-screenshare-echo` off `upstream/main`. Bring over ONLY the net code diff of the 9-item surgical surface (wasapiLoopback.ts + relocated getter; wasapiTransport.ts renamed + gated swap; preload.mts wiring; bridge.ts stopWasapiLoopback field; screenshare.ts 3-way gate + the one surviving log; regenerated gen.ts/types.ts; build.ts prebuild entry + copyNativeAddonsToOutDir; electron-builder.ts asarUnpack ONLY; package.json optionalDependency line). EXCLUDE: all `.planning/` and `ci-artifacts/`, `screensharePatch.ts` (downloaded from upstream), the deleted `screenshareDebug.ts`/`deliverySpike.ts` (never existed upstream), `.github/workflows/testBuild.yml` (fork CI), and the in-tree `native/wasapi-loopback/` dir (now its own repo — A5). KEEP the getPlatformString hunk OUT of this branch (D-12 — capture it as a separate standalone commit/branch for a possible follow-up PR; include only the asarUnpack hunk of electron-builder.ts here). Stage the surface into a few logical commits with authentic messages following the Q4 boundaries: (1) native WASAPI exclude-tree capture (wasapiLoopback.ts + wasapiTransport.ts feeder/gated-swap + preload wiring), (2) additive 3-way audio gate + ECHO-03 fallback gate + regenerated IPC (screenshare.ts + gen.ts/types.ts; the ECHO-03 guard rides with wasapiTransport.ts in commit 1), (3) package the prebuilt `.node` + optionalDependency (build.ts + electron-builder.ts asarUnpack + package.json).
  </action>
  <acceptance_criteria>
    - `git ls-tree -r pr/fix-windows-screenshare-echo --name-only | grep -c "^.planning/"` is 0; `... | grep -c "ci-artifacts/"` is 0
    - `git log --oneline upstream/main..pr/fix-windows-screenshare-echo` shows a few (≈3) logical commits, none exposing the debugging churn
    - `git diff --name-only upstream/main..pr/fix-windows-screenshare-echo` lists ONLY the 9-item surface (no screensharePatch.ts, no screenshareDebug.ts, no deliverySpike.ts, no testBuild.yml, no native/wasapi-loopback/)
    - `git diff upstream/main..pr/fix-windows-screenshare-echo -- electron-builder.ts` contains the asarUnpack change but NOT the getPlatformString change
  </acceptance_criteria>
  <verify>
    <automated>test "$(git ls-tree -r pr/fix-windows-screenshare-echo --name-only | grep -c '^.planning/')" = "0" && git diff --name-only upstream/main..pr/fix-windows-screenshare-echo</automated>
  </verify>
  <done>A clean, surgical echo-fix branch off upstream/main exists with a few logical commits, zero planning artifacts, and the getPlatformString fix excluded.</done>
</task>

<task type="auto">
  <name>Task 2: Write the ready-to-paste, verification-first PR description (no AI mention, no slop template)</name>
  <files>.planning/phases/05-verification-upstream-pr/05-PR-DESCRIPTION.md</files>
  <read_first>
    - 05-VERIFICATION.md (the completed report — lead with this evidence: build#, viewer hears desktop audio + no echo, excluded root PID + Audio Service PID in subtree, graceful fallback)
    - 04-03-SUMMARY.md "The debugging chain" (the non-fakeable war-stories: CoreMessaging dual-capture heap crash, PROPVARIANT ManuallyDrop heap fix, napi CalleeHandled (err,chunk), host-agnostic ts-out/native copy for Bun's Windows-host file-loader bug, asarUnpack)
    - 02-FINDINGS.md §2.1-2.3 (EXCLUDE-vs-INCLUDE, exclude the Electron tree incl. the Audio Service child, build floor ≈19041, clean-room GO) — the "how it works / why no echo" substance
    - D-05/D-06 (no AI mention; NO `## Summary / ## Why / ## Testing` skeleton; authentic engineer voice; maintainer framing "native audio capture, akin to venmic but for Windows"; clean-room from the public MS ApplicationLoopback sample); D-08 (note the maintainer may fork the addon repo under Milkshiift/ and flip the optionalDependency ref)
  </read_first>
  <action>
    Write `05-PR-DESCRIPTION.md` as the ready-to-paste PR body that Closes #46, in an authentic engineering voice (NO AI mention; NO Summary/Why/Testing skeleton). Structure: (1) open with the concrete two-device hardware verification — the detected Windows build, viewer hears the shared desktop audio AND no call echo, the excluded root PID with the Audio Service PID inside its app.getAppMetrics() subtree; (2) "how it works / why no echo" — same in-OS WASAPI per-process-tree EXCLUDE loopback Discord uses (venmic-for-Windows), independent clean-room Rust from the public MS ApplicationLoopback sample (MIT, retained), capturing everything except GoofCord's own Electron process tree, plus the web-client MediaStream bridge that's unique to wrapping the Discord WEB client; (3) the genuine debugging detail (CoreMessaging dual-capture heap crash, PROPVARIANT ManuallyDrop, the CalleeHandled (err,chunk) arg, the host-agnostic native copy for Bun's Windows-host file-loader bug, asarUnpack); (4) the surgical diff surface + that native ships via a separate clean-room addon repo (MIT, its own windows-latest CI) consumed by one optionalDependencies line, with a note the maintainer is welcome to fork it under Milkshiift/ and flip the ref; (5) graceful fallback (unsupported builds + Linux/macOS unchanged — the win32 gate + os-skip). Keep it tight and substance-first.
  </action>
  <acceptance_criteria>
    - `grep -q "Closes #46" .planning/phases/05-verification-upstream-pr/05-PR-DESCRIPTION.md`
    - The body contains NO "## Summary"/"## Why"/"## Testing" headings and NO mention of AI/Claude/LLM
    - It leads with the two-device verification evidence (build# + viewer-audible + no-echo) before any mechanism prose
    - It names the clean-room MS sample provenance and the separate addon-repo + optionalDependency delivery
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Closes #46" .planning/phases/05-verification-upstream-pr/05-PR-DESCRIPTION.md && ! grep -qiE "## Summary|## Why|## Testing|claude|\\bAI\\b|LLM" .planning/phases/05-verification-upstream-pr/05-PR-DESCRIPTION.md</automated>
  </verify>
  <done>A substance-first, verification-led PR description that closes #46 is ready to paste, with no AI mention and no slop template.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: User opens the upstream PR against Milkshiift/GoofCord</name>
  <files>none (user-identity GitHub action — Claude prepared the branch + description)</files>
  <what-built>Claude prepared the clean `pr/fix-windows-screenshare-echo` branch (9-item surface, a few logical commits, 0 .planning/) and the ready-to-paste `05-PR-DESCRIPTION.md` closing #46.</what-built>
  <read_first>
    - 05-PR-DESCRIPTION.md (the body to paste) + D-02 (the user opens the PR — outward-facing on the user's identity) + D-07 (leave PR #210 as-is) + D-01 (separate PR closing #46, NOT stacked on #210)
  </read_first>
  <action>
    The user pushes the branch and opens the PR (this uses the user's GitHub identity — Claude does not open/push outward-facing PRs). Claude pauses until the user confirms the PR is open.
  </action>
  <how-to-verify>
    1. Push `pr/fix-windows-screenshare-echo` to your `origin` (`thomas-quant/GoofCord`).
    2. Open a NEW pull request against `Milkshiift/GoofCord` `main` from that branch, separate from PR #210 (do not stack on it).
    3. Paste the body from `05-PR-DESCRIPTION.md` (confirm it Closes #46).
    4. Do NOT touch PR #210 (no comment, no edit). Optionally open the separate getPlatformString fix as its own small follow-up later (not part of this PR).
  </how-to-verify>
  <acceptance_criteria>
    - A new PR against Milkshiift/GoofCord exists from `pr/fix-windows-screenshare-echo`, body = the prepared description, Closes #46
    - PR #210 is unchanged
  </acceptance_criteria>
  <verify>
    <human-check>User confirms the new PR is open against Milkshiift/GoofCord (pastes the PR URL) and PR #210 is untouched.</human-check>
  </verify>
  <resume-signal>Type "opened" with the new PR URL, or describe what blocked you.</resume-signal>
</task>

</tasks>

<verification>
- The PR branch is clean (0 .planning/, 9-item surface, a few logical commits, getPlatformString excluded) and the description is substance-first with no AI mention / no slop template.
- The user has opened the separate PR closing #46; #210 untouched.
</verification>

<success_criteria>
SC#3 (PR shape) + UPST-02: a surgical, upstream-PR-ready diff (native via the prebuilt-`.node` optionalDependency pattern, instrumentation stripped) is opened upstream as a separate PR closing #46, with a verification-first description.
</success_criteria>

<output>
Create `.planning/phases/05-verification-upstream-pr/05-07-SUMMARY.md` when done.
</output>
