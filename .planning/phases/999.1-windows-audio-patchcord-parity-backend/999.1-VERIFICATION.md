---
phase: 999.1-windows-audio-patchcord-parity-backend
verified: 2026-07-04T05:55:33Z
status: human_needed
score: 10/12 must-haves verified (2 present, behavior-unverified)
behavior_unverified: 2
overrides_applied: 0
behavior_unverified_items:
  - truth: "On Windows, app-audio mode (INCLUDE) actually captures ONLY the chosen app's audio and is audible/self-free to a second-device viewer."
    test: "Play audio in a 3rd-party app, check ONLY that app in the picker, start a share, listen on a second device."
    expected: "The second-device viewer hears exactly that app's audio and NOT the user's own Discord call/voice (self-free); stop/restart the share once for idempotency."
    why_human: "Requires a real Windows box with live WASAPI audio sessions and a second listening device — no automated repro exists, and grep/type-check cannot observe captured audio content or COM activation success at runtime."
  - truth: "On Windows, the endpoint/source selector actually loopback-captures the chosen render endpoint (or the console default) and is audible to a second-device viewer; the Sonar render-vs-capture question is answered."
    test: "Open the picker in system mode, pick a specific render endpoint with audio playing, listen on a second device; also pick \"Default\"; check whether SteelSeries Sonar - Stream appears in the dropdown."
    expected: "The chosen endpoint's mix is heard exactly on the second device; \"Default\" captures the console default and preserves the zero-config process-exclude behavior for untouched users; the Sonar render-vs-capture finding is recorded."
    why_human: "Requires a real Windows box with real render endpoints (and ideally a Sonar/VAC setup) — cannot be exercised via static analysis; this is also the one open research question the plans explicitly deferred to on-box testing."
human_verification:
  - test: "CI: thomas-quant/wasapi-loopback Windows workflow compiles green for the INCLUDE + enumerator (Plan 01) and endpoint (Plan 03) bindings/feature gates; obtain the prebuilt .node."
    expected: "Windows build job is green; no missing windows-rs feature-gate/import errors; a .node exporting startIncludeProcessTree, listAudioApps, listRenderEndpoints, startRenderEndpoint, startDefaultRenderEndpoint is produced."
    why_human: "CI-only-build project rule — verifier is prohibited from running cargo build/check/clippy or triggering gh workflow run."
  - test: "CI: GoofCord testBuild.yml (--repo thomas-quant/GoofCord) produces win-artifacts consuming the wasapi-loopback .node from the step above."
    expected: "win-artifacts portable zip is produced and consumes the correct compiled addon (INCLUDE + endpoint symbols present)."
    why_human: "CI-only-build project rule — verifier must not trigger CI or push to any remote."
  - test: "BOX: app INCLUDE mode audible to a second-device viewer + self-free; system EXCLUDE default unchanged from #211; fail-closed = silence (never broad fallback); userData wasapi-capture.log shows per-share capture kind + verdict; no CoreMessaging crash."
    expected: "Second device hears only the chosen app's audio; default system share behaves identically to the shipped #211 fix; forced activation failure in app mode produces silence, never a Chromium loopback fallback; the log file records exclude-self / include-pid + verdict lines; no crash."
    why_human: "Requires a physical/second-device Windows audio test — no DevTools, no automated repro; this is the phase's explicit end-of-phase human checkpoint (human_verify_mode)."
  - test: "BOX: a chosen render endpoint audible to a second-device viewer; \"Default\" preserves the zero-config process-exclude default; endpoint failure = silence; the Sonar Stream render-endpoint-vs-capture-device open question is answered on-box and recorded."
    expected: "Selected endpoint's mix is heard exactly on the second device; Default preserves shipped behavior for normal users; a forced endpoint-activation failure produces silence; the Sonar finding (render endpoint vs. capture device) is documented, not silently assumed."
    why_human: "Requires a physical/second-device Windows audio test with real render endpoints (ideally including a VAC/Sonar setup) — the phase's explicit end-of-phase human checkpoint."
known_issues:
  - id: WR-01
    severity: warning
    description: "App INCLUDE mode passes `pids[0]` = undefined to the native addon with no explicit guard when the user selects app mode but checks no app (src/modules/native/wasapiLoopback.ts:279, src/windows/screenshare/preload/preload.mts:141). Fails closed (safe — the resulting napi throw is caught and mapped to failed-no-fallback) but produces a silent, unexplained audio-less share rather than user-visible feedback. Confirmed present in code by this verifier; not fixed (advisory per 999.1-REVIEW.md, not phase-blocking)."
  - id: WR-02
    severity: warning
    description: "stop()'s 1500ms-timeout detach path (native/wasapi-loopback/src/lib.rs, both mirrors) can leave a still-running capture thread alive across a fast re-click, risking a second concurrent WASAPI session (the CoreMessaging co-run condition) in a narrow timing window. Pre-existing #211 code, widened in surface by the new INCLUDE/endpoint modes but not introduced by them. Confirmed present in code by this verifier; not fixed (advisory per 999.1-REVIEW.md, not phase-blocking)."
---

# Phase 999.1: Windows audio — patchcord-parity capture backend Verification Report

**Phase Goal:** Grow the Windows WASAPI process-loopback addon from a fixed echo-fix into a patchcord-parity audio backend, scoped to EXACTLY TWO opt-in features — (1) per-app INCLUDE (single app, `PROCESS_LOOPBACK_MODE_INCLUDE_TARGET_PROCESS_TREE`) and (2) an endpoint/source selector (endpoint loopback at the fixed 48k/stereo/f32 format). EXCLUDE-self stays the zero-config `mode:"system"` default (#211). All AEC/self-cancel/multi-app/window-capture is DEFERRED/DEAD.

**Verified:** 2026-07-04T05:55:33Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Verification Method

This is a `human_verify_mode: end-of-phase` phase (see CLAUDE.md CI-only-build rule + all four plans' `checkpoint:human-verify` terminal tasks). Per the project's hard constraints, this verification:
- Did NOT run `bun run build`, `cargo build/check/clippy`, `gh workflow run`, or push to any remote.
- DID run the allowed static checks: `diff -q` mirror identity, `grep` structural checks, read-throughs of the fail-closed dispatch/gate branches, and `bun run check` / `bun run lint` (both noEmit/static, no compilation of native code).
- Confirms the mechanism was built correctly-by-construction (code-level). Runtime correctness (does the audio actually route/sound right on a Windows box) is NOT and cannot be verified here — it is explicitly deferred to the human CI + box gates documented in every plan's Task 4/3.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | WIN-APP-01: Addon exports `startIncludeProcessTree` (INCLUDE mode) + `listAudioApps` (session enumerator), both byte-identical across mirrors | ✓ VERIFIED | `diff -q native/wasapi-loopback/src/lib.rs wasapi-loopback-repo/src/lib.rs` exit 0; `grep -c startIncludeProcessTree` == 1, `grep -c listAudioApps` == 1, `grep -c PROCESS_LOOPBACK_MODE_INCLUDE_TARGET_PROCESS_TREE` == 2, `grep -c PROCESS_LOOPBACK_MODE_EXCLUDE_TARGET_PROCESS_TREE` == 3 (legacy path present); `struct AudioAppInfo { process_id, display_name, binary }` confirmed at lib.rs:851 |
| 2 | WIN-APP-01: TS wrapper dispatches `mode:"app"` → `startIncludeProcessTree(pids[0])`; win32 `audioNodes` populated from `listAudioApps` (own Audio Service PID dropped); advanced UI reachable via `isWasapiAudio` | ✓ VERIFIED | `wasapiLoopback.ts:279` dispatch call read directly; `screenshare.ts:44/listWin32AudioNodes()` drops the `Audio Service` child PID (line 46) and maps to `ShareableNode`; `preload.mts` gate generalized to `isPatchcordMode \|\| isWasapiAudio` (lines 142, 192, 234) |
| 3 | WIN-APP-01 (behavior): a real second-device viewer hears ONLY the selected app's audio when app INCLUDE mode is used, self-free | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Code path present + wired (see #1, #2); no automated repro exists for Windows COM audio capture. See Human Verification. |
| 4 | WIN-AUDIOCFG-01: fail-closed dispatch — only `mode:"system"`+`captureSource:"process-exclude"` may set `result.audio = "loopback"`; app mode and explicit-endpoint mode ALWAYS resolve to `failed-no-fallback` → `result.audio` stays unset | ✓ VERIFIED | `wasapiLoopback.ts:211-212` (`toleratesFallback` gate) + `screenshare.ts:139-161` read in full: the `unsupported-fallback-ok` arm is the ONLY arm assigning `result.audio = "loopback"`; the `failed-no-fallback` (else) arm explicitly does not. Independently confirmed by `999.1-REVIEW.md` ("no path where an app- or endpoint-mode native failure sets result.audio"). |
| 5 | WIN-AUDIOCFG-01: widened `AudioConfig { mode; pids; captureSource: "process-exclude"\|"endpoint"; endpointId: "default"\|string }` persists + normalizes old configs; schema default matches | ✓ VERIFIED | `preload.mts:19,21` (type), `:209,211-212` (normalization `??=`), `settingsSchema.ts:423` (default) all present; `bun run check` (tsgo, noEmit) exits 0 — a mismatch between the widened type and the schema default would fail this check |
| 6 | WIN-ENDPOINT-01: Addon exports `listRenderEndpoints` / `startRenderEndpoint` / `startDefaultRenderEndpoint`, all routed through the shared `initialize_loopback_client` at the fixed format — `GetMixFormat` is never called on the endpoint path | ✓ VERIFIED | `grep -c listRenderEndpoints/startRenderEndpoint/startDefaultRenderEndpoint` all == 1; `grep -c initialize_loopback_client` == 4 (helper def + process call + endpoint call + doc ref); `grep -c build_wave_format` == 3; the single `GetMixFormat` occurrence at lib.rs:379 is a comment documenting its deliberate non-use; `struct RenderEndpointInfo { id, name, isDefault }` confirmed at lib.rs:1035 |
| 7 | WIN-ENDPOINT-01: TS wrapper dispatches `system`+`endpoint` → `startRenderEndpoint`/`startDefaultRenderEndpoint`; payload carries `renderEndpoints`; `#endpoint-select` dropdown wired; `captureSource` derivation flips to `"endpoint"` ONLY for a non-`"default"` choice | ✓ VERIFIED | `wasapiLoopback.ts:261-274` dispatch read directly; `screenshare.ts:73,88` (`renderEndpoints` payload); `preload.mts:151-157` (`getFormSettings` derivation — `if (chosenEndpoint !== "default") { captureSource = "endpoint" ... }`, else the initialized `"process-exclude"`/`"default"` stand) |
| 8 | WIN-ENDPOINT-01 (behavior): a real second-device viewer hears exactly the chosen render endpoint's mix (or the console default); the Sonar render-vs-capture open question is answered | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Code path present + wired (see #6, #7); no automated repro for real WASAPI endpoint loopback. See Human Verification. |
| 9 | System-mode default (EXCLUDE-self, #211) remains behavior-unchanged — the process-exclude call site (`wasapi.start(process.pid, onChunk)`) is untouched by the new dispatch | ✓ VERIFIED | `wasapiLoopback.ts:280-286`: the `else` branch (system+process-exclude) is the same single unmodified call as pre-phase; `999.1-01-SUMMARY.md`/`999.1-REVIEW.md` both independently confirm the legacy EXCLUDE path is byte-behavior-preserved (thin-wrapper refactor, not a rewrite) |
| 10 | No dead/parked lineage (AEC, self-cancel, B1 subtraction, multi-app mixer) was introduced; `captureSource` has EXACTLY two literal values | ✓ VERIFIED | `grep -in "selfcancel\|self_cancel\|NLMS\|cancell\|ringbuffer\|ring_buffer"` across all 4 touched TS/Rust files returns zero matches; `captureSource: "process-exclude" \| "endpoint"` is the only union in `preload.mts`/`settingsSchema.ts`, enforced by a passing `bun run check` |
| 11 | Both Rust mirrors (`native/wasapi-loopback/` and `wasapi-loopback-repo/`) remain byte-identical after all 4 plans' edits | ✓ VERIFIED | `diff -q` on both `lib.rs` and `Cargo.toml` exit 0 (checked directly by this verifier, not just cited from the SUMMARYs) |
| 12 | Capture-kind + verdict diagnostics are written to a userData log file (no-DevTools requirement) | ✓ VERIFIED | `wasapiLoopback.ts:185` (`CAPTURE_LOG_PATH = path.join(userDataPath, "wasapi-capture.log")`), `:188` (`appendFile(...)`) confirmed directly |

**Score:** 10/12 truths verified (2 present, behavior-unverified — both are the on-box audio-audibility truths that this phase's own `checkpoint:human-verify` tasks defer to a human)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `native/wasapi-loopback/src/lib.rs` + `wasapi-loopback-repo/src/lib.rs` | INCLUDE + enumerator + endpoint exports, byte-identical | ✓ VERIFIED | All new exports present; `diff -q` clean |
| `native/wasapi-loopback/Cargo.toml` + mirror | New windows-rs feature gates for session/property-store types | ✓ VERIFIED | `Win32_UI_Shell_PropertiesSystem` / `Win32_Devices_FunctionDiscovery` present; `diff -q` clean |
| `src/modules/native/wasapiLoopback.ts` | `WasapiVerdict` + `tryStartWasapiLoopback(audioConfig)` config dispatch, fail-closed, userData log | ✓ VERIFIED | All dispatch branches + logging confirmed by direct read |
| `src/settingsSchema.ts` | Extended `screensharePreviousSettings.audioConfig` default | ✓ VERIFIED | Matches widened type; `bun run check` passes |
| `src/windows/screenshare/screenshare.ts` | win32 `audioNodes`/`renderEndpoints` population, verdict-branched gate | ✓ VERIFIED | Confirmed by direct read of `fetchScreenshareData` + `selectScreenshareSource` |
| `src/windows/screenshare/preload/preload.mts` | Widened `AudioConfig`, advanced UI (app checklist + endpoint dropdown), `getFormSettings` derivation | ✓ VERIFIED | Confirmed by direct read |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `preload.mts` `#endpoint-select` value | `audioConfig.captureSource`/`endpointId` | `getFormSettings` derivation | ✓ WIRED | Only a non-`"default"` selection flips `captureSource` (line 151-157) |
| `audioConfig` (from picker) | native addon start call | `tryStartWasapiLoopback` mode/captureSource branch | ✓ WIRED | Full 3-way dispatch confirmed (lines 261-286) |
| native start verdict | `result.audio` | `screenshare.ts` verdict branch | ✓ WIRED | Only `unsupported-fallback-ok` sets `result.audio="loopback"`; `failed-no-fallback` leaves it unset (fail-closed invariant intact) |
| `listAudioApps()`/`listRenderEndpoints()` (Rust) | picker checklist/dropdown | `wasapiLoopback.ts` accessors → `screenshare.ts` payload → `preload.mts` render | ✓ WIRED | `listWasapiAudioApps`/`listRenderEndpoints` accessors, `audioNodes`/`renderEndpoints` payload fields, and `renderAudioApps`/`#endpoint-select` population all confirmed by direct read |
| endpoint/process activation (Rust) | fixed 48k/stereo/f32 format | `initialize_loopback_client` shared helper | ✓ WIRED | 4 references (helper def + 2 call sites + doc); no divergent/mix-format path |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| WIN-APP-01 | 999.1-01, 999.1-02 | Per-app INCLUDE (addon + end-to-end wiring) | ✓ SATISFIED (code-level); runtime audibility PRESENT_BEHAVIOR_UNVERIFIED | Truths #1-#3, #9-#11 |
| WIN-ENDPOINT-01 | 999.1-03, 999.1-04 | Endpoint/source selector (addon + end-to-end wiring) | ✓ SATISFIED (code-level); runtime audibility PRESENT_BEHAVIOR_UNVERIFIED | Truths #6-#8, #10-#11 |
| WIN-AUDIOCFG-01 | 999.1-02, 999.1-04 | Widened `AudioConfig` + fail-closed verdict dispatch | ✓ SATISFIED | Truths #4, #5, #12 |

No orphaned requirements — these three phase-local tags are the only ones mapped to this phase (per ROADMAP.md line 60, no formal REQUIREMENTS.md entries expected).

### Anti-Patterns Found

None blocking. `grep -n -E "TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER"` across all 8 touched files (both Rust mirrors, both Cargo.toml, and the 4 TS/preload files) returns zero matches. No stub returns, no hardcoded empty data flowing to render, no console.log-only implementations found in the diff.

### Static Checks Run By This Verifier

- `diff -q native/wasapi-loopback/src/lib.rs wasapi-loopback-repo/src/lib.rs` → exit 0
- `diff -q native/wasapi-loopback/Cargo.toml wasapi-loopback-repo/Cargo.toml` → exit 0
- `bun run check` (tsgo, noEmit) → exit 0
- `bun run lint` (oxlint) → exit 0, 6 warnings / 0 errors (all pre-documented in 999.1-02/04-SUMMARY.md: `await-thenable` on synchronous napi calls mirroring the pre-existing #211 pattern, and `no-redundant-type-constituents` on the intentional `"default" | string` sentinel type)
- Structural greps for every acceptance criterion listed across all 4 PLAN.md files (INCLUDE/EXCLUDE constants, enumerator drops, fixed-format routing, fail-closed branch structure, dead-lineage absence)

No `cargo build`/`check`/`clippy`, no `bun run build`, no `gh workflow run`, and no push to any remote were performed (CI-only-build hard rule honored).

### Behavioral Spot-Checks

SKIPPED (no runnable entry points for Windows WASAPI audio capture on this non-Windows dev machine — this is inherent to the phase, not a gap). All runtime behavior is deferred to the BOX human-verification items below, per the phase's own `human_verify_mode: end-of-phase` design.

### Probe Execution

No probes declared or found (`find scripts -path '*/tests/probe-*.sh'` empty; no probe references in any PLAN/SUMMARY for this phase). N/A.

### Known Issues (Advisory, Non-Blocking)

Both carried forward from `999.1-REVIEW.md` (0 Critical / 2 Warning), independently re-confirmed present in the current code by this verifier:

- **WR-01** — App INCLUDE mode has no explicit guard against an empty `pids[]` (user selects app mode, checks nothing). Fails closed safely (a napi throw is caught → `failed-no-fallback`), but produces a silent, unexplained audio-less share instead of user feedback. Not fixed; recorded as an advisory per the review, not a phase blocker.
- **WR-02** — `stop()`'s 1500ms timeout-detach path (pre-existing #211 code, widened in surface by the new modes) can theoretically leave two WASAPI sessions briefly concurrent across a very fast re-click. Not fixed; recorded as an advisory per the review, not a phase blocker.

Neither issue undermines the two HARD safety invariants (fail-closed dispatch, fixed transport format) that this verification independently re-confirmed hold in the code (Truths #4 and #6).

### Human Verification Required

The following items are the phase's own deferred `checkpoint:human-verify` gates (all four plans' terminal tasks) — they were intentionally NOT performed during execution per the CI-only-build + no-push-until-instructed project rules, and cannot be performed by this verifier either:

1. **CI: wasapi-loopback Windows compile (INCLUDE + endpoint)**
   **Test:** Push the branch, trigger `thomas-quant/wasapi-loopback` Windows workflow (`--repo thomas-quant/wasapi-loopback`), confirm green, obtain the prebuilt `.node`.
   **Expected:** Compile succeeds; `.node` exports `startIncludeProcessTree`, `listAudioApps`, `listRenderEndpoints`, `startRenderEndpoint`, `startDefaultRenderEndpoint`.
   **Why human:** CI-only-build hard rule; verifier must not trigger CI or push to any remote.

2. **CI: GoofCord testBuild.yml produces win-artifacts**
   **Test:** `gh workflow run testBuild.yml --repo thomas-quant/GoofCord --ref <branch>`, consuming the addon above.
   **Expected:** Green Windows build producing the `win-artifacts` portable zip with the new addon baked in.
   **Why human:** Same CI-only-build rule.

3. **BOX: App INCLUDE end-to-end**
   **Test:** Start a screenshare in app-audio mode, check one 3rd-party app, confirm on a SECOND device that only that app's audio is heard (self-free); confirm system-mode default is unchanged from #211; force an activation failure and confirm silence (never a broad fallback); read `wasapi-capture.log` for capture-kind + verdict lines; confirm no CoreMessaging crash.
   **Expected:** All five conditions hold as described.
   **Why human:** No DevTools, no automated repro — requires a physical Windows box + a second listening device.

4. **BOX: Endpoint selector end-to-end**
   **Test:** Open the picker in system mode, confirm the capture-source dropdown lists "Default" + active render endpoints; pick a specific endpoint with audio playing and confirm it's heard on a second device; pick "Default" and confirm console-default + zero-config preservation; check whether `SteelSeries Sonar - Stream` is a render endpoint (in the dropdown) or a capture device (not listed) and record the finding; force an endpoint activation failure and confirm silence; confirm the log records `endpoint-default`/`endpoint:<id>` kinds; confirm no CoreMessaging crash.
   **Expected:** All conditions hold; the Sonar open question is answered and documented (not silently assumed).
   **Why human:** Same — physical Windows box + second device required; this is also the phase's one explicitly open research question.

### Gaps Summary

No code-level gaps found. All static/structural must-haves for WIN-APP-01, WIN-ENDPOINT-01, and WIN-AUDIOCFG-01 are verified directly against the current codebase (not merely cited from SUMMARY.md claims): both Rust mirrors are byte-identical and export the required INCLUDE/endpoint/enumerator symbols routed through the shared fixed-format helper; the TS wrapper's three-way dispatch and fail-closed verdict mapping were read in full and match the documented invariant (only `system`+`process-exclude` ever tolerates the Chromium fallback); the widened `AudioConfig` type-checks cleanly end-to-end; no dead AEC/self-cancel/B1 lineage exists anywhere in the touched files; `bun run check`/`lint` are green with only the pre-documented benign warnings; no debt markers (TBD/FIXME/XXX) are present.

What remains is exactly what this phase's design (`human_verify_mode: end-of-phase`) always intended to defer to a human: the addon has never been compiled (CI-only-build rule) and no runtime audio has ever been heard. Two truths (app-INCLUDE audibility, endpoint-selector audibility) are therefore PRESENT_BEHAVIOR_UNVERIFIED rather than VERIFIED or FAILED — the code is present and correctly wired, but no test or CI run has yet exercised it. Two known, non-blocking review advisories (WR-01, WR-02) remain unfixed as recorded in 999.1-REVIEW.md. The phase is correctly represented by `status: human_needed`, not `passed` (runtime unverified) and not `gaps_found` (no code-level defect blocks the goal).

**Next action:** Run the four human-verification items above (2 CI gates, 2 box tests) in order, then re-run `/gsd-verify-work 999.1` to close out.

---

*Verified: 2026-07-04T05:55:33Z*
*Verifier: Claude (gsd-verifier)*
