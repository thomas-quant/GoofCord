---
status: testing
phase: 999.1-windows-audio-patchcord-parity-backend
source: [999.1-VERIFICATION.md]
started: 2026-07-04T05:58:05Z
updated: 2026-07-04T06:46:44Z
---

## Current Test

number: 3
name: BOX — app INCLUDE end-to-end (per-app / patchcord-parity headline)
expected: |
  Install the win-artifacts build (GoofCord run 28698026482). Start a screenshare: the picker shows an
  audio-mode control + app checklist. Check ONLY a 3rd-party app playing audio, share → a SECOND device
  hears exactly that app, NOT your Discord voice (self-free); stop/restart once is idempotent. System
  (default) mode unchanged from #211. Forced app-mode failure = SILENCE (never Chromium "loopback"). The
  userData wasapi-capture.log records capture kind (include-pid vs exclude-self) + verdict. No CoreMessaging crash.
awaiting: user response

(Tests 1 & 2 — the CI gates — PASSED via CI on 2026-07-04; see results below. Only the two BOX tests remain.)

## Tests

### 1. CI — wasapi-loopback Windows compile (INCLUDE + enumerator + endpoint)
expected: The `thomas-quant/wasapi-loopback` Windows workflow compiles GREEN for the Plan 01 (INCLUDE + `listAudioApps`) and Plan 03 (endpoint enum + loopback) bindings/feature gates; a prebuilt `.node` exporting `startIncludeProcessTree` / `listAudioApps` / `listRenderEndpoints` / `startRenderEndpoint` / `startDefaultRenderEndpoint` is obtained.
how: Push branch `spike/999.1-per-app-include` to the fork, then `gh workflow run <win> --repo thomas-quant/wasapi-loopback --ref <branch>`. If a feature gate/import is missing, fix in BOTH byte-identical mirrors (`native/wasapi-loopback/src/lib.rs` + `wasapi-loopback-repo/src/lib.rs`), keep `diff -q` clean, re-run.
result: PASSED (2026-07-04) — addon CI run 28697943156 GREEN on branch `feat/999.1-per-app-include-endpoint`; prebuilt `wasapi-loopback-win32-x64.node` (393 KB) committed at `7f40452`; all 5 new exports present in the compiled source.

### 2. CI — GoofCord testBuild.yml win-artifacts
expected: GoofCord `testBuild.yml` (`--repo thomas-quant/GoofCord`) produces the `win-artifacts` portable zip, consuming the wasapi-loopback `.node` from test 1 (INCLUDE + endpoint symbols present).
how: `gh workflow run testBuild.yml --repo thomas-quant/GoofCord --ref <branch>` (the `--repo` flag is REQUIRED — gh defaults to upstream). Grep app.asar for the new markers before testing (renderer-bundle caveat).
result: PASSED (2026-07-04) — GoofCord testBuild run 28698026482 GREEN; "Assert wasapi addon packaged" ✓ (new `.node` present in ts-out AND packaged dist — not a silent fallback); `win-artifacts` 121 MB (id 8079247651). GoofCord pinned to addon `7f40452` (`4a9c161`).

### 3. BOX — app INCLUDE end-to-end (per-app / patchcord-parity headline)
expected: On the Windows box, the screenshare picker shows an audio-mode control + app checklist. Checking ONLY a 3rd-party app playing audio and sharing → a SECOND device hears exactly that app and NOT your Discord call/voice (self-free); stop/restart once is idempotent. System (default) mode is byte-behavior-identical to the shipped #211 EXCLUDE-self fix. Forcing app-mode activation failure yields SILENCE (never a Chromium `"loopback"` broad fallback). The userData `wasapi-capture.log` records each share's capture kind (`include-pid` vs `exclude-self`) + verdict. No CoreMessaging crash on any system-audio share.
result: [pending]

### 4. BOX — endpoint/source selector end-to-end (VAC/Sonar fix) + Sonar open question
expected: In system mode the picker shows a capture-source dropdown ("Default" + active render endpoints). Picking a specific render endpoint with audio playing → the SECOND device hears exactly that endpoint's mix. "Default" captures the console default AND preserves the zero-config process-exclude behavior for untouched users. Forcing endpoint-activation failure yields SILENCE (never broad fallback). The `wasapi-capture.log` records `endpoint-default` / `endpoint:<id>` kinds; no CoreMessaging crash. OPEN QUESTION to answer + record: does `SteelSeries Sonar - Stream` appear in the dropdown as a render endpoint (loopback-capturable) or is it a capture/input device (needs different handling)? Do NOT declare the feature done-for-Sonar if it is a capture device.
result: [pending]

## Summary

total: 4
passed: 2
issues: 0
pending: 2
skipped: 0
blocked: 0

_CI gates (1, 2) verified via GitHub Actions 2026-07-04. BOX gates (3, 4) require the physical second-device Windows test._

## Gaps
