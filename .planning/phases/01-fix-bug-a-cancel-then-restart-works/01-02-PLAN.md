---
phase: 01-fix-bug-a-cancel-then-restart-works
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/windows/screenshare/screenshare.ts
  - src/windows/main/renderer/postVencord/screensharePatch.ts
autonomous: false
requirements: [STREAM-01, STREAM-02, STREAM-03, STREAM-04, UPST-01]
must_haves:
  truths:
    - "On the Windows artifact, after cancelling the source picker, a second start-stream click re-opens the picker (a picker window appears)"
    - "A cancelled-then-restarted screenshare starts and streams normally with no application restart"
    - "Cancelling the source picker shows no uncaught or visible JavaScript error in Discord (710cfde preserved)"
    - "Repeated cancel then retry cycles remain stable — no progressive wedging, no main-process 'Object has been destroyed' errors across many cycles"
    - "Teardown is consolidated into an exactly-once finishRequest(wcId, result) called from select, cancel, and the closed handler; the map-delete is the idempotency token and delete precedes the callback"
    - "The renderer rejection name is set (kept or switched) based on diagnostic evidence, and whatever name is chosen is still swallowed by Discord as a cancellation"
    - "The upstream PR contains ONLY the finishRequest + error-name fix; the instrumentation commit is stripped (D-04)"
  artifacts:
    - path: "src/windows/screenshare/screenshare.ts"
      provides: "Exactly-once finishRequest(wcId, result) consolidating select/cancel/closed teardown"
      contains: "function finishRequest"
    - path: "src/windows/main/renderer/postVencord/screensharePatch.ts"
      provides: "Evidence-gated rejection name (NotAllowedError kept, or AbortError if H1 proven and NotAllowedError fails to clear the latch)"
      contains: "DOMException"
  key_links:
    - from: "selectScreenshareSource select/cancel paths"
      to: "finishRequest"
      via: "single-owner teardown call"
      pattern: "finishRequest\\("
    - from: "capturerWindow closed handler"
      to: "finishRequest"
      via: "single-owner teardown call (idempotent via map-delete guard)"
      pattern: "finishRequest\\("
---

<objective>
Apply the surgical fix for Bug A and verify it on a single combined Windows CI artifact (D-01). Two code changes, landed as distinct commit(s) separate from 01-01's instrumentation:

1. **`screenshare.ts` — exactly-once teardown.** Consolidate the two cleanup paths (the `selectScreenshareSource` select/cancel handler and the window `closed` handler) into one `finishRequest(wcId, result)` that uses the map-delete as the idempotency token. Call it from select, cancel, AND `closed`. This makes the Electron `callback` exactly-once by construction and kills the "Object has been destroyed" / double-fire / zero-callback race (Pitfall 3).

2. **`screensharePatch.ts` — evidence-gated rejection name.** The `NotAllowedError` → `AbortError` switch is DATA-DRIVEN: only switch if the diagnostic trace from the combined build proves H1 (Discord latches go-live state and never re-issues `getDisplayMedia`) AND `NotAllowedError` does not clear the latch. Whatever name is chosen MUST still be swallowed by Discord as a cancellation (preserve 710cfde / STREAM-03).

Per D-01, BOTH the speculative fix above and 01-01's instrumentation ride ONE Windows CI build. This plan triggers that single build, runs the manual verification on it (D-03), and — only if the trace proves H1 and the error-name switch alone does not clear the latch — pursues the conditional Discord go-live Flux abort sub-step. Finally, per D-04, it strips the 01-01 instrumentation commit so the upstream PR contains only the fix.

Purpose: Make the second start-stream click after a cancel reliably re-open the picker and stream normally, stably across repeated cycles, with a surgical PR-ready diff.
Output: A fixed `screenshare.ts` + `screensharePatch.ts`, a verified Windows artifact, and a clean fix-only commit set with instrumentation stripped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fix-bug-a-cancel-then-restart-works/01-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-fix-bug-a-cancel-then-restart-works/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate teardown into an exactly-once finishRequest() in screenshare.ts</name>
  <files>src/windows/screenshare/screenshare.ts</files>
  <read_first>
    - src/windows/screenshare/screenshare.ts (the file being modified — the `activeRequests` map at line 17; the `selectScreenshareSource` handler lines 54-87 with the `!req` return (56), delete-before-callback (58), cancel `callback({})` (62) and grant `callback(result)` (85); the `closed` handler lines 118-123 with its `activeRequests.has(wcId)` guard)
    - .planning/research/PITFALLS.md (Pitfall 3 — read the exact finishRequest sketch: `const req = activeRequests.get(wcId); if (!req) return; activeRequests.delete(wcId); try { req.callback(result); } catch {} if (!req.window.isDestroyed()) req.window.close();` — and "How to avoid": single-owner cleanup, delete-before-callback is the idempotency token, snapshot callback/window before any await)
    - .planning/phases/01-fix-bug-a-cancel-then-restart-works/01-01-SUMMARY.md (the `[ScreenshareDebug]` log tags 01-01 added at these exact sites — when you route the callbacks through finishRequest, preserve or relocate the C log lines so the trace still counts callbacks-per-request through the combined build)
  </read_first>
  <action>
    Add `function finishRequest(wcId, result)` to `screenshare.ts` implementing the Pitfall 3 sketch with EXACTLY this ordering: (1) `const req = activeRequests.get(wcId)`; (2) guard `if (!req) return;` — this is what makes the callback exactly-once; (3) `activeRequests.delete(wcId)` — the map-delete BEFORE the callback is the idempotency token, preserve delete-before-callback ordering; (4) `try { req.callback(result); } catch {}` — swallow a throw so teardown completes; (5) `if (!req.window.isDestroyed()) req.window.close();`.

    Rewire all three teardown sites to call `finishRequest`:
    - `selectScreenshareSource` cancel branch (the `if (!id)` block, current lines 61-65): replace `callback({}); if (!window.isDestroyed()) window.close();` with `finishRequest(event.sender.id, {})`. Because finishRequest now does the get/delete/close, simplify the top of `selectScreenshareSource` so it does not pre-delete or double-close — keep the early `const req = activeRequests.get(event.sender.id); if (!req) return;` for reading `frame`/audio config, but let `finishRequest` own the delete + callback + close. Do not delete the entry twice and do not call the callback twice.
    - `selectScreenshareSource` grant branch (current line 85-86): replace `callback(result); if (!window.isDestroyed()) window.close();` with `finishRequest(event.sender.id, result)` AFTER `result` is fully assembled (including the `result.audio = "loopback"` / patchcord branch).
    - The `closed` handler (current lines 118-123): replace the body with `finishRequest(wcId, {})`. The `!req` guard inside finishRequest replaces the `activeRequests.has(wcId)` check — if select/cancel already ran, `finishRequest` is a no-op (idempotent), which is the whole point.

    Snapshot `callback`/`window`/`frame` into locals before any `await` (the `patchcordStart*` await and the `frame.executeJavaScript`) so a concurrent `closed` event cannot observe a half-mutated entry — but note that with finishRequest owning the delete, the first finisher wins and the second is a no-op. Keep the existing `frame.executeJavaScript(...).catch(() => {})` destroyed-frame guard. Do NOT re-register the handler, do NOT call `setDisplayMediaRequestHandler(null)`, do NOT add `useSystemPicker`, do NOT add dependencies. This is the fix commit — keep it separate from 01-01's instrumentation commit (D-01). Preserve the `[ScreenshareDebug][C]` log lines through this build (move them so they still fire at each callback path inside/around finishRequest) so the combined artifact's trace remains valid; they are stripped later in Task 5.
  </action>
  <verify>
    <automated>grep -n "function finishRequest" src/windows/screenshare/screenshare.ts && grep -B2 -A6 "function finishRequest" src/windows/screenshare/screenshare.ts | grep -n "activeRequests.delete" && bun run check</automated>
  </verify>
  <acceptance_criteria>
    - `screenshare.ts` contains `function finishRequest(wcId, result)`
    - Inside `finishRequest`, `activeRequests.delete(wcId)` appears BEFORE `req.callback(result)` (delete-before-callback ordering preserved)
    - `finishRequest` contains the `if (!req) return;` exactly-once guard and a `try { ... } catch {}` around the callback and an `isDestroyed()` guard before `window.close()`
    - All three teardown sites (cancel branch, grant branch, `closed` handler) call `finishRequest`; no remaining bare `callback({})` / `callback(result)` outside `finishRequest`
    - The handler is still registered exactly once; no `setDisplayMediaRequestHandler(null)`, no `useSystemPicker`, no new imports/dependencies
    - `bun run check` exits 0 and `bun run build` succeeds
  </acceptance_criteria>
  <done>Teardown is single-owner and exactly-once across select/cancel/closed; the map-delete is the idempotency token; no double-callback, no zero-callback, no destroyed-object path remains. Committed as a distinct fix commit.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm the renderer rejection name is set deliberately (default-keep NotAllowedError) in screensharePatch.ts</name>
  <files>src/windows/main/renderer/postVencord/screensharePatch.ts</files>
  <read_first>
    - src/windows/main/renderer/postVencord/screensharePatch.ts (the file being modified — the synthetic `throw new DOMException("Permission denied by system", "NotAllowedError")` at line 30, inside the catch of the patched getDisplayMedia)
    - .planning/research/PITFALLS.md (Pitfall 1: the `NotAllowedError` vs `AbortError` lever; "verify Discord clears its starting-stream state on a thrown NotAllowedError; if it only clears on AbortError, switch the name and re-test" — and the constraint that whatever name is chosen must still be swallowed as a cancellation)
    - .planning/phases/01-fix-bug-a-cancel-then-restart-works/01-CONTEXT.md (Claude's Discretion: the rejection-name decision is DATA-DRIVEN from the diagnostic build; only switch if H1 is proven AND NotAllowedError does not clear the latch — Task 4 makes the switch decision; this task only ensures the name is a deliberate, documented constant for the combined build)
  </read_first>
  <action>
    Do NOT change the error name yet — for the combined build (D-01), KEEP the existing `NotAllowedError` re-throw, because research's strongest prior is that `finishRequest` + the existing `NotAllowedError` clears the latch, and the diagnostic trace must measure that exact configuration first. Make the rejection name a clearly-marked, single-point-of-truth so Task 4 can flip it with a one-token change if evidence demands: ensure the `DOMException(... , "NotAllowedError")` is on one line and add a short inline comment marking it as the evidence-gated cancellation-name lever (referencing that switching to `"AbortError"` is the conditional fix per PITFALLS Pitfall 1). Confirm the re-throw is still inside the `catch` so cancellation stays error-free in Discord (STREAM-03 / preserve 710cfde). This task makes NO behavioral change — it sets up the conditional in Task 4. Keep this in the fix commit set, separate from 01-01 instrumentation.
  </action>
  <verify>
    <automated>grep -n "NotAllowedError" src/windows/main/renderer/postVencord/screensharePatch.ts && bun run check</automated>
  </verify>
  <acceptance_criteria>
    - `screensharePatch.ts` still throws `new DOMException("Permission denied by system", "NotAllowedError")` inside the `catch` of the patched getDisplayMedia (unchanged behavior for the combined build)
    - An inline comment marks the error name as the evidence-gated cancellation lever (the AbortError switch is the conditional per Task 4)
    - The re-throw remains in the catch so cancellation is still swallowed by Discord (STREAM-03 preserved)
    - `bun run check` exits 0
  </acceptance_criteria>
  <done>The rejection name is a deliberate, documented, single-point lever set to NotAllowedError for the first combined-build measurement; Task 4 owns any evidence-driven switch.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Trigger the single combined Windows CI build and run the cancel→restart acceptance test (MANUAL)</name>
  <files>.github/workflows/testBuild.yml (verification vehicle — no source files modified by this manual task)</files>
  <action>Trigger ONE combined Windows x64 CI build via `gh workflow run .github/workflows/testBuild.yml` (or have the developer dispatch it from the Actions tab), wait for the `win-artifacts` zip, then hand the build to the developer to run the cancel→restart acceptance test on their own Windows box per the steps in how-to-verify (D-03). This is the single combined build of D-01 — do NOT trigger a second build here. See how-to-verify for the exact pass/fail signals.</action>
  <verify><human-check>Developer confirms on the Windows artifact: second start-stream click after cancel re-opens the picker; stream restarts normally; cancel is error-free; 10+ repeated cycles stable with no destroyed-object errors. See how-to-verify for trace details.</human-check></verify>
  <done>The combined Windows build was produced and the developer recorded the cancel→restart acceptance outcome and the H1 routing (did A and B re-fire on the second click?) needed to decide Task 4.</done>
  <read_first>
    - .github/workflows/testBuild.yml (the Windows x64 CI build, `workflow_dispatch` only — `gh workflow run` triggers it; artifact `win-artifacts` contains the packaged zip)
    - .planning/phases/01-fix-bug-a-cancel-then-restart-works/01-CONTEXT.md (D-01: ONE combined build carrying instrumentation + fix; D-03: developer runs the downloaded artifact on their own Windows box with DevTools F12; STREAM-04: developer runs repeated cancel→retry cycles)
    - .planning/research/PITFALLS.md ("Looks Done But Isn't" checklist — single-callback, no destroyed-object errors, second click re-opens the picker)
  </read_first>
  <what-built>
    A single Windows x64 CI artifact carrying BOTH 01-01's diagnostic log points (A/B/C → DevTools + `screenshare-debug.log`) AND this plan's fix (`finishRequest` consolidation + the kept `NotAllowedError` name). This is the one and only combined build per D-01. The executor triggers the build via `gh workflow run .github/workflows/testBuild.yml` (or instructs the developer to dispatch it from the Actions tab) and waits for the `win-artifacts` zip. The actual Windows testing is performed by the developer on their own box (D-03) — it cannot be automated.
  </what-built>
  <how-to-verify>
    On a Windows machine, download and run the `win-artifacts` build. Open Discord, open DevTools (F12), and locate `screenshare-debug.log` under the app's userData folder (`%APPDATA%\goofcord\screenshare-debug.log`, or the portable `goofcord-data` folder if running portable).

    1. Start a screenshare → the source picker window appears. CANCEL it (close the picker / back out).
       - PASS signal: cancelling shows NO uncaught/visible JavaScript error in Discord (STREAM-03). DevTools shows log point A fired once; `screenshare-debug.log` shows log point B fired once and exactly ONE `[C]` callback line for that request (no double-fire, no zero-fire).
    2. Click "Go Live" / start-stream a SECOND time.
       - PASS signal (STREAM-01): the source picker window RE-OPENS. In the trace, log point A fires a SECOND time (counter incremented) AND log point B fires a SECOND time (handler invoked) — this confirms H1 is cleared by the fix. If A fires but B does not, the handler isn't firing (investigate H2). If A does NOT fire on the second click, Discord still latched go-live state → record this; Task 4's conditional applies.
    3. Select a source on the second attempt → the stream starts and plays normally with NO application restart (STREAM-02).
    4. Repeat cancel→retry many times (10+ cycles) (STREAM-04).
       - PASS signal: the start-stream control keeps working every cycle; `screenshare-debug.log` shows NO `TypeError: Object has been destroyed`; `activeRequests` does not leak (no growing set of orphaned picker windows); callback count stays exactly one per request.

    Record in the resume notes: whether the second-click picker appeared (STREAM-01 pass/fail), whether A and B both fired on the second click (H1 routing), whether any double/zero callback or destroyed-object error appeared, and whether repeated cycles stayed stable.
  </how-to-verify>
  <acceptance_criteria>
    - The combined `win-artifacts` build was produced and run on Windows
    - After cancel, a second start-stream click re-opens the picker (STREAM-01) OR the trace records that A did not re-fire (routing Task 4's conditional)
    - Cancelling produces no uncaught/visible JS error in Discord (STREAM-03)
    - `screenshare-debug.log` shows exactly one callback per request across the cancel and the restart (no double/zero fire), and no "Object has been destroyed" across 10+ repeated cycles (STREAM-04)
    - The developer has recorded the H1 routing outcome (did A and B re-fire on the second click?) needed to decide Task 4
  </acceptance_criteria>
  <resume-signal>Type "approved" if the second-click picker re-opens, the stream restarts normally, cancellation is error-free, and repeated cycles are stable. If the second click still produces NO picker (A does not re-fire), report "H1 persists — error-name switch needed" so Task 4 runs. Otherwise describe the observed trace.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: CONDITIONAL — switch the rejection name to AbortError, and (only if still latched) dispatch a Discord go-live abort</name>
  <files>src/windows/main/renderer/postVencord/screensharePatch.ts</files>
  <read_first>
    - src/windows/main/renderer/postVencord/screensharePatch.ts (the file being modified — the `DOMException(..., "NotAllowedError")` re-throw at line 30; the existing `Common.FluxDispatcher` / `Common.UserStore` usage at lines 89-103 shows the Flux access pattern available in this renderer context if a direct abort dispatch is needed)
    - .planning/research/PITFALLS.md (Pitfall 1 "How to avoid": switch the synthetic exception name to AbortError if Discord only clears on AbortError; and the fallback — dispatch the Flux action Discord uses to abort go-live / reset the ApplicationStreamingStore pending flag)
    - .planning/research/SUMMARY.md (the Phase 2 research flag: if H1 confirmed AND the name switch alone does not clear the latch, identify and dispatch the correct ApplicationStreamingStore abort action)
    - .planning/phases/01-fix-bug-a-cancel-then-restart-works/01-CONTEXT.md (Deferred Ideas: the Flux store flag exploration is a CONDITIONAL sub-step — pursue ONLY IF H1 confirmed AND the NotAllowedError→AbortError switch alone does not clear the latch)
  </read_first>
  <action>
    GATE: Run this task ONLY IF Task 3 reported that the second start-stream click still produced NO picker (H1 persists — `getDisplayMedia` not re-issued) despite `finishRequest` + `NotAllowedError`. If Task 3 was "approved" (picker re-opened), SKIP this task entirely and proceed to Task 5; record in the summary that the error-name switch was not needed.

    Step 4a (first escalation): Switch the synthetic exception name from `"NotAllowedError"` to `"AbortError"` in the `DOMException("Permission denied by system", "AbortError")` re-throw in `screensharePatch.ts` — the single-point lever Task 2 set up. This is the candidate that makes Discord treat the cancel as a user-abort and re-arm the go-live button. Keep it inside the `catch` so cancellation stays swallowed (STREAM-03 / 710cfde preserved). Then re-trigger ONE combined Windows build and re-run the Task 3 acceptance test. If the second-click picker now re-opens, STOP here — 4b is not needed.

    Step 4b (second escalation, only if 4a still latched): Identify the correct Discord `ApplicationStreamingStore` go-live abort/reset action and dispatch it from the renderer on cancel using the `Common.FluxDispatcher` pattern already present in this file (lines 89-103). The goal is to clear Discord's latched "stream pending/starting" flag so the next click re-enters the `getDisplayMedia` path. Keep the dispatch minimal and gated to the cancel path; do NOT add dependencies and keep it upstream-friendly. Re-build and re-verify via Task 3's protocol.

    Whatever name/dispatch is chosen MUST still be swallowed by Discord as a cancellation (STREAM-03). Document the final decision and the evidence that drove it. This stays in the fix commit set, separate from instrumentation.
  </action>
  <verify>
    <automated>grep -nE "AbortError|NotAllowedError" src/windows/main/renderer/postVencord/screensharePatch.ts && bun run check</automated>
  </verify>
  <acceptance_criteria>
    - If skipped: the summary records that Task 3 passed with `NotAllowedError` and no switch was needed
    - If run (4a): the re-throw is `new DOMException("Permission denied by system", "AbortError")`, still inside the catch, and a follow-up combined build confirmed the second-click picker re-opens (STREAM-01/02)
    - If run (4b): a minimal, gated Discord go-live abort dispatch was added via `Common.FluxDispatcher` on the cancel path, with the specific store/action documented, and verified to re-arm the button
    - Cancellation remains error-free in Discord in all cases (STREAM-03)
    - No new dependencies; diff stays surgical (UPST-01)
  </acceptance_criteria>
  <done>The latched-go-live failure mode is cleared by the minimum necessary lever (kept name → AbortError → Flux abort, escalating only as evidence requires), with cancellation still swallowed and the diff surgical.</done>
</task>

<task type="auto">
  <name>Task 5: Strip the 01-01 instrumentation before the upstream PR (D-04)</name>
  <files>src/windows/screenshare/screenshare.ts, src/windows/main/renderer/postVencord/screensharePatch.ts</files>
  <read_first>
    - .planning/phases/01-fix-bug-a-cancel-then-restart-works/01-01-SUMMARY.md (the exact `[ScreenshareDebug]` tag format, the `appendDebugLog` helper, the log file path, and the instrumentation commit hash/message recorded there)
    - src/windows/screenshare/screenshare.ts and src/windows/main/renderer/postVencord/screensharePatch.ts (remove every `[ScreenshareDebug]` log line, the `appendDebugLog` helper, the request counters, and the unused `fs`/`userDataPath` imports they introduced — leaving ONLY the finishRequest fix and the final error-name)
    - .planning/phases/01-fix-bug-a-cancel-then-restart-works/01-CONTEXT.md (D-04: strip instrumentation so the upstream PR contains ONLY the finishRequest + error-name fix; the userData log-file writes must NOT ship; D-01: instrumentation is its own commit so it is trivially reverted)
  </read_first>
  <action>
    Now that the combined build has served its diagnostic purpose, remove ALL instrumentation so the PR is maximally surgical (UPST-01). Either `git revert` the 01-01 instrumentation commit (clean if no fix code touched the same lines) OR manually delete every `[ScreenshareDebug]` log line, the `appendDebugLog` helper, the per-request debug counters, and any now-unused `fs` / `userDataPath` imports that the instrumentation added — across BOTH `screenshare.ts` and `screensharePatch.ts`. Verify NO `screenshare-debug.log` write path remains and NO `[ScreenshareDebug]` tag remains. The finishRequest consolidation and the final chosen error name MUST remain. Commit the strip as its own commit (or fold into a clean fix-only branch state) so the resulting history for the PR is: finishRequest fix + error-name decision, with zero debug scaffolding. Confirm `bun run check` and `bun run build` still pass after the strip.
  </action>
  <verify>
    <automated>! grep -rn "ScreenshareDebug\|appendDebugLog\|screenshare-debug.log" src/windows/screenshare/screenshare.ts src/windows/main/renderer/postVencord/screensharePatch.ts && grep -n "function finishRequest" src/windows/screenshare/screenshare.ts && bun run check</automated>
  </verify>
  <acceptance_criteria>
    - No `[ScreenshareDebug]` tag, no `appendDebugLog` helper, no `screenshare-debug.log` write path, and no instrumentation-only imports remain in either file
    - `function finishRequest` and the final chosen rejection name (NotAllowedError or AbortError) DO remain
    - The diff that would form the upstream PR contains ONLY the finishRequest consolidation + error-name fix (no debug scaffolding) (UPST-01)
    - `bun run check` exits 0 and `bun run build` succeeds
  </acceptance_criteria>
  <done>Instrumentation is fully stripped; the remaining diff is the surgical, PR-ready finishRequest + error-name fix only (D-04 / UPST-01).</done>
</task>

</tasks>

<verification>
- `finishRequest(wcId, result)` exists with delete-before-callback ordering and the `!req` exactly-once guard; called from select, cancel, and `closed`.
- The Windows combined artifact (D-01) demonstrated: second-click picker re-opens (STREAM-01), restart streams normally with no app restart (STREAM-02), cancel is error-free (STREAM-03), and 10+ repeated cycles stay stable with no destroyed-object errors (STREAM-04).
- The error-name decision is evidence-gated (kept NotAllowedError unless H1 proven + latch persists, then AbortError, then Flux abort).
- Instrumentation is stripped (D-04); the PR-ready diff is finishRequest + error-name only, no new dependencies, no handler re-registration, no useSystemPicker (UPST-01).
</verification>

<success_criteria>
- After cancelling the source picker, clicking start-stream again re-opens the picker on the Windows build. [STREAM-01]
- A cancelled-then-restarted screenshare starts and streams normally with no app restart. [STREAM-02]
- Cancelling shows no uncaught/visible JS error in Discord (710cfde preserved). [STREAM-03]
- Repeated cancel→retry cycles remain stable — no progressive wedging, no "Object has been destroyed". [STREAM-04]
- The diff is surgical: finishRequest in screenshare.ts + renderer error-shape adjustment, instrumentation stripped, no re-registration, no useSystemPicker, no new deps — PR-ready for upstream GoofCord. [UPST-01]
</success_criteria>

<output>
Create `.planning/phases/01-fix-bug-a-cancel-then-restart-works/01-02-SUMMARY.md` when done. Record: the final finishRequest signature/ordering, the final rejection-name decision and the evidence that drove it (and whether Task 4 / the Flux sub-step was needed), the combined-build verification results for STREAM-01..04, and confirmation that the instrumentation was stripped (D-04) leaving a PR-ready diff.
</output>
