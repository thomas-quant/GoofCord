---
phase: 01-fix-bug-a-cancel-then-restart-works
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/windows/screenshare/screenshare.ts
  - src/windows/main/renderer/postVencord/screensharePatch.ts
autonomous: true
requirements: [STREAM-01, STREAM-04, UPST-01]
must_haves:
  truths:
    - "On the Windows artifact, performing cancel then a second start-stream click produces a deterministic trace in screenshare-debug.log that routes H1 (renderer never re-issues getDisplayMedia) vs H2 (handler not invoked) vs H3 (callback double/zero-fire)"
    - "Every callback() site, the !req early-return, and the closed handler in screenshare.ts emit a tagged log line that survives a packaged NSIS build (no attached console)"
    - "The patched getDisplayMedia entry in screensharePatch.ts emits a log line visible in DevTools on the developer's Windows box"
    - "Each request is tagged with a monotonic counter so the FIRST click and the SECOND click are unambiguous in one trace"
  artifacts:
    - path: "src/windows/screenshare/screenshare.ts"
      provides: "Log points B (handler entry) and C (every callback site + !req return + closed handler) writing to a userData log file via a local appendDebugLog helper"
      contains: "appendDebugLog"
    - path: "src/windows/main/renderer/postVencord/screensharePatch.ts"
      provides: "Log point A at the first line of the patched getDisplayMedia"
      contains: "getDisplayMedia"
  key_links:
    - from: "src/windows/screenshare/screenshare.ts"
      to: "app.getPath('userData')/screenshare-debug.log"
      via: "fs.appendFileSync in appendDebugLog helper"
      pattern: "appendFileSync"
    - from: "src/windows/main/renderer/postVencord/screensharePatch.ts"
      to: "DevTools console"
      via: "console.log at getDisplayMedia entry"
      pattern: "console\\.log"
---

<objective>
Instrument Bug A across both fix-surface files so a single Windows CI artifact yields a deterministic diagnostic trace. Add three log points: (A) the first line of the patched `getDisplayMedia` in `screensharePatch.ts`; (B) the first line inside the `setDisplayMediaRequestHandler` callback in `screenshare.ts`; (C) every `callback(...)` call site PLUS the `!req` early-return PLUS the `closed` handler in `screenshare.ts`. Main-process probes (B and C) append to a `screenshare-debug.log` file under `app.getPath('userData')` because a packaged NSIS GUI build has no attached console (D-02). The renderer probe (A) logs to DevTools.

This is the instrumentation commit. It builds and verifies against the SAME single combined CI artifact as plan 01-02 (D-01) — this plan does NOT trigger its own Windows build. Per D-04 this instrumentation is its own commit so it can be stripped before the upstream PR.

Purpose: Route H1 (Discord renderer never re-issues `getDisplayMedia` on the second click — latched go-live state) vs H2 (main handler never fires — Electron dispatch wedge) vs H3 (callback double-fire / zero-fire race) from one round-trip. Without this, plan 01-02's fix would be a blind guess against a scarce CI artifact.
Output: Two instrumented source files, committed as a distinct "instrumentation" commit, ready for the combined build in 01-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fix-bug-a-cancel-then-restart-works/01-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/research/SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add log point A — patched getDisplayMedia entry in screensharePatch.ts</name>
  <files>src/windows/main/renderer/postVencord/screensharePatch.ts</files>
  <read_first>
    - src/windows/main/renderer/postVencord/screensharePatch.ts (the file being modified — note the existing `console.log("Setting stream's content hint...")` at line 32 and the synthetic `NotAllowedError` re-throw at line 30; renderer already uses plain `console.log`, match that convention)
    - .planning/research/PITFALLS.md (Pitfall 1 warning signs: "Second start-stream click produces no picker window — handler not invoked"; this log point is how you distinguish H1 from H2/H3)
    - .planning/phases/01-fix-bug-a-cancel-then-restart-works/01-CONTEXT.md (D-02: renderer log point A reads from DevTools; the "Specific Ideas" note that the trace must tag the SECOND click vs the first via a counter)
  </read_first>
  <action>
    In `patchScreenshare()`, add a module-scoped monotonic request counter (e.g. `let getDisplayMediaCallCount = 0`). At the FIRST line inside the patched `navigator.mediaDevices.getDisplayMedia = async function (opts)` (currently line 22, before the `try`), increment the counter and emit log point A via `console.log` with a stable, greppable tag: tag string `[ScreenshareDebug][A]` plus `getDisplayMedia called`, the current counter value, and a timestamp (`Date.now()` or `new Date().toISOString()`). This is the lever that proves whether Discord re-issues `getDisplayMedia` on the second click (H1) at all. Do NOT change the existing `NotAllowedError` re-throw behavior — only add a log line. Do NOT alter the Patchcord block or the constraints logic. Keep the diff to the single added counter declaration plus the one log line so it reverts cleanly (D-04). This is renderer code — DevTools is the read path (D-02), so plain `console.log` is correct here; do not attempt to write to a userData file from the renderer.
  </action>
  <verify>
    <automated>grep -n "ScreenshareDebug\]\[A\]" src/windows/main/renderer/postVencord/screensharePatch.ts</automated>
  </verify>
  <acceptance_criteria>
    - `screensharePatch.ts` contains a `console.log` with the literal tag `[ScreenshareDebug][A]` as the first statement inside the patched `getDisplayMedia` function (before the `try`)
    - A module-scoped call counter is declared and incremented per `getDisplayMedia` invocation so the second click is distinguishable from the first
    - The existing `throw new DOMException("Permission denied by system", "NotAllowedError")` at line 30 is unchanged
    - The Patchcord `getVirtmic()` block and the constraints/contentHint logic are unchanged
    - `bun run check` exits 0
  </acceptance_criteria>
  <done>Log point A fires on every `getDisplayMedia` call with an incrementing counter, visible in DevTools, and no other renderer behavior changed.</done>
</task>

<task type="auto">
  <name>Task 2: Add log points B and C to screenshare.ts with a userData file-append helper</name>
  <files>src/windows/screenshare/screenshare.ts</files>
  <read_first>
    - src/windows/screenshare/screenshare.ts (the file being modified — the `setDisplayMediaRequestHandler` callback at lines 97-127; the `selectScreenshareSource` handler with the `!req` early-return at line 56, the cancel `callback({})` at line 62, and the grant `callback(result)` at line 85; the `closed` handler `callback({})` at lines 118-123)
    - src/utils.ts (lines 1-43 — note `import fs from "node:fs"`, `import { app } from "electron"`, and the exported `userDataPath = app.getPath("userData")`; you may import `userDataPath` from `../../utils.ts` or call `app.getPath("userData")` directly, matching the existing import style in screenshare.ts which already imports from `../../utils.ts`)
    - .planning/research/PITFALLS.md (Pitfall 3: the two cleanup paths — select/cancel handler and the `closed` event; Pitfall 1 warning sign: "console.log in the handler never prints on the second click"; the "Looks Done But Isn't" single-callback checklist item)
    - .planning/phases/01-fix-bug-a-cancel-then-restart-works/01-CONTEXT.md (D-02: main-process probes append to a userData log file because packaged NSIS builds have no console; the request-counter idea to disambiguate first vs second click)
  </read_first>
  <action>
    Add a local helper `appendDebugLog(line: string)` in `screenshare.ts` that appends a single line (prefixed with an ISO timestamp) to `path.join(userDataPath, "screenshare-debug.log")` using `fs.appendFileSync`, wrapped in try/catch so a write failure never throws into the handler. Import `fs` from `node:fs` and `userDataPath` from `../../utils.ts` (or call `app.getPath("userData")` — match existing import style). Add a module-scoped request counter so each `setDisplayMediaRequestHandler` invocation is tagged with an incrementing number, making the FIRST vs SECOND click unambiguous in one trace (D-02 / Specific Ideas).

    Log point B: as the FIRST line inside the `setDisplayMediaRequestHandler` callback (currently line 97, before `new BrowserWindow`), call `appendDebugLog` with tag `[ScreenshareDebug][B]`, the request counter, and the `wcId` (resolve wcId logging after `capturerWindow.webContents.id` is available — log B at entry with the counter, then a `[B] wcId=` line once wcId exists). This proves whether the main handler fires on the second click (distinguishes H1 from H2).

    Log point C — instrument EVERY callback site and the zero-callback risk paths:
    - The `!req` early-return at line 56 of `selectScreenshareSource`: log `[ScreenshareDebug][C] selectScreenshareSource: no active request (early return)` with the `event.sender.id` BEFORE the `return`.
    - The cancel `callback({})` at line 62: log `[ScreenshareDebug][C] cancel callback({}) for wcId=` immediately BEFORE the `callback({})`.
    - The grant `callback(result)` at line 85: log `[ScreenshareDebug][C] grant callback(result) for wcId=` (include whether `result.audio` was set) immediately BEFORE the `callback(result)`.
    - The `closed` handler at lines 118-123: log `[ScreenshareDebug][C] closed handler for wcId=` and whether `activeRequests.has(wcId)` was true (i.e. whether THIS path fired the callback) immediately inside the `closed` callback.

    The point of C is to count callback invocations per request: exactly-one is correct, two = double-fire (H3), zero = hang (H3 zero-callback). Do NOT yet consolidate into `finishRequest()` — that is plan 01-02's fix commit. This plan ONLY adds logging; the teardown structure (delete-before-callback, dual paths) stays exactly as-is so the diagnostic trace reflects the CURRENT behavior. Keep all added lines greppable by the `[ScreenshareDebug]` tag so plan 01-02 / D-04 can strip them in one revert.
  </action>
  <verify>
    <automated>grep -c "ScreenshareDebug" src/windows/screenshare/screenshare.ts | { read n; [ "$n" -ge 5 ] && echo "ok: $n probes"; }</automated>
  </verify>
  <acceptance_criteria>
    - `screenshare.ts` contains a local `appendDebugLog` helper that writes to `screenshare-debug.log` under `app.getPath("userData")` / `userDataPath` via `fs.appendFileSync`, wrapped in try/catch
    - Log point B (`[ScreenshareDebug][B]`) is the first statement inside the `setDisplayMediaRequestHandler` callback and includes a per-request incrementing counter
    - Log point C (`[ScreenshareDebug][C]`) appears at: the `!req` early-return in `selectScreenshareSource`, the cancel `callback({})` site, the grant `callback(result)` site, and the `closed` handler — at least 5 total `[ScreenshareDebug]` occurrences in the file
    - Each C log line includes the `wcId` (or `event.sender.id`) so callback invocations can be counted per request
    - The existing teardown structure is UNCHANGED (no `finishRequest()` yet, no reordering of delete/callback/close, dual cleanup paths intact)
    - `bun run check` exits 0 and `bun run build` succeeds
  </acceptance_criteria>
  <done>Log points B and C write tagged, counted lines to the userData log file at every callback site and zero-callback risk path; the trace can route H1/H2/H3 and count callbacks-per-request; no teardown behavior changed.</done>
</task>

<task type="auto">
  <name>Task 3: Commit instrumentation as a distinct, revertable commit</name>
  <files>src/windows/screenshare/screenshare.ts, src/windows/main/renderer/postVencord/screensharePatch.ts</files>
  <read_first>
    - .planning/phases/01-fix-bug-a-cancel-then-restart-works/01-CONTEXT.md (D-01: instrumentation and fix land as DISTINCT commits in ONE build; D-04: the instrumentation commit is stripped/reverted before the upstream PR — so it must be self-contained and revertable)
    - CLAUDE.md (project commit conventions; global git config author per user instructions — do NOT use Claude as author)
  </read_first>
  <action>
    Stage ONLY the two instrumented files (`src/windows/screenshare/screenshare.ts`, `src/windows/main/renderer/postVencord/screensharePatch.ts`) and create ONE commit dedicated solely to instrumentation. Use a message that makes the strip-before-PR step (D-04) trivial to locate, e.g. `debug(screenshare): add Bug A diagnostic log points A/B/C (revert before PR)`. Do NOT bundle any fix code into this commit — the `finishRequest()` consolidation and error-name decision are plan 01-02's separate commit(s). This separation is what makes D-04 a one-line revert. Use the author name/email from the global git config (per user instructions), not Claude.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -iE "debug.*screenshare|instrument|diagnostic"</automated>
  </verify>
  <acceptance_criteria>
    - A single git commit exists containing ONLY the two instrumented files
    - The commit message clearly marks it as diagnostic instrumentation to be reverted before the upstream PR
    - No fix code (`finishRequest`, error-name change) is included in this commit
    - Commit author is the global git config user, not Claude
  </acceptance_criteria>
  <done>Instrumentation is one self-contained, clearly-labeled, revertable commit — ready to ride the combined CI build alongside 01-02's fix commit (D-01) and to be stripped before PR (D-04).</done>
</task>

</tasks>

<verification>
- All three log points (A renderer/DevTools, B + C main/userData file) are present and greppable by the `[ScreenshareDebug]` tag.
- `bun run check` exits 0; `bun run build` succeeds (the build is what the Windows CI runs).
- No teardown/behavior changes in this plan — instrumentation only. The diagnostic trace reflects current (pre-fix) behavior so 01-02 can route the hypothesis.
- The instrumentation is a single distinct commit (D-01) that reverts cleanly (D-04).
</verification>

<success_criteria>
- Instrumentation enables the Windows artifact to answer: does `getDisplayMedia` fire on the second click (H1)? does the main handler fire (H2)? is `callback` invoked exactly once per request (H3)? [STREAM-01, STREAM-04]
- Each request is counter-tagged so the cancel→re-click sequence is unambiguous in one trace.
- The diff is surgical and self-contained as one revertable commit, preserving upstream PR cleanliness. [UPST-01]
</success_criteria>

<output>
Create `.planning/phases/01-fix-bug-a-cancel-then-restart-works/01-01-SUMMARY.md` when done. In it, record: the exact `[ScreenshareDebug]` tag format and counter scheme used, the log file path, and a note that 01-02 must strip this commit before the upstream PR (D-04).
</output>
