---
phase: 03-delivery-path-spike-pcm-mediastream-go-no-go
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - .planning/phases/03-delivery-path-spike-pcm-mediastream-go-no-go/03-FINDINGS.md
  - .planning/phases/03-delivery-path-spike-pcm-mediastream-go-no-go/03-SPIKE-RUNBOOK.md
autonomous: false
requirements: []  # spike — no owned requirements; de-risks ECHO-01 (Phase 4)
user_setup: []

must_haves:
  truths:
    - "A copy-pasteable manual test runbook exists describing: triggering the Windows x64 CI build, launching the artifact with GOOFCORD_DELIVERY_SPIKE=1, confirming `spike-loaded` in screenshare-debug.log BEFORE any audio test, the second-device viewer-side audible check, and reading getStats from the log."
    - "The packaging-reachability proof is confirmed first: screenshare-debug.log contains a `spike-loaded` line from the launched CI artifact (empty/absent log ⇒ packaging gap, NOT a NO-GO)."
    - "A written GO/NO-GO verdict is recorded in 03-FINDINGS.md naming the working delivery mechanism + transport on GO, or naming Option A (virtual capture device, no kernel driver) as the first re-scope avenue on NO-GO."
    - "GO requires BOTH a second-device viewer audibly confirming the injected pattern AND screenshare-debug.log showing outbound-rtp audio packetsSent/bytesSent climbing."
    - "The verdict names the main→renderer PCM transport as the explicit residual risk Phase 4 must own (chunked transferables, never per-frame ipcRenderer.send)."
  artifacts:
    - path: ".planning/phases/03-delivery-path-spike-pcm-mediastream-go-no-go/03-SPIKE-RUNBOOK.md"
      provides: "Copy-pasteable manual CI-build + second-device verification protocol the developer runs on the Windows artifact"
      contains: "GOOFCORD_DELIVERY_SPIKE"
    - path: ".planning/phases/03-delivery-path-spike-pcm-mediastream-go-no-go/03-FINDINGS.md"
      provides: "The terminal GO/NO-GO decision document with observed results, mechanism named, residual risk recorded"
      contains: "GO/NO-GO"
  key_links:
    - from: "03-SPIKE-RUNBOOK.md"
      to: ".github/workflows/testBuild.yml"
      via: "manual workflow_dispatch trigger of the Windows x64 zip artifact"
      pattern: "testBuild|workflow_dispatch"
    - from: "03-FINDINGS.md verdict"
      to: "Phase 4 (ECHO-01)"
      via: "named working delivery path (GO) or Option A re-scope (NO-GO) + main→renderer transport residual risk"
      pattern: "GO|NO-GO|residual"
---

<objective>
Produce the spike's terminal deliverable: an explicit, written GO/NO-GO decision (`03-FINDINGS.md`), proven on a real Windows x64 CI artifact. The executor authors the manual test runbook and the verdict skeleton; the developer runs the inherently-manual second-device audible test (the streamer cannot self-verify — Electron mutes local echo) and records the observed result; the verdict is then finalized.

Purpose: SC#1 (viewer hears injected audio) is inherently manual on a second device against a Windows CI artifact — it cannot be a passing automated test in the executor's environment. This plan makes that honest: the acceptance criteria are "the CI artifact builds with the spike gate present, a documented manual protocol exists, the `spike-loaded` reachability proof is confirmed, and the verdict records the observed GO/NO-GO" — NOT a fake automated assertion.
Output: `03-SPIKE-RUNBOOK.md` (manual protocol) + `03-FINDINGS.md` (the GO/NO-GO verdict — this phase's whole reason to exist).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-delivery-path-spike-pcm-mediastream-go-no-go/03-CONTEXT.md
@.planning/phases/03-delivery-path-spike-pcm-mediastream-go-no-go/03-RESEARCH.md
@.planning/phases/03-delivery-path-spike-pcm-mediastream-go-no-go/03-01-SUMMARY.md
@.planning/phases/03-delivery-path-spike-pcm-mediastream-go-no-go/03-02-SUMMARY.md
@.github/workflows/testBuild.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the manual CI-build + second-device verification runbook</name>
  <files>.planning/phases/03-delivery-path-spike-pcm-mediastream-go-no-go/03-SPIKE-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/03-delivery-path-spike-pcm-mediastream-go-no-go/03-RESEARCH.md (§Validation Architecture L427-455 — manual runbook structure, SC->test map, Wave 0 gaps; §Pitfall 1 "Verification task the plan MUST include" L291; §Pitfall 3 second-device requirement L299-302; §Pitfall 2 MSTG-absent-is-still-GO L293-297)
    - .planning/phases/03-delivery-path-spike-pcm-mediastream-go-no-go/03-CONTEXT.md (§Decisions "GO/NO-GO success bar" — BOTH viewer-audible AND getStats packetsSent/bytesSent climbing; §"Verification (no DevTools)" — log to userData, second-device viewer-side, keep audio playing; §Decisions "Fallback if Option B fails" — NO-GO names Option A)
    - .planning/STATE.md (Blockers/Concerns — manual verification on CI artifact, second device/account, no DevTools 60%-keyboard constraint, screenshare-debug.log in userData)
    - .github/workflows/testBuild.yml (the `workflow_dispatch` Windows `windows-latest` x64 `zip` artifact build — the sole verification vehicle)
    - CLAUDE.md (Verification: manual via testBuild.yml Windows x64 CI artifact; no automated repro)
  </read_first>
  <action>
    Write `03-SPIKE-RUNBOOK.md` as a copy-pasteable, numbered manual protocol the developer runs. It MUST cover, in order:

    1. Trigger the Windows x64 CI build: `workflow_dispatch` on `.github/workflows/testBuild.yml` (windows-latest, `--x64`, `zip` target), download the `win-artifacts` zip from the run.
    2. Locate the userData `screenshare-debug.log` path on Windows (`%APPDATA%/goofcord/screenshare-debug.log` — `app.getPath("userData")`; note the portable-mode `goofcord-data` exception per utils.ts:16-18). State that there is NO DevTools available (60%-keyboard / no F12) — all diagnostics come from this file.
    3. Launch the artifact with the gate ON: set `GOOFCORD_DELIVERY_SPIKE=1` (or pass `--delivery-spike`) — give the exact Windows command to set the env var and launch the unpacked exe.
    4. PACKAGING-REACHABILITY GATE (do this BEFORE any audio test): open `screenshare-debug.log` and confirm it contains a `spike-loaded` line (with the logged `chrome=`/userAgent). If the log is empty or absent ⇒ the spike code did not ship/run (packaging gap, RESEARCH §Pitfall 1) — STOP and fix packaging; this is NOT a NO-GO. Also record the logged `mechanism=MSTG present/absent` line (answers SC#2).
    5. Second-device audible test (SC#1): on the streamer box start a Discord screenshare WITH audio; on a SECOND account/device join as viewer and listen. The streamer CANNOT self-verify (Electron mutes local echo — Pitfall 3). The viewer confirms hearing the distinctive injected beep/sweep (distinguishable from Discord audio/silence/system sounds). Keep the stream live long enough to read several getStats polls.
    6. getStats corroboration (SC#2): read `screenshare-debug.log` for `outbound-rtp audio packetsSent=/bytesSent=` lines across ≥2 polls and confirm the counters are CLIMBING (the viewer-independent objective signal), plus `track.readyState=live`/`muted=false`.
    7. Record raw observations (viewer heard? which mechanism? packetsSent climbing?) in a clearly-marked OBSERVATIONS section the verdict (Task 3) reads.

    Make explicit the GO/NO-GO bar: GO = BOTH viewer-audible AND climbing packetsSent. MSTG absent + Web Audio success + climbing packetsSent = GO via Web Audio (a fine outcome, MSTG refuted). NO-GO only if BOTH MSTG and Web Audio fail to reach the viewer.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-delivery-path-spike-pcm-mediastream-go-no-go/03-SPIKE-RUNBOOK.md && grep -q 'GOOFCORD_DELIVERY_SPIKE' .planning/phases/03-delivery-path-spike-pcm-mediastream-go-no-go/03-SPIKE-RUNBOOK.md && grep -q 'spike-loaded' .planning/phases/03-delivery-path-spike-pcm-mediastream-go-no-go/03-SPIKE-RUNBOOK.md && grep -q 'packetsSent' .planning/phases/03-delivery-path-spike-pcm-mediastream-go-no-go/03-SPIKE-RUNBOOK.md && grep -qi 'second' .planning/phases/03-delivery-path-spike-pcm-mediastream-go-no-go/03-SPIKE-RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - `03-SPIKE-RUNBOOK.md` exists and is a numbered, copy-pasteable protocol.
    - It triggers the Windows x64 `testBuild.yml` artifact, sets `GOOFCORD_DELIVERY_SPIKE=1`, and names the userData `screenshare-debug.log` path.
    - The `spike-loaded` packaging-reachability gate is explicitly ordered BEFORE the audio test, with the empty-log = packaging-gap (not NO-GO) caveat.
    - The second-device viewer-side step is present (streamer cannot self-verify) and the getStats `packetsSent` climbing corroboration is required for GO.
    - An OBSERVATIONS section exists for the developer to fill.
  </acceptance_criteria>
  <done>The runbook is complete enough that the developer can execute it end-to-end on a Windows box with no further questions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Developer runs the runbook on the Windows CI artifact and reports results</name>
  <files>.planning/phases/03-delivery-path-spike-pcm-mediastream-go-no-go/03-SPIKE-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/03-delivery-path-spike-pcm-mediastream-go-no-go/03-SPIKE-RUNBOOK.md (the protocol authored in Task 1 — the developer executes it verbatim)
    - .planning/STATE.md (Blockers/Concerns — second device/account required, no DevTools, screenshare-debug.log in userData)
  </read_first>
  <action>
    HUMAN-RUN checkpoint — no source change; this task gates Task 3. The developer executes `03-SPIKE-RUNBOOK.md` on a real Windows box against the Windows x64 CI artifact and reports the observed results so Task 3 can write the verdict. The executor MUST NOT proceed to Task 3 until the developer reports back. A missing/empty `screenshare-debug.log` at the reachability step is a packaging gap to fix in Plan 02 — NOT a NO-GO — and must be reported as such.
  </action>
  <what-built>
    Plans 01-02 added the env-gated spike: a userData `screenshare-debug.log` writer, a `GOOFCORD_DELIVERY_SPIKE` gate, and a packaged main-preload spike module that reconstructs a distinctive synthetic 48k/stereo/f32 audio track (MSTG probe → Web Audio fallback), swaps it into `getDisplayMedia`, captures the audio `RTCRtpSender`, and polls `getStats()`. Task 1 wrote `03-SPIKE-RUNBOOK.md`. Everything needed to run the manual test ships in the Windows x64 CI artifact behind the gate.
  </what-built>
  <how-to-verify>
    Follow `03-SPIKE-RUNBOOK.md` exactly:
    1. Trigger `.github/workflows/testBuild.yml` (Windows x64), download the `win-artifacts` zip, unpack it.
    2. Launch with `GOOFCORD_DELIVERY_SPIKE=1` (or `--delivery-spike`).
    3. REACHABILITY FIRST: confirm `%APPDATA%/goofcord/screenshare-debug.log` contains a `spike-loaded` line. If absent/empty → packaging gap (NOT a NO-GO) → report back so Plan 02's injection is fixed; do not proceed to the audio test.
    4. Note the logged `mechanism=MSTG present/absent` and `chrome=` version.
    5. Start a Discord screenshare WITH audio; on a SECOND account/device, join as viewer and listen for the distinctive injected beep/sweep (you cannot self-verify — Electron mutes local echo). Keep audio playing and the stream live for ~30s+.
    6. Read the log for `outbound-rtp audio packetsSent=/bytesSent=` across ≥2 polls; confirm climbing.
    7. Report: (a) did the viewer hear the injected pattern? (b) which mechanism succeeded? (c) is packetsSent climbing? (d) any errors in the log? Paste the relevant `screenshare-debug.log` lines.
  </how-to-verify>
  <verify>
    <human-check>Developer confirms (or refutes) viewer-audible + climbing packetsSent on the real Windows CI artifact, after the `spike-loaded` reachability proof, and pastes the relevant log lines.</human-check>
  </verify>
  <acceptance_criteria>
    - The `spike-loaded` reachability proof is confirmed in the artifact's `screenshare-debug.log` BEFORE any audio conclusion (or a packaging gap is reported for fix).
    - The developer reports: viewer-audible yes/no, which mechanism succeeded, packetsSent climbing yes/no, and any log errors, with relevant `screenshare-debug.log` lines pasted.
  </acceptance_criteria>
  <resume-signal>Report the observed results (viewer-audible yes/no, mechanism, packetsSent climbing yes/no, relevant log lines) — or describe a packaging-reachability failure to fix first.</resume-signal>
  <done>The developer has run the runbook and reported the observed reachability + audible + getStats results (or a packaging-gap fix request).</done>
</task>

<task type="auto">
  <name>Task 3: Write the terminal GO/NO-GO verdict (03-FINDINGS.md)</name>
  <files>.planning/phases/03-delivery-path-spike-pcm-mediastream-go-no-go/03-FINDINGS.md</files>
  <read_first>
    - .planning/phases/03-delivery-path-spike-pcm-mediastream-go-no-go/03-SPIKE-RUNBOOK.md (the OBSERVATIONS the developer filled in Task 2)
    - .planning/phases/03-delivery-path-spike-pcm-mediastream-go-no-go/03-CONTEXT.md (§Decisions "GO/NO-GO success bar"; §"Fallback if Option B fails" — NO-GO names Option A as first re-scope avenue, do NOT build it; §Decisions "Transport scope" — main→renderer transport is the named Phase 4 residual risk; §Decisions "Scaffolding lifecycle" — keep reconstruction as Phase 4 seed, throw away generator+getStats)
    - .planning/ROADMAP.md (Phase 3 Success Criteria SC#1-SC#4 — the verdict must address each; Phase 4 goal — what the GO path feeds)
    - .planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-FINDINGS.md (the verdict-document style precedent; cross-phase: the Audio-Service separate-process complication that informs Phase 4's PID resolution, NOT the spike)
    - .planning/phases/03-delivery-path-spike-pcm-mediastream-go-no-go/03-RESEARCH.md (§Summary; §Open Questions — the carried residual risk; ARCHITECTURE.md:250-253 per-frame ipcRenderer.send anti-pattern reference)
  </read_first>
  <action>
    Write `03-FINDINGS.md` as the explicit, written GO/NO-GO decision document — the phase's terminal deliverable. Mirror the `02-FINDINGS.md` style. Address EACH Success Criterion with the observed evidence from Task 2:

    - HEADLINE VERDICT: GO or NO-GO, stated up front.
    - SC#1 (viewer-audible): record whether the second-device viewer heard the injected pattern, on which Windows build, with the `screenshare-debug.log` lines pasted as evidence.
    - SC#2 (mechanism + corroboration): name which reconstruction mechanism succeeded (`MediaStreamTrackGenerator` confirmed-present, or refuted → Web Audio fallback), log the resolved `process.versions.chrome`, and the climbing `outbound-rtp audio packetsSent`/`bytesSent` evidence. This NAMES the working transport the GO path uses.
    - SC#3 (NO-GO handling): if GO, state it plainly. If NO-GO (BOTH MSTG and Web Audio failed to reach the viewer), document it as a real milestone-re-scope trigger and NAME Option A (a GoofCord-created virtual capture device read via `getUserMedia`, no kernel driver) as the FIRST re-scope avenue to investigate — do NOT build Option A here.
    - SC#4 (additive/throwaway split): confirm the spike was env-gated and additive (normal `"loopback"` path byte-identical when off), and record the keep/throwaway split — KEEP the MSTG/Web-Audio reconstruction as the Phase 4 seed; THROW AWAY the synthetic beep generator + getStats logging.
    - RESIDUAL RISK FOR PHASE 4 (mandatory): explicitly name the main→renderer PCM transport as unproven by this renderer-only spike — Phase 4 MUST own it using chunked `ArrayBuffer`/transferable transport, NEVER per-frame `ipcRenderer.send` of raw PCM (ARCHITECTURE.md:250-253). Also carry forward the Audio-Service separate-process PID-resolution note from 02-FINDINGS for Phase 4's exclude-target work.
    - HANDOFF TO PHASE 4: on GO, name the exact delivery path Phase 4 wires the real WASAPI capture into (reconstruction mechanism + swap seam at screensharePatch.ts:79-84 + the transport Phase 4 must build).

    Then update `.planning/STATE.md` (Current Position + Accumulated Context/Decisions) to record the Phase 3 verdict and unblock/redirect Phase 4 accordingly.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-delivery-path-spike-pcm-mediastream-go-no-go/03-FINDINGS.md && grep -qi 'GO/NO-GO\|VERDICT' .planning/phases/03-delivery-path-spike-pcm-mediastream-go-no-go/03-FINDINGS.md && grep -q 'SC#1' .planning/phases/03-delivery-path-spike-pcm-mediastream-go-no-go/03-FINDINGS.md && grep -qi 'residual\|Phase 4' .planning/phases/03-delivery-path-spike-pcm-mediastream-go-no-go/03-FINDINGS.md && grep -qi 'Option A\|MediaStreamTrackGenerator\|Web Audio' .planning/phases/03-delivery-path-spike-pcm-mediastream-go-no-go/03-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - `03-FINDINGS.md` states a headline GO or NO-GO verdict and addresses SC#1–SC#4 each with the observed Task 2 evidence (pasted log lines where relevant).
    - On GO: names the working reconstruction mechanism + the transport, and the exact delivery path Phase 4 will wire the real capture into.
    - On NO-GO: documents it as a milestone-re-scope trigger and names Option A as the first re-scope avenue (without building it).
    - The main→renderer PCM transport is named as the explicit Phase 4 residual risk (chunked transferables, never per-frame ipcRenderer.send).
    - The additive/env-gated/throwaway split is confirmed (keep reconstruction, throw away generator+getStats).
    - `.planning/STATE.md` updated with the verdict and Phase 4 direction.
  </acceptance_criteria>
  <done>The GO/NO-GO verdict document exists, is evidence-backed against SC#1–SC#4, names the residual transport risk, and STATE.md reflects the outcome.</done>
</task>

</tasks>

<verification>
- `03-SPIKE-RUNBOOK.md` and `03-FINDINGS.md` both exist; the runbook contains the gate var, the `spike-loaded` reachability step ordered before the audio test, the second-device step, and the getStats corroboration.
- `03-FINDINGS.md` carries a headline verdict, per-SC evidence, the named Phase 4 transport residual risk, and (on NO-GO) Option A as the first re-scope avenue.
- The packaging-reachability proof (`spike-loaded` confirmed in the artifact's log) is established before any GO/NO-GO conclusion is drawn.
</verification>

<success_criteria>
- The phase ends in an explicit, written, evidence-backed GO/NO-GO decision proven on a real Windows x64 CI artifact (SC#3).
- SC#1 (viewer-audible) and SC#2 (mechanism + climbing packetsSent) are recorded with the actual observed result, not a fake automated pass.
- SC#4 (additive/env-gated/throwaway split) is confirmed and the kept Phase-4 seed code is marked.
- The main→renderer PCM transport residual risk is carried forward to Phase 4 in writing.
</success_criteria>

<output>
Create `.planning/phases/03-delivery-path-spike-pcm-mediastream-go-no-go/03-03-SUMMARY.md` when done.
</output>
