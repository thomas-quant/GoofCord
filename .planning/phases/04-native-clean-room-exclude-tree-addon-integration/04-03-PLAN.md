---
phase: 04-native-clean-room-exclude-tree-addon-integration
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - src/modules/native/wasapiLoopback.ts
  - src/windows/screenshare/screenshare.ts
  - build/build.ts
  - .github/workflows/testBuild.yml
files_modified_note: "Touches wasapiLoopback.ts (created in 04-01) — sequential after 04-01 (wave 3). Consumes the .node from 04-02."
autonomous: false
requirements: [ECHO-01, ECHO-02, ECHO-03]
must_haves:
  truths:
    - "On a Windows x64 CI build, a second-device viewer hears the shared desktop/app audio but does NOT hear the Discord call echoed back (the core #46 fix, end-to-end with the REAL WASAPI addon behind the proven transport)."
    - "screenshare.ts dispatches via an additive 3-way gate: Linux patchcord (BYTE-IDENTICAL) → Windows native exclude-tree (tryStartWasapiLoopback) → existing \"loopback\" universal fallback. macOS and the Linux branch are unchanged (ECHO-03)."
    - "screenshare-debug.log records activation=ok|unsupported, the excluded root PID, the Audio Service PID, the hop-2 mechanism, and chunk count on a current Windows build (ECHO-02); on an unavailable-API build the addon require()s, reports 'unsupported', and falls through to \"loopback\" with no crash (ECHO-03)."
    - "The prebuilt wasapi-loopback-win32-x64.node is packaged into the Windows artifact and a CI packaging assertion proves it is present in the build output; an in-Electron N-API smoke call confirms it loads under Electron 41.3.0 (not just bare Node)."
  artifacts:
    - path: "src/modules/native/wasapiLoopback.ts"
      provides: "Real-addon wrapper: native-module .node load (require via createRequire), PID resolution, addon.start(rootPid, onChunk) → MessageChannelMain transport, in-Electron smoke, idempotent stop"
      contains: "native-module:../../../assets/native/wasapi-loopback-*.node"
    - path: "src/windows/screenshare/screenshare.ts"
      provides: "Additive 3-way audio dispatch gate (Linux patchcord → win32 native exclude-tree → loopback fallback)"
      contains: "tryStartWasapiLoopback"
    - path: "build/build.ts"
      provides: "copyNativeModules() third entry for wasapi-loopback with GOOFCORD_WASAPI_LOOPBACK_PATH env override → wasapi-loopback-win32-x64.node"
      contains: "GOOFCORD_WASAPI_LOOPBACK_PATH"
    - path: ".github/workflows/testBuild.yml"
      provides: "windows-latest Rust build step (napi build) + GOOFCORD_WASAPI_LOOPBACK_PATH for bun run build + packaging assertion that the .node is present"
      contains: "wasapi-loopback-win32-x64.node"
  key_links:
    - from: "src/windows/screenshare/screenshare.ts (audioConfig branch)"
      to: "tryStartWasapiLoopback()"
      via: "additive else-if process.platform === 'win32' && await tryStartWasapiLoopback()"
      pattern: "tryStartWasapiLoopback"
    - from: "wasapiLoopback.ts onChunk"
      to: "port1.postMessage(buf, [buf])"
      via: "real addon ThreadsafeFunction callback → the SAME MessageChannelMain transport proven in Plan 01"
      pattern: "port1\\.postMessage"
    - from: "build/build.ts copyNativeModules()"
      to: "assets/native/wasapi-loopback-win32-x64.node"
      via: "GOOFCORD_WASAPI_LOOPBACK_PATH env override → dest name contains both win32 AND x64 (nativeModulePlugin glob match)"
      pattern: "wasapi-loopback"
---

<objective>
Slice 2, part 2 (wire the real capture in + ship it). Swap the real Plan 02 WASAPI addon behind the EXACT transport proven GO in Plan 01, replacing the synthetic tone with real EXCLUDE-tree captured PCM. Integrate it into `screenshare.ts` as an additive 3-way gate, resolve the exclude-target root PID via `app.getAppMetrics()` (covering the Audio Service child), package the `.node` into the Windows artifact with a CI build step + packaging assertion + in-Electron N-API smoke, and verify the #46 echo fix viewer-side on a Windows x64 CI artifact.

After this slice, a real Windows user screensharing audio in a live call has viewers who hear desktop audio but NOT the call echoed back — the full milestone capability (ECHO-01), end-to-end, packaged, with graceful fallback everywhere the API is unavailable (ECHO-03).

Purpose: Deliver the integrated, packaged, verified echo fix. This is the last Phase 4 plan; Phase 5 only strips instrumentation and shapes the upstream PR.
Output: A real-addon `wasapiLoopback.ts`, the additive 3-way `screenshare.ts` gate, the `copyNativeModules()` entry + CI Rust build + packaging assertion + in-Electron smoke, and a viewer-side echo-fix verification recorded in `screenshare-debug.log`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04-native-clean-room-exclude-tree-addon-integration/04-CONTEXT.md
@.planning/phases/04-native-clean-room-exclude-tree-addon-integration/04-RESEARCH.md
@.planning/phases/04-native-clean-room-exclude-tree-addon-integration/04-PATTERNS.md
@.planning/phases/04-native-clean-room-exclude-tree-addon-integration/04-01-SUMMARY.md
@.planning/phases/04-native-clean-room-exclude-tree-addon-integration/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Real-addon wrapper + 3-way screenshare.ts gate + in-Electron N-API smoke</name>
  <files>src/modules/native/wasapiLoopback.ts, src/windows/screenshare/screenshare.ts</files>
  <read_first>
    - src/modules/native/wasapiLoopback.ts (current state from Plan 01 — the synthetic-tone transport host; this task swaps the synthetic source for the real addon's ThreadsafeFunction onChunk while KEEPING the MessageChannelMain port plumbing intact)
    - src/modules/native/venbind.ts (ANALOG — `native-module:` import glob L7, `createRequire` L14, `obtainVenbind()` load-once + `--no-venbind` guard L19-31; the new code uses `native-module:../../../assets/native/wasapi-loopback-*.node` and `--no-wasapi`)
    - src/modules/native/patchcord.ts (ANALOG — app.getAppMetrics PID L80; idempotent stop + before-quit Promise.race L151-184)
    - src/windows/screenshare/screenshare.ts (current state — the 2-way audioConfig gate at L90-100; PATTERNS.md §screenshare.ts lines 116-155 with the exact target shape lines 136-152)
    - .planning/phases/04-native-clean-room-exclude-tree-addon-integration/04-RESEARCH.md (§Pattern 4 3-way gate lines 325-346 + the result.audio="loopback" note line 346 / Assumption A3; §Code Examples tryStartWasapiLoopback JS-side fallback lines 491-506; Open Q1 in-Electron smoke lines 590-598)
    - .planning/phases/04-native-clean-room-exclude-tree-addon-integration/04-PATTERNS.md (§wasapiLoopback.ts divergences lines 54-58; §screenshare.ts target shape lines 135-155; §Shared Patterns error-handling lines 317-326)
    - .planning/phases/04-native-clean-room-exclude-tree-addon-integration/04-02-SUMMARY.md (the exact addon export signatures `start(excludeRootPid, onChunk)` / `stop()` this wrapper calls)
  </read_first>
  <action>
Convert `wasapiLoopback.ts` from the Plan 01 synthetic-tone host into the REAL-addon wrapper, KEEPING the MessageChannelMain port transport (the Plan 01 KEEP). Add the `.node` load (mirror venbind.ts):
- `// @ts-nocheck` header (Bun won't install the addon on macOS/linux — match venbind.ts:1), `import { createRequire } from "node:module"`, `// @ts-expect-error` `import wasapiPath from "native-module:../../../assets/native/wasapi-loopback-*.node"`. The glob `*` matches on `win32`+`x64` substrings — the name MUST be `wasapi-loopback-win32-x64.node` (Pitfall 3 — a mismatch silently emits `export default null` → silent "loopback" fallback). Log `wasapiPath resolved? <bool>` at load.
- `obtainWasapiLoopback()` — load-once flag + `--no-wasapi` guard (mirror `obtainVenbind` L19-31); `require(wasapiPath)` in try/catch; null on failure.
- `tryStartWasapiLoopback(): Promise<boolean>` — REPLACE the Plan 01 synthetic body: return false if `process.platform !== "win32"` or `--no-wasapi`; obtain the addon (null → log `wasapi unsupported: addon not loaded`, return false); resolve `const rootPid = process.pid`, log the PID line `wasapi exclude-root=${rootPid} audioService=${audioService?.pid ?? "not-found"} procs=...` (ECHO-02, RESEARCH lines 423-435); create the `MessageChannelMain`, `webContents.postMessage("wasapi:pcm-port", null, [port2])`, `port1.start()`; call `const ok = await addon.start(rootPid, (chunk) => port1.postMessage(toArrayBuffer(chunk), [transferred]))` — the real addon's ThreadsafeFunction onChunk pushes 480-frame f32 buffers which we forward zero-copy down `port1` (the SAME transport Plan 01 proved). Log `wasapi activation=${ok ? "ok" : "unsupported"} hop1=messageport chunks=<n>`. On `addon.start` resolving false (activation != S_OK) → log unsupported, close the port, return false (→ "loopback" fallback). Wrap in try/catch: on throw, `void appendScreenshareDebug(`wasapi start threw: ${getErrorMessage(e)}`)`, return false (NEVER crash — ECHO-03).
- In-Electron N-API smoke (SC#5, Open Q1 lines 596): at module init or first obtain, after `require`, call a no-op/identity export of the addon (or just confirm `typeof addon.start === "function"`) and log `wasapi smoke: loaded under electron napi ok` to `screenshare-debug.log`. This catches an ABI mismatch as a startup signal, not a wasted second-device round-trip (PITFALLS T3).
- KEEP `stopWasapiLoopback<IPCHandle>()` from Plan 01 but make it call `addon.stop()` + close `port1` (mirror patchcord stop + before-quit Promise.race so a hung native stop can't wedge quit — Pitfall 4). Idempotent.

In `screenshare.ts` (PATTERNS.md target shape lines 136-152): add an additive `else if (process.platform === "win32" && (await tryStartWasapiLoopback()))` branch BETWEEN the Linux patchcord branch and the `else { result.audio = "loopback" }` fallback. In the new branch, STILL set `result.audio = "loopback"` (Assumption A3 / RESEARCH note line 346) so the renderer's swap seam (stop/remove existing → addTrack reconstructed) is identical to Phase 3 / patchcord — the loopback track is captured-then-discarded, the reconstructed exclude-tree track replaces it. Add `import { tryStartWasapiLoopback } from "@root/src/modules/native/wasapiLoopback.ts"`. The Linux patchcord branch and the universal `"loopback"` fallback MUST remain byte-identical (ECHO-03 — do NOT refactor into a "shared" abstraction, RESEARCH anti-pattern).

The renderer-side MSTG feeder + hop-2 forward from Plan 01 are UNCHANGED — they already consume exactly the 480-frame/f32 chunks the real addon now emits.
  </action>
  <verify>
    <automated>bun run check</automated>
    <automated>grep -q "native-module:../../../assets/native/wasapi-loopback-\*.node" src/modules/native/wasapiLoopback.ts && grep -q "tryStartWasapiLoopback" src/windows/screenshare/screenshare.ts && echo WIRE_OK</automated>
    <automated>grep -A4 'process.platform === "linux"' src/windows/screenshare/screenshare.ts | grep -q "patchcordStartSystem" && echo LINUX_BRANCH_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - `wasapiLoopback.ts` imports `native-module:../../../assets/native/wasapi-loopback-*.node` and loads it via `createRequire` with a `--no-wasapi` guard (mirrors venbind).
    - `tryStartWasapiLoopback` forwards the real addon's chunks down the Plan 01 `port1` (`grep -c "port1.postMessage" src/modules/native/wasapiLoopback.ts` ≥ 1) and returns false (never throws) on addon-not-loaded / activation-unsupported / exception.
    - An in-Electron smoke log line (`wasapi smoke:`) is emitted after a successful require (SC#5).
    - `screenshare.ts` has a THREE-way gate: the Linux `patchcordStartSystem/App` branch is byte-identical, a new `else if (process.platform === "win32" && (await tryStartWasapiLoopback()))` branch exists, and the `else { result.audio = "loopback" }` fallback is unchanged.
    - `grep -v '^[[:space:]]*//' src/modules/native/wasapiLoopback.ts | grep -c "ipcRenderer.send"` returns 0 (locked anti-pattern absent).
    - `bun run check` passes.
  </acceptance_criteria>
  <done>The real WASAPI addon is wired behind the proven Plan 01 transport via an additive 3-way screenshare.ts gate; the wrapper loads the .node, resolves + logs the exclude-root and Audio Service PIDs, does an in-Electron smoke, and never crashes (falls through to "loopback"). Linux/macOS paths unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Packaging — copyNativeModules() entry + CI Rust build step + GOOFCORD_WASAPI_LOOPBACK_PATH + packaging assertion</name>
  <files>build/build.ts, .github/workflows/testBuild.yml</files>
  <read_first>
    - build/build.ts (current state — `copyNativeModules()` L168-232; the `modules[]` array L174-193 with venbind at L183-192; the envPath override branch L204-218 that builds dest as `${mod.name}-${platform}-${TARGET_ARCH}${ext}`; PATTERNS.md §build.ts lines 249-263)
    - build/nativeImport.ts (NO EDIT — the glob substring match L37-40 on `win32`+`x64`; confirms the name `wasapi-loopback-win32-x64.node` matches automatically; PATTERNS.md §nativeImport lines 267-276)
    - electron-builder.ts (NO EDIT — win `files: [...files, "!ts-out/native/*-linux-*.node"]` L45 KEEPS win32 .node; linux excludes win32; PATTERNS.md §testBuild lines 280-288)
    - .github/workflows/testBuild.yml (current state — single windows-latest job: checkout → setup-bun → setup-node 24.x → bun install → electron-builder -g → bun run build → electron-builder → upload; no .node build step yet)
    - .planning/phases/04-native-clean-room-exclude-tree-addon-integration/04-RESEARCH.md (Open Q1 resolution lines 590-598 — build the addon on the testBuild.yml windows runner behind the env override; §Pitfall 3 packaging / glob name mismatch lines 397-401)
    - .planning/phases/04-native-clean-room-exclude-tree-addon-integration/04-PATTERNS.md (§build.ts entry lines 249-263; §testBuild.yml additions lines 280-288; §.node consume pipeline lines 328-330)
    - .planning/phases/04-native-clean-room-exclude-tree-addon-integration/04-02-SUMMARY.md (the exact `napi build` command + output `.node` filename from Plan 02)
  </read_first>
  <action>
Make the prebuilt `.node` flow from the Plan 02 crate into the packaged Windows artifact, with a CI assertion that it actually shipped.

In `build/build.ts` `copyNativeModules()`: add a THIRD `modules[]` entry mirroring venbind (build.ts:183-192):
- `name: "wasapi-loopback"`, `envPath: process.env.GOOFCORD_WASAPI_LOOPBACK_PATH` (mirrors `GOOFCORD_VENBIND_PATH` L185), and a `prebuilds: [{ src: ["wasapi-loopback", "prebuilds", "windows-x86_64", "wasapi-loopback-win32-x64.node"], platform: "win32", arch: "x64" }]` entry (harmless for Phase 4 which uses the envPath branch; sets up the Phase 5 published-package path).
- CRITICAL (Pitfall 3): with `name: "wasapi-loopback"` the envPath branch (build.ts:207) produces dest `wasapi-loopback-win32-x64.node` on a win32/x64 build — containing BOTH `win32` AND `x64`, exactly what `nativeImport.ts` matches. Do NOT use a name lacking either substring (silent `export default null` → silent "loopback" fallback, looks like "the fix doesn't work" with no error).

In `.github/workflows/testBuild.yml` (windows-latest job, additions per RESEARCH Open Q1 lines 592-598):
1. BEFORE `bun run build`: add a Rust build step — `dtolnay/rust-toolchain@stable`, install `@napi-rs/cli` (in the `native/wasapi-loopback/` dir), run `napi build --release --target x86_64-pc-windows-msvc` (the exact command from 04-02-SUMMARY), producing `wasapi-loopback-win32-x64.node` (or rename the napi output to that name).
2. Set `GOOFCORD_WASAPI_LOOPBACK_PATH` (env on the `bun run build` step) → the produced `.node` so `copyNativeModules()` copies+renames it (build.ts:205-218 honors it). A Rust COMPILE ERROR fails the build here — caught BEFORE the second-device round-trip (PITFALLS T3 — the cheap failure mode).
3. AFTER `electron-builder` packages: add a PACKAGING ASSERTION step (PowerShell on windows-latest) that asserts `wasapi-loopback-win32-x64.node` is present in the packaged output (e.g. in `ts-out/native/` and/or inside the unpacked `.zip`/`app.asar.unpacked`) and FAILS the job if absent. A missing `.node` is otherwise a SILENT fallback (Pitfall 3).
NOTE: `electron-builder.ts` needs NO change — win `files` keeps `ts-out/native/*-win32-*.node` (only `*-linux-*.node` is excluded on win); the name matches what is NOT excluded.

This temporarily couples GoofCord's CI to Rust (the Phase-4-only pragmatic path per RESEARCH Open Q1); Phase 5 removes it when the addon moves to its own published repo + `optionalDependencies`. Mark the new CI steps with a comment noting the Phase-5 removal.
  </action>
  <verify>
    <automated>bun run build && test -f assets/native/wasapi-loopback-* 2>/dev/null; grep -q "wasapi-loopback" build/build.ts && grep -q "GOOFCORD_WASAPI_LOOPBACK_PATH" build/build.ts && echo BUILD_ENTRY_OK</automated>
    <automated>grep -q "wasapi-loopback-win32-x64.node" .github/workflows/testBuild.yml && grep -q "napi build" .github/workflows/testBuild.yml && grep -q "GOOFCORD_WASAPI_LOOPBACK_PATH" .github/workflows/testBuild.yml && echo CI_OK</automated>
  </verify>
  <acceptance_criteria>
    - `build/build.ts` `copyNativeModules()` has a third `modules[]` entry with `name: "wasapi-loopback"` and `envPath: process.env.GOOFCORD_WASAPI_LOOPBACK_PATH`.
    - `bun run build` succeeds on this (non-Windows) box WITHOUT the env var set (the wasapi entry's prebuild copy is best-effort `.catch(() => {})` like venbind's, so a missing prebuild does not fail the build — same as the existing venbind behavior on the wrong platform).
    - `.github/workflows/testBuild.yml` contains: a Rust toolchain + `napi build` step (`grep -c "napi build"` ≥ 1), `GOOFCORD_WASAPI_LOOPBACK_PATH` set for the `bun run build` step, and a packaging-assertion step referencing `wasapi-loopback-win32-x64.node`.
    - The dest filename produced by the envPath branch contains BOTH `win32` AND `x64` substrings (verified against build.ts:207 logic with `name: "wasapi-loopback"`).
    - `electron-builder.ts` is UNCHANGED (no edit needed — win build keeps `*-win32-*.node`).
  </acceptance_criteria>
  <done>The prebuilt wasapi-loopback-win32-x64.node is built on the windows-latest CI runner, copied into the artifact by copyNativeModules() via the env override, and a CI step asserts its presence in the packaged output. The local build is unaffected when the env var is absent.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual viewer-side echo-fix verification — the #46 fix end-to-end (ECHO-01/02/03)</name>
  <files>(no source files — manual viewer-side CI verification on a Windows x64 artifact)</files>
  <action>
This is a BLOCKING human-verify checkpoint, not an autonomous coding task — the human steps are in `<what-built>` / `<how-to-verify>` / `<resume-signal>` below. Claude's role: trigger `.github/workflows/testBuild.yml` (Windows x64), confirm the Rust build step AND the packaging assertion PASSED (the `.node` is in the artifact), then PAUSE for the developer's two-device echo-fix test. Do NOT self-assess complete — wait for the developer's "approved" with both viewer conditions (a)+(b), the PIDs, the activation result, the hop-2 mechanism, and chunk count.
  </action>
  <what-built>
The complete native echo fix: the clean-room WASAPI EXCLUDE-tree addon (Plan 02) captures everything except GoofCord's own Electron process tree, pushes 48k/stereo/f32 chunks over the Plan 01 MessageChannelMain transport → main-world MSTG feeder → getDisplayMedia swap seam → viewer. Wired via the additive 3-way screenshare.ts gate, packaged into the Windows x64 artifact with a CI packaging assertion + in-Electron N-API smoke, and graceful-fallback to "loopback" everywhere the API is unavailable.
  </what-built>
  <how-to-verify>
MANUAL, viewer-side — NO automated screenshare repro exists (CLAUDE.md / CONTEXT). The streamer CANNOT self-verify (Electron mutes local echo; WASAPI loopback is silent on an idle endpoint). Follow the PITFALLS.md two-device + audio-playing protocol (this re-confirms ECHO-01 and is the Phase 4 gate before Phase 5):

  1. Trigger `.github/workflows/testBuild.yml` (Windows x64). Confirm the build PASSED the Rust step AND the packaging assertion (the `.node` is in the artifact). Download `win-artifacts`.
  2. Launch the artifact on a Win10 2004/19041+ or Win11 box (NOT armed with the transport spike — this is the real path). Start a live call AND play non-call audio (music/video) OUTSIDE the GoofCord tree.
  3. With a SECOND device/account as the viewer, screenshare with audio.
  4. Confirm BOTH (the dispositive ground truth):
     - (a) the viewer HEARS the desktop/app audio you are playing (capture works), AND
     - (b) the viewer does NOT hear the Discord call voices echoed back (EXCLUDE works — the #46 fix).
  5. Read userData `screenshare-debug.log` and confirm:
     - `wasapi smoke: loaded under electron napi ok` (SC#5 — N-API loads under Electron, not just bare Node)
     - `wasapi exclude-root=<rootPid> audioService=<asPid> procs=<name:pid,...>` (ECHO-02 — root PID logged, Audio Service PID present)
     - `wasapi activation=ok hop1=messageport hop2=<port-forward|contextBridge> chunks=<n>` with n > 0 (activated on a current build)
  6. ECHO-03 graceful-fallback check: confirm Linux (patchcord) audio still works and macOS is untouched (no Windows-only code runs there); on any Windows build where activation fails, confirm the log shows `activation=unsupported` and audio falls through to "loopback" with no crash.

A "pass" report MUST include: the build number, the second-device confirmation of BOTH (a) and (b), audio actively playing, the excluded root PID, the Audio Service PID, the activation result, the hop-2 mechanism, and chunk count > 0. A report missing any of these is NOT a pass (PITFALLS V1-V3 / V5). If echo persists despite `activation=ok`, the Audio Service may be a detached sibling (Assumption A4) — record the PIDs and flag for excluding a broader ancestor.
  </how-to-verify>
  <resume-signal>Type "approved" with the build number, both (a)+(b) viewer confirmations, the excluded root PID + Audio Service PID, activation result, hop-2 mechanism, and chunk count — or describe the failure (echo persists / silence / fallback on a current build / packaging assertion failed) for gap closure.</resume-signal>
  <verify>
    <human-check>On a Windows x64 CI artifact (Rust build + packaging assertion green), a second-device viewer hears the shared desktop audio AND does NOT hear the Discord call echoed back, with non-call audio actively playing; screenshare-debug.log shows `wasapi smoke: loaded under electron napi ok`, the excluded root PID + Audio Service PID, `activation=ok hop1=messageport hop2=<mechanism> chunks=<n>` (n>0). Linux patchcord still works; macOS untouched; unavailable-API builds fall through to "loopback" with no crash.</human-check>
  </verify>
  <done>Developer returns "approved" confirming BOTH (a) viewer hears desktop audio and (b) no call echo, plus the build number, PIDs, activation=ok, hop-2 mechanism, and chunk count > 0 — the #46 echo fix is verified end-to-end. Graceful fallback + Linux/macOS non-regression confirmed.</done>
</task>

</tasks>

<verification>
- `bun run check` and `bun run build` pass on this box (automatable; the wasapi prebuild copy is best-effort so a missing prebuild off-Windows does not fail the build).
- CI assertions (automatable on the runner): the Rust `napi build` step succeeds, `GOOFCORD_WASAPI_LOOPBACK_PATH` is honored by `copyNativeModules()`, and the packaging-assertion step confirms `wasapi-loopback-win32-x64.node` is in the artifact (Pitfall 3 — a missing .node is otherwise silent).
- 3-way gate byte-identity: the Linux patchcord branch and the `"loopback"` fallback are unchanged (source review — ECHO-03).
- In-Electron N-API smoke (SC#5): `wasapi smoke: loaded under electron napi ok` in `screenshare-debug.log` proves the .node loads under Electron 41.3.0, not just bare Node.
- Viewer-side echo fix (MANUAL, Task 3): second-device viewer hears desktop audio AND no call echo on a Windows x64 CI artifact with audio playing; log records activation=ok, excluded root PID, Audio Service PID, hop-2 mechanism, chunk count (ECHO-01/02). Graceful fallback confirmed on unavailable-API builds + Linux/macOS non-regression (ECHO-03).
</verification>

<success_criteria>
The native echo fix is integrated, packaged, and verified end-to-end: on a Windows x64 CI build a second-device viewer hears shared desktop/app audio but NOT the Discord call echoed back (ECHO-01); the native exclude-tree path activates on a current Windows build with the excluded root PID + Audio Service PID logged (ECHO-02); the additive 3-way gate preserves Linux/macOS and falls through to "loopback" with no crash where the API is unavailable (ECHO-03); and the `.node` is packaged with a CI assertion + in-Electron N-API smoke (SC#5). Phase 4 is complete; Phase 5 strips instrumentation and shapes the upstream PR.
</success_criteria>

<output>
Create `.planning/phases/04-native-clean-room-exclude-tree-addon-integration/04-03-SUMMARY.md` when done. Record: the verified build number, the BOTH-conditions viewer confirmation, the excluded root PID + Audio Service PID + whether the exclude covered the call audio (Assumption A4 outcome), the activation result, the confirmed hop-2 mechanism, chunk count, whether the loopback device honored 48k/stereo/f32 or forced another format (Assumption A2 outcome), and the graceful-fallback + Linux/macOS non-regression results. Note any residual items for Phase 5 (instrumentation to strip, the addon-repo extraction, optionalDependencies).
</output>
