---
phase: 04-native-clean-room-exclude-tree-addon-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/native/wasapiLoopback.ts
  - src/windows/main/preload/preload.mts
  - src/windows/main/preload/bridge.ts
  - src/windows/main/renderer/postVencord/screensharePatch.ts
  - src/ipc/gen.ts    # GENERATED — regenerated via `bun run build --onlyGenerators`, never hand-authored
  - src/ipc/types.ts  # GENERATED — regenerated via `bun run build --onlyGenerators`, never hand-authored
autonomous: false
requirements: [ECHO-01]
must_haves:
  truths:
    - "With GOOFCORD_TRANSPORT_SPIKE armed, a second-device viewer hears the distinctive synthetic tone — proving main-process PCM reaches the viewer over the real MessagePort transport (the last unproven half of the make-or-break path)."
    - "screenshare-debug.log records which hop-2 mechanism worked (hop2=port-forward ready-handshake ok | hop2=contextBridge-fallback), the hop-1 mechanism (hop1=messageport), and a non-zero chunk count — auditable without DevTools."
    - "With the spike gate OFF, the normal Windows \"loopback\" path is byte-identical to upstream (no injection fires)."
    - "Linux (patchcord) and macOS audio paths are untouched."
  artifacts:
    - path: "src/modules/native/wasapiLoopback.ts"
      provides: "Main-process transport host: MessageChannelMain port handshake + synthetic 48k/stereo/f32 tone source (throwaway) + stopWasapiLoopback teardown"
      contains: "MessageChannelMain"
    - path: "src/windows/main/preload/preload.mts"
      provides: "Hop-2 port-forward into the page main world via window.postMessage(..., [port]) with readiness handshake, behind the spike gate"
      contains: "wasapi:pcm-port"
    - path: "src/windows/main/renderer/postVencord/screensharePatch.ts"
      provides: "Main-world MSTG feeder fed from the forwarded MessagePort + swap seam + STREAM_CLOSE teardown"
      contains: "MediaStreamTrackGenerator"
  key_links:
    - from: "src/modules/native/wasapiLoopback.ts"
      to: "preload.mts (wasapi:pcm-port)"
      via: "mainWindow.webContents.postMessage('wasapi:pcm-port', null, [port2])"
      pattern: "webContents\\.postMessage"
    - from: "preload.mts"
      to: "page main world MSTG feeder"
      via: "window.postMessage(..., [port]) after ready handshake"
      pattern: "window\\.postMessage"
    - from: "main-world MSTG feeder"
      to: "Discord getDisplayMedia MediaStream"
      via: "swap seam at screensharePatch.ts:79-84 (stop/removeTrack → addTrack)"
      pattern: "stream\\.addTrack"
---

<objective>
Slice 1 (MVP vertical slice — the transport spike, CONTEXT T1). Prove the SECOND half of the make-or-break delivery path that Phase 3 left unproven: getting audio from the MAIN PROCESS into the page main world over the real chunked-transferable MessagePort transport, and on to a remote viewer. Phase 3 proved the renderer half with a synthetic in-page source; this slice moves the synthetic source to the main process and ships it over the REAL transport — exactly mirroring the Phase 3 de-risk instinct, so a NO-GO turns on the transport alone, not on native WASAPI code that does not exist yet.

This is a complete end-to-end working slice: armed by an env gate, a synthetic 48k/stereo/f32 tone generated in the main process is batched into ~10 ms / 480-frame ArrayBuffer chunks, transferred over `MessageChannelMain` → `webContents.postMessage` (hop-1) → preload `window.postMessage(..., [port])` (hop-2) → main-world `MediaStreamTrackGenerator` feeder → the proven `getDisplayMedia` swap seam → viewer. After this slice, a real second-device viewer can HEAR the tone — something not possible after Phase 3 (which had no IPC transport at all).

Purpose: De-risk the transport (Phase 3 Residual Risk #1) before any native investment. The synthetic tone is THROWAWAY; the transport plumbing it proves (`wasapiLoopback.ts` MessageChannelMain host, the hop-2 port-forward, the MessagePort-fed MSTG feeder) is the KEEP that Slice 2's real WASAPI addon swaps behind.
Output: A new `wasapiLoopback.ts` transport host, hop-2 forwarding in the preload, a MessagePort-fed MSTG feeder in `screensharePatch.ts`, regenerated IPC, and a manual viewer-side CI GO verdict recorded in `screenshare-debug.log`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04-native-clean-room-exclude-tree-addon-integration/04-CONTEXT.md
@.planning/phases/04-native-clean-room-exclude-tree-addon-integration/04-RESEARCH.md
@.planning/phases/04-native-clean-room-exclude-tree-addon-integration/04-PATTERNS.md
@.planning/phases/03-delivery-path-spike-pcm-mediastream-go-no-go/03-FINDINGS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Main-process transport host — wasapiLoopback.ts with MessageChannelMain + synthetic tone source (KEEP transport, THROWAWAY tone)</name>
  <files>src/modules/native/wasapiLoopback.ts (authored); src/ipc/gen.ts, src/ipc/types.ts (GENERATED — produced by `bun run build --onlyGenerators`, never hand-authored)</files>
  <read_first>
    - src/modules/native/wasapiLoopback.ts (does not exist yet — creating it; confirm absence first)
    - src/modules/native/venbind.ts (PRIMARY ANALOG — load-once flag, `--no-venbind` guard pattern, `mainWindow` import at L12, `pc.green` LOG_PREFIX; PATTERNS.md §wasapiLoopback.ts lines 27-58)
    - src/modules/native/patchcord.ts (SECONDARY ANALOG — `stopPatchcord<IPCHandler>` L151-166 idempotent stop, `before-quit` Promise.race([dispose, timeout]) L168-184, `pc.cyan("[Screenshare]")` prefix; PATTERNS.md §stop/teardown lines 90-110)
    - src/modules/screenshareDebug.ts (REUSE — `appendScreenshareDebug<IPCHandle>` writer + `isDeliverySpikeEnabled<IPCOn>` gate precedent; this task adds a sibling `isTransportSpikeEnabled` gate)
    - .planning/phases/04-native-clean-room-exclude-tree-addon-integration/04-RESEARCH.md (§Pattern 1 hop-1 lines 248-266; §Code Examples PID resolution lines 423-435; required log-line content lines 507-510)
    - .planning/phases/04-native-clean-room-exclude-tree-addon-integration/04-PATTERNS.md (§wasapiLoopback.ts full section lines 22-113; §Shared Patterns — diagnostics + native lifecycle lines 301-334; §IPC handle marker line 112)
    - src/utils.ts (getErrorMessage — catch `unknown` → string, per CLAUDE.md error-handling convention)
  </read_first>
  <action>
Create `src/modules/native/wasapiLoopback.ts` as the venbind-shaped main-process transport host. For THIS spike it ships a SYNTHETIC tone (throwaway), NOT WASAPI — the file name and shape are the KEEP that Slice 2 (Plan 02/03) swaps a real addon into.

Add a sibling spike gate to `src/modules/screenshareDebug.ts`: `export function isTransportSpikeEnabled<IPCOn>() { return process.env.GOOFCORD_TRANSPORT_SPIKE === "1" || process.argv.includes("--transport-spike"); }` (mirror `isDeliverySpikeEnabled` exactly; mark THROWAWAY in the existing file header comment).

In `wasapiLoopback.ts`:
- Import `MessageChannelMain` from "electron", `mainWindow` from "../../windows/main/main.ts" (same import venbind.ts:12 uses), `appendScreenshareDebug` from "@root/src/modules/screenshareDebug.ts", `getErrorMessage` from "@root/src/utils.ts", `pc` from "picocolors". LOG_PREFIX = `pc.cyan("[Screenshare]")`.
- `export async function tryStartWasapiLoopback(): Promise<boolean>` — for the spike: if `!isTransportSpikeEnabled()` return false; resolve `const rootPid = process.pid` and `const audioService = app.getAppMetrics().find((p) => p.name === "Audio Service")` and log the line `wasapi exclude-root=${rootPid} audioService=${audioService?.pid ?? "not-found"} procs=${metrics.map((p)=>`${p.name}:${p.pid}`).join(",")}` (RESEARCH lines 423-435 — this logging is real even in the spike so ECHO-02's PID discipline is exercised early). Then create `const { port1, port2 } = new MessageChannelMain()`, transfer `port2` to the renderer with `mainWindow.webContents.postMessage("wasapi:pcm-port", null, [port2])`, call `port1.start()`, start a `setInterval` at ~10 ms that synthesizes a 480-frame interleaved-stereo f32 distinctive tone (440→660 Hz sweep — copy the cadence shape from `deliverySpike.ts:85-91 makeDistinctiveSample`, but generate in the MAIN process), packs it into a fresh `ArrayBuffer` of 480*2*4 = 3840 bytes, and posts it with `port1.postMessage(buf, [buf])` (transfer list → zero-copy, NOT structured clone — RESEARCH line 264). Maintain a chunk counter; periodically log `wasapi activation=spike-synthetic hop1=messageport chunks=${n}`. Return true.
- `export async function stopWasapiLoopback<IPCHandle>()` — idempotent: clear the synthetic interval, close `port1`, null state (mirror `stopPatchcord` L151-166). Make it safe to call twice.
- `app.on("before-quit", ...)` with `Promise.race([<teardown>, setTimeout 1500])` so a hung stop can't wedge quit (mirror patchcord.ts:168-184).
- Wrap start in try/catch: on throw, `void appendScreenshareDebug(`wasapi start threw: ${getErrorMessage(e)}`)` and return false (never crash — the byte-identical-fallback discipline ECHO-03 will later rely on).

Use `<IPCHandle>` on `stopWasapiLoopback` (channel `wasapiLoopback:stopWasapiLoopback`) and `<IPCOn>` on `isTransportSpikeEnabled`. After adding these, run `bun run build --onlyGenerators` to regenerate `src/ipc/gen.ts` and `src/ipc/types.ts` — NEVER hand-edit those two files (CLAUDE.md IPC-codegen constraint).

Do NOT touch the Linux/macOS branches. Do NOT introduce per-frame `ipcRenderer.send` of PCM (locked anti-pattern T2, ARCHITECTURE.md:250-253).
  </action>
  <verify>
    <automated>bun run build --onlyGenerators && grep -q "wasapiLoopback:stopWasapiLoopback" src/ipc/gen.ts && grep -q "screenshareDebug:isTransportSpikeEnabled" src/ipc/gen.ts && echo GEN_OK</automated>
    <automated>bun run check</automated>
  </verify>
  <acceptance_criteria>
    - `src/modules/native/wasapiLoopback.ts` exists and exports `tryStartWasapiLoopback` and `stopWasapiLoopback<IPCHandle>`.
    - `grep -c "MessageChannelMain" src/modules/native/wasapiLoopback.ts` returns ≥ 1; `grep -c "port1.postMessage" src/modules/native/wasapiLoopback.ts` returns ≥ 1 and the postMessage uses a transfer list `[buf]`.
    - `grep -v '^[[:space:]]*//' src/modules/native/wasapiLoopback.ts | grep -c "ipcRenderer.send"` returns 0 (locked anti-pattern absent).
    - `bun run build --onlyGenerators` regenerates gen.ts; `grep -q "wasapiLoopback:stopWasapiLoopback" src/ipc/gen.ts` and `grep -q "screenshareDebug:isTransportSpikeEnabled" src/ipc/gen.ts` both succeed.
    - `bun run check` (tsgo) passes with no new errors.
    - `screenshareDebug.ts` gains `isTransportSpikeEnabled<IPCOn>` mirroring `isDeliverySpikeEnabled`.
  </acceptance_criteria>
  <done>wasapiLoopback.ts ships the synthetic-tone-over-MessagePort transport host, regenerated IPC exposes the new channels, and the project type-checks. No Linux/macOS code changed; no per-frame PCM IPC.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Hop-2 port-forward in preload + MessagePort-fed MSTG feeder in the main world (renderer)</name>
  <files>src/windows/main/preload/preload.mts, src/windows/main/preload/bridge.ts, src/windows/main/renderer/postVencord/screensharePatch.ts</files>
  <read_first>
    - src/windows/main/preload/preload.mts (current state — `injectDeliverySpike()` gated `webFrame.executeJavaScript` injection at L29-40; PATTERNS.md §preload lines 201-235)
    - src/windows/main/preload/bridge.ts (current state — `api` object L13-51; `stopPatchcord` L44 and the THROWAWAY spike fields L45-47 are the precedent for adding `stopWasapiLoopback` + the contextBridge fallback method)
    - src/windows/main/preload/deliverySpike.ts (KEEP SEED — `buildSyntheticAudioTrack()` MSTG feeder L78-163, the swap-seam injection L246-279, `spikeMainWorldSource` serialization L284-287; the MSTG/AudioData/monotonic-µs shape is IDENTICAL to what this task needs — replace the synthetic SAMPLE loop with MessagePort-delivered chunks)
    - src/windows/main/renderer/postVencord/screensharePatch.ts (current state — swap seam L79-84, STREAM_CLOSE L90-104; PATTERNS.md §screensharePatch lines 159-197)
    - .planning/phases/04-native-clean-room-exclude-tree-addon-integration/04-RESEARCH.md (§Pattern 2 hop-2 lines 268-294 INCLUDING the load-bearing readiness-handshake note line 294; §Pitfall 1 readiness race lines 385-389; §KEEP seed MSTG feeder lines 512-531 with bounded-ring guidance line 531)
    - .planning/phases/04-native-clean-room-exclude-tree-addon-integration/04-PATTERNS.md (§preload hop-2 lines 201-245; §bridge.ts addition lines 239-245)
  </read_first>
  <action>
Wire hop-2 (preload isolated world → page main world) plus the MessagePort-fed MSTG feeder. DEFAULT mechanism = zero-copy port-forward (RESEARCH resolved it viable on Electron 41 / Chromium 146); the contextBridge structured-clone callback is the recorded FALLBACK.

In `preload.mts` (mirror `injectDeliverySpike` at L32-40, gate on the new `screenshareDebug:isTransportSpikeEnabled` sync channel):
- Add `injectWasapiTransport()` that, when the gate is on, injects a self-contained main-world script string (built like `spikeMainWorldSource` via `.toString()` of an installer function — author it in a new `src/windows/main/preload/wasapiTransport.ts`, mirroring deliverySpike.ts's serialization pattern) via `webFrame.executeJavaScript`. The main-world script must register its `window.addEventListener("message", ...)` listener and THEN post `window.postMessage("goofcord:wasapi-ready", "*")` so the preload knows the main world is listening (READINESS HANDSHAKE — Pitfall 1 is load-bearing; a port forwarded before the listener exists silently loses the port + first chunks → viewer hears silence).
- Add `ipcRenderer.on("wasapi:pcm-port", (event) => { ... })` that buffers `event.ports[0]` until it has seen `goofcord:wasapi-ready`, then forwards with `window.postMessage("goofcord:wasapi-pcm-port", "*", [port])` (zero-copy port→port). Log the outcome via the goofcord bridge: `hop2=port-forward ready-handshake ok` on success.
- If `window.postMessage` with a port transfer is not honored into the injected main world on this build, fall back: land PCM in the preload's `ipcRenderer.on` handler and invoke a main-world callback via a new `goofcord` contextBridge method (structured-clones each chunk); log `hop2=contextBridge-fallback`. Add that fallback method on the `api` object in `bridge.ts` (mirror `appendScreenshareDebug` L47). The spike RECORDS which path worked — that is the auditable T3 resolution.

In `bridge.ts`: add `stopWasapiLoopback: () => invoke("wasapiLoopback:stopWasapiLoopback")` (mirror `stopPatchcord` L44) so the main-world STREAM_CLOSE handler can tear capture down.

In the main-world feeder (the new `wasapiTransport.ts` installer, structurally a clone of `deliverySpike.ts`'s `buildSyntheticAudioTrack` but fed externally): on `port.onmessage`, push the transferred ArrayBuffer into a BOUNDED RING (T4: ~3-5 chunk / ~30-50 ms depth; drop-oldest on overflow; on underrun write a zero-filled `AudioData` of the same 480-frame shape to keep the MSTG timeline monotonic — RESEARCH line 531). A drain loop reads the ring and `writer.write(new AudioData({ format:"f32", sampleRate:48000, numberOfFrames:480, numberOfChannels:2, timestamp: tsUs, data }))` with monotonic `tsUs += round((480/48000)*1e6)` (RESEARCH lines 516-528 — the monotonic-µs requirement is load-bearing or frames garble). The MSTG instance IS the live track; swap it in at the seam (stop/removeTrack existing → addTrack the gen — screensharePatch.ts:79-84 shape). Wire STREAM_CLOSE/track-ended teardown: clear the drain loop, release the writer, close the port, call `GoofCord.stopWasapiLoopback()`.

Keep all of this GATED and THROWAWAY-marked (header comment matching deliverySpike.ts L1-3). With the gate off the page is byte-identical to upstream.
  </action>
  <verify>
    <automated>bun run build</automated>
    <automated>bun run check</automated>
    <automated>grep -q "goofcord:wasapi-ready" src/windows/main/preload/wasapiTransport.ts && grep -q "MediaStreamTrackGenerator" src/windows/main/preload/wasapiTransport.ts && grep -q "stopWasapiLoopback" src/windows/main/preload/bridge.ts && echo HOP2_OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/windows/main/preload/wasapiTransport.ts` exists, contains a `MediaStreamTrackGenerator` feeder fed from a `MessagePort.onmessage`, and posts `goofcord:wasapi-ready` BEFORE the port is forwarded (readiness handshake present — `grep -c "goofcord:wasapi-ready"` ≥ 1).
    - `preload.mts` gates injection on `screenshareDebug:isTransportSpikeEnabled` (`grep -c "isTransportSpikeEnabled" src/windows/main/preload/preload.mts` ≥ 1) and forwards the port via `window.postMessage(...)` with a transfer list (`grep -c "goofcord:wasapi-pcm-port" src/windows/main/preload/preload.mts` ≥ 1).
    - `bridge.ts` exposes `stopWasapiLoopback: () => invoke("wasapiLoopback:stopWasapiLoopback")`.
    - The bounded ring is present with a finite depth constant (3-5 chunks) and a drop-oldest branch; underrun writes a zero-filled AudioData (source-visible in `wasapiTransport.ts`).
    - `bun run build` (Bun bundle of main + preloads + renderer) succeeds; `bun run check` passes.
    - With the gate OFF, no transport code path executes (the injection is behind the `isTransportSpikeEnabled` guard, identical structure to `injectDeliverySpike`).
  </acceptance_criteria>
  <done>The preload forwards the MessagePort into the main world after a ready handshake (default) with a contextBridge fallback; the main-world MSTG feeder consumes transferred PCM chunks through a bounded ring and swaps the reconstructed track into the getDisplayMedia stream. Build + type-check pass; gate-off path is byte-identical.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual viewer-side CI verification — transport-spike GO/NO-GO (the slice-completing gate)</name>
  <files>(no source files — manual viewer-side CI verification on a Windows x64 artifact)</files>
  <action>
This is a BLOCKING human-verify checkpoint, not an autonomous coding task — the human steps are in `<what-built>` / `<how-to-verify>` / `<resume-signal>` below. Claude's role: trigger the `.github/workflows/testBuild.yml` Windows x64 CI build (workflow_dispatch on this branch), confirm the `win-artifacts` upload succeeded, then PAUSE for the developer's viewer-side audible test. Do NOT self-assess this task complete — wait for the developer's GO with the confirmed hop-2 mechanism and chunk count.
  </action>
  <what-built>
The full main→renderer→main-world PCM transport: a synthetic 48k/stereo/f32 tone generated in the MAIN process, batched into 480-frame ArrayBuffer chunks, transferred over `MessageChannelMain` → `webContents.postMessage` (hop-1) → preload `window.postMessage(..., [port])` (hop-2, port-forward default) → main-world `MediaStreamTrackGenerator` feeder → the proven `getDisplayMedia` swap seam → Discord's RTCPeerConnection. All gated behind `GOOFCORD_TRANSPORT_SPIKE`; all diagnostics to userData `screenshare-debug.log`.
  </what-built>
  <how-to-verify>
This acceptance is MANUAL and viewer-side — there is NO automated screenshare repro on Windows (CLAUDE.md / CONTEXT verification constraint). The streamer CANNOT self-verify (Electron mutes local echo). Follow the Phase 3 runbook discipline (03-SPIKE-RUNBOOK.md is the template):

  1. Trigger the Windows x64 CI build: `.github/workflows/testBuild.yml` (workflow_dispatch on this branch). Download the `win-artifacts` zip.
  2. Launch the artifact with the gate armed: set `GOOFCORD_TRANSPORT_SPIKE=1` (or pass `--transport-spike`).
  3. With a SECOND device/account as the viewer, start a screenshare with audio in a live call.
  4. Confirm the viewer HEARS the distinctive synthetic 440→660 Hz sweep tone (NOT silence, NOT Discord audio).
  5. Open userData `screenshare-debug.log` and confirm these lines are present:
     - `wasapi exclude-root=<pid> audioService=<pid|not-found> procs=<name:pid,...>`
     - `wasapi activation=spike-synthetic hop1=messageport chunks=<n>` with n > 0
     - the hop-2 outcome line: `hop2=port-forward ready-handshake ok` (default) OR `hop2=contextBridge-fallback`
  6. Record the working hop-2 mechanism in the SUMMARY — this settles CONTEXT T3 empirically.

GO = viewer hears the tone AND the log shows non-zero chunks + a recorded hop-2 mechanism. NO-GO = silence despite chunks pushed (likely the readiness-handshake race — Pitfall 1; the hop-2 line will be missing `ready ok`), which must be documented, not absorbed, before Plan 02 proceeds.
  </how-to-verify>
  <resume-signal>Type "GO" with the confirmed hop-2 mechanism and chunk count from screenshare-debug.log, or describe the failure (missing audio / missing log lines / handshake race) for transport debugging before the native addon is built.</resume-signal>
  <verify>
    <human-check>Second-device viewer hears the synthetic 440→660 Hz tone on a Windows x64 CI artifact, AND screenshare-debug.log shows hop1=messageport, a recorded hop-2 mechanism (port-forward ready-handshake ok | contextBridge-fallback), and chunks > 0.</human-check>
  </verify>
  <done>Developer returns "GO" with the confirmed hop-2 mechanism and a non-zero chunk count; the transport is proven viewer-audible. (NO-GO if the viewer hears silence or the hop-2 ready line is missing — document, do not absorb.)</done>
</task>

</tasks>

<verification>
- `bun run build` and `bun run check` pass (automatable; prove the code compiles and type-checks).
- Regenerated IPC contains `wasapiLoopback:stopWasapiLoopback` and `screenshareDebug:isTransportSpikeEnabled` (automatable grep on gen.ts).
- Gate-off byte-identity: the transport injection is behind the `isTransportSpikeEnabled` guard exactly as `injectDeliverySpike` is behind `isDeliverySpikeEnabled` (source review; mirrors the Phase 3 SC#4 byte-identical proof).
- Viewer-side GO (MANUAL, Task 3): second-device viewer hears the synthetic tone over the real transport on a Windows x64 CI artifact; `screenshare-debug.log` records hop-1 = messageport, the hop-2 mechanism, and chunk count > 0.
</verification>

<success_criteria>
The main→renderer→main-world PCM transport (Phase 3 Residual Risk #1) is PROVEN end-to-end on a Windows x64 CI artifact: a synthetic main-process tone reaches a second-device viewer over `MessageChannelMain` + `window.postMessage(..., [port])`, with the working hop-2 mechanism recorded in `screenshare-debug.log`. The transport plumbing (`wasapiLoopback.ts` host, hop-2 forward, MessagePort-fed MSTG feeder) is the KEEP that Plan 02/03's real WASAPI addon swaps behind. GO gates Slice 2.
</success_criteria>

<output>
Create `.planning/phases/04-native-clean-room-exclude-tree-addon-integration/04-01-SUMMARY.md` when done. Record: the confirmed hop-2 mechanism (T3 resolution), observed chunk count, the excluded-root + Audio-Service PIDs from the log, and the GO/NO-GO verdict for the transport.
</output>
