---
phase: 02-fix-bug-b-windows-loopback-audio-captured
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-FINDINGS.md
  - .planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-RECON-RUNBOOK.md
autonomous: true
requirements: [AUDIO-01, AUDIO-02]

must_haves:
  truths:
    - "A findings document exists with the four success-criteria sections scaffolded (Mechanism+Evidence, Replication Parameters, Clean-room Go/No-Go, Recon-only Boundary)"
    - "The desk-research-able public baseline is written: the public WASAPI process-loopback API surface, the min Windows build 20348, the macOS-driver contrast, and the Electron separate-audio-process complication"
    - "A copy-pasteable hands-on inspection runbook exists with exact paths, exact tool commands, and exact symbol strings the developer will grep for"
    - "Every desk-research claim is tagged with provenance and confidence, and no hands-on conclusion is pre-filled (the mechanism verdict is left as an open confirm/refute slot for 02-02)"
    - "No GoofCord source is changed (recon-only boundary, [UPST-01])"
  artifacts:
    - path: ".planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-FINDINGS.md"
      provides: "Findings-doc skeleton with all desk-research sections filled and hands-on slots left empty"
      contains: "## 1. Mechanism"
    - path: ".planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-RECON-RUNBOOK.md"
      provides: "Copy-pasteable Windows inspection runbook for the developer"
      contains: "dumpbin /imports"
  key_links:
    - from: "02-RECON-RUNBOOK.md"
      to: "02-FINDINGS.md"
      via: "runbook result slots map 1:1 to the FINDINGS hands-on evidence rows"
      pattern: "Mechanism|EXCLUDE|process tree"
---

<objective>
Produce the AI-doable half of the Phase 2 recon deliverable: (1) the findings-document skeleton `02-FINDINGS.md` with every desk-research-able section filled in, and (2) the copy-pasteable hands-on inspection runbook `02-RECON-RUNBOOK.md` the developer will execute on their own Windows box in plan 02-02.

This plan establishes the publicly-documented baseline so the developer's on-box inspection becomes a fast **confirm/refute** step, not a discover-from-zero step. It writes NO GoofCord code, builds NO prototype, and installs NO packages (D-02, D-04). The mechanism *verdict* and clean-room go/no-go *conclusion* are deliberately left as empty, clearly-marked slots — they depend on the human observations gathered in 02-02 and must not be pre-filled from inference (Pitfall 1, the D-08/D-09 honesty theme).

Purpose: De-risk the whole Bug-B direction cheaply by arming the developer with the public baseline + an exact inspection script, before any native-module investment is greenlit (D-06).
Output: `02-FINDINGS.md` (skeleton + desk-research sections + provenance/confidence legends) and `02-RECON-RUNBOOK.md` (ordered, copy-pasteable Windows commands and expected-symbol strings).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-CONTEXT.md
@.planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-RESEARCH.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold 02-FINDINGS.md with the four-section structure, provenance/confidence legends, and empty hands-on slots</name>
  <files>.planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-FINDINGS.md</files>
  <read_first>
    - .planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-CONTEXT.md (Phase Boundary; D-01 echo target; D-02/D-04 recon-only; D-05 clean-room LOCKED; the recon checklist)
    - .planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-RESEARCH.md ("Recommended findings-document structure" maps 1:1 to the 4 ROADMAP success criteria; "Pattern: evidence provenance + confidence tagging"; "Anti-patterns to avoid")
    - .planning/ROADMAP.md (Phase 2 Success Criteria 1-4 — the section headings must map 1:1 to these)
  </read_first>
  <action>
    Create `02-FINDINGS.md` using the structure in RESEARCH.md "Recommended findings-document structure" as the source of truth. Author the four top-level sections so their headings map 1:1 to the 4 ROADMAP Success Criteria, using exactly these heading texts so they are grep-checkable: `## 1. Mechanism + Evidence`, `## 2. Replication Parameters`, `## 3. Clean-room Go/No-Go`, `## 4. Recon-only Boundary`.

    At the top of the doc, define the two legends from RESEARCH.md's "evidence provenance + confidence tagging" pattern: a Provenance legend (`[hands-on: tool/DLL/symbol]` vs `[desk-research: source]` vs `[inference]`) and a Confidence legend (`hands-on confirmed on my box` / `desk-research / public report` / `inference`). State that every substantive claim in the doc MUST carry both tags.

    Under `## 1. Mechanism + Evidence`, create an empty, labelled Evidence table with columns {claim, tool used, DLL/source, symbol/string, confidence} and a clearly-marked placeholder `Verdict: [TO BE FILLED IN 02-02 FROM HANDS-ON OBSERVATIONS — one of: virtual-device driver | public WASAPI process-loopback | other | inconclusive]`. Do NOT pre-fill the verdict from inference — this is the central question the human step resolves (RESEARCH.md A1/A2; Pitfall 1).

    Under `## 4. Recon-only Boundary`, include an attestation line stating no GoofCord source was changed and no prototype built this phase ([UPST-01], D-02), plus a reference to the boundary check `git diff --name-only` showing no `src/` changes.

    Leave `## 2. Replication Parameters` and `## 3. Clean-room Go/No-Go` as headed sections with their desk-research-able sub-points to be filled by Task 2 and their hands-on-dependent sub-points clearly marked `[depends on 02-02 observations]`.

    Do NOT paste Discord's proprietary internals or DLL strings as an implementation recipe — reading the DLL answers only which approach Discord chose; the how comes only from the Microsoft sample (D-05, RESEARCH.md anti-patterns).
  </action>
  <acceptance_criteria>
    - `02-FINDINGS.md` exists in the phase directory.
    - `grep -c '^## 1. Mechanism + Evidence'` ≥ 1; same for `^## 2. Replication Parameters`, `^## 3. Clean-room Go/No-Go`, `^## 4. Recon-only Boundary`.
    - The doc contains both a Provenance legend and a Confidence legend (grep for `Provenance` and `Confidence`).
    - The mechanism Verdict is an explicit unfilled placeholder, not a stated conclusion: `grep -i 'TO BE FILLED IN 02-02'` returns at least one line under section 1.
    - Section 4 contains a recon-only attestation: `grep -i 'no GoofCord source'` (or equivalent) returns ≥ 1 line.
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && f=.planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-FINDINGS.md && test -f "$f" && grep -q '^## 1. Mechanism + Evidence' "$f" && grep -q '^## 2. Replication Parameters' "$f" && grep -q '^## 3. Clean-room Go/No-Go' "$f" && grep -q '^## 4. Recon-only Boundary' "$f" && grep -qi 'TO BE FILLED IN 02-02' "$f" && grep -q 'Provenance' "$f" && grep -q 'Confidence' "$f" && echo PASS</automated>
  </verify>
  <done>02-FINDINGS.md exists with the four success-criteria section headings, both legends, an unfilled mechanism-verdict placeholder, and a recon-only boundary attestation.</done>
</task>

<task type="auto">
  <name>Task 2: Fill the desk-research-able baseline sections in 02-FINDINGS.md (public WASAPI API, min build, macOS contrast, Electron multi-process)</name>
  <files>.planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-FINDINGS.md</files>
  <read_first>
    - .planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-RESEARCH.md ("Standard Stack" → "The clean-room reference" table; "State of the Art"; "Don't Hand-Roll"; "Open Questions" 2 and 3; "Sources")
    - .planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-CONTEXT.md (D-05 clean-room LOCKED; canonical_refs → "The candidate mechanism (public, clean-room)")
    - .planning/research/PITFALLS.md (Pitfall 4 Electron separate-audio-process / PID; Pitfall 6 disable_local_echo viewer-side verification)
  </read_first>
  <action>
    Fill the desk-research-able content into the sections scaffolded in Task 1. Source all facts from RESEARCH.md (its "Standard Stack — clean-room reference" table and "Sources" are the authoritative public references) — do not re-derive from the open web unless RESEARCH.md is silent.

    Under `## 2. Replication Parameters`, write the desk-confirmed facts: explain INCLUDE vs EXCLUDE and why `PROCESS_LOOPBACK_MODE_EXCLUDE_TARGET_PROCESS_TREE` is the echo fix; record the minimum Windows build (20348) and the fallback story for builds < 20348 (per RESEARCH.md Open Question 3: only the user-side separate-output-device workaround or no system audio); document the Electron/Chromium separate "Audio Service" utility process as a future-impl PID-resolution complication (per PITFALLS Pitfall 4 and RESEARCH.md Open Question 2 — name the process tree as the exclude target, NOT a single window PID, and flag the exact PID on Electron 41.3.0 as an open detail). Tag each claim `[desk-research: <source>]` with confidence; leave the "which process tree Discord *actually* excludes" sub-point marked `[depends on 02-02 observations]`.

    Under `## 3. Clean-room Go/No-Go`, write the public-API portion: state that the entire exclude-tree capability already exists as the public Microsoft ApplicationLoopback sample (the clean-room source of truth, D-05) and that the go/no-go MUST rest only on the public API, never on copied Discord internals. Reference the public symbol surface by name without lifting Discord code: `ActivateAudioInterfaceAsync`, `AUDIOCLIENT_ACTIVATION_PARAMS`, `AUDIOCLIENT_ACTIVATION_TYPE_PROCESS_LOOPBACK`, `AUDIOCLIENT_PROCESS_LOOPBACK_PARAMS`, `VIRTUAL_AUDIO_DEVICE_PROCESS_LOOPBACK`, and the `E_NOTIMPL`-on-`GetMixFormat` fixed-format quirk — citing RESEARCH.md as the source. Leave the final yes/no/conditional verdict marked `[depends on 02-02 mechanism finding]`.

    Add a short macOS-driver contrast note (Discord ships the Rogue Amoeba Audio Capture Engine on macOS because macOS lacks the in-OS API; Windows has it since 20348) tagged `[desk-research / public report]` at MEDIUM confidence per RESEARCH.md.

    Do NOT present any of this desk-research baseline as a hands-on-confirmed fact about Discord's actual Windows build — every claim here is `[desk-research]` or `[inference]`, never `[hands-on confirmed]` (Pitfall 1; honesty guardrail).
  </action>
  <acceptance_criteria>
    - `02-FINDINGS.md` records the minimum Windows build: `grep -E '20348'` returns ≥ 1 line.
    - The EXCLUDE-tree mechanism is named: `grep 'EXCLUDE_TARGET_PROCESS_TREE'` returns ≥ 1 line.
    - The Electron separate-audio-process complication is documented: `grep -i 'Audio Service'` (or `process tree`) returns ≥ 1 line under section 2.
    - The public clean-room reference is named: `grep 'ActivateAudioInterfaceAsync'` returns ≥ 1 line under section 3.
    - The hands-on-dependent verdicts remain unfilled: `grep -ci 'depends on 02-02'` ≥ 1 (a hands-on conclusion is NOT prematurely asserted).
    - No line in the desk-research sections tags a Discord-mechanism claim as `hands-on confirmed` (manual review note in SUMMARY; grep guard: `grep -n 'hands-on confirmed' 02-FINDINGS.md` should return nothing in section 1's verdict at this stage).
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && f=.planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-FINDINGS.md && grep -Eq '20348' "$f" && grep -q 'EXCLUDE_TARGET_PROCESS_TREE' "$f" && grep -q 'ActivateAudioInterfaceAsync' "$f" && grep -qi 'depends on 02-02' "$f" && grep -qiE 'Audio Service|process tree' "$f" && echo PASS</automated>
  </verify>
  <done>The desk-research baseline (public WASAPI exclude-tree API, build 20348 + fallback, macOS contrast, Electron multi-process PID complication) is written into sections 2 and 3 with provenance/confidence tags, and all hands-on-dependent verdicts remain explicitly unfilled.</done>
</task>

<task type="auto">
  <name>Task 3: Author 02-RECON-RUNBOOK.md — the copy-pasteable Windows inspection script the developer runs in 02-02</name>
  <files>.planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-RECON-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-CONTEXT.md (specifics → "Recon checklist (the floor)"; D-09 no DevTools / CLI+GUI tools only)
    - .planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-RESEARCH.md ("Standard Stack — Free Windows inspection tools" table; "Code Examples — The hands-on DLL inspection commands"; "Architecture Patterns — Recon evidence-flow diagram"; "Common Pitfalls" 1-5)
    - .planning/research/PITFALLS.md (Pitfall 5 strings-false-positive corroboration; Pitfall 6 viewer-side verification caveat)
  </read_first>
  <action>
    Create `02-RECON-RUNBOOK.md` as a literal, ordered, copy-pasteable checklist the developer executes on their own Windows box. Use the CONTEXT recon checklist (the 6-step floor) and the RESEARCH "Free Windows inspection tools" table and "hands-on DLL inspection commands" as the source of truth. Each step states: the exact tool, the exact command or GUI click-path, the exact strings/symbols to look for, and a blank result slot the developer fills.

    Include these exact steps with exact identifiers:
    1. Locate Discord's native audio modules under `%LocalAppData%\Discord\app-*\modules\` — examine `discord_voice*`, `discord_krisp*`, and audio-helper DLLs (give the `dir /b /s` enumeration command).
    2. Device Manager (`devmgmt.msc`) → *Sound, video and game controllers* + *Audio inputs and outputs* — check for a Discord-installed virtual audio device; record present/absent.
    3. Fast string pass with Sysinternals `strings` then `findstr /I` for: `ActivateAudioInterface`, `PROCESS_LOOPBACK`, `VIRTUAL_AUDIO_DEVICE`, `AUDIOCLIENT_ACTIVATION`.
    4. Authoritative import-table check with `dumpbin /imports` filtered to `mmdevapi` / `ActivateAudioInterfaceAsync` (run in a VS Developer Command Prompt). State explicitly that a `strings` hit must be corroborated by `dumpbin /imports` before upgrading confidence (Pitfall 5 / Pitfall 3 strings-false-positive).
    5. Determine INCLUDE vs EXCLUDE from the symbols/behaviour — `PROCESS_LOOPBACK_MODE_EXCLUDE_TARGET_PROCESS_TREE` vs `..._INCLUDE_...`.
    6. Task Manager (Details) / `tasklist /v | findstr /I discord` to enumerate Discord + Chromium "Audio Service" PIDs and reason about which process tree a future module would exclude.
    7. Record the Windows build via `winver` (is it ≥ 20348?).

    For each step, provide a "Result:" slot and a "Provenance/Confidence:" slot so the developer's raw observations drop straight into the 02-FINDINGS evidence table (each runbook step maps to a row in `## 1. Mechanism + Evidence`).

    Add a short honesty note at the top: record only what was actually observed; a step that was NOT run stays `not run / unknown`, never inferred. Add the verification caveat for any future audio test (viewer-side only; keep audio playing) referencing D-08, but mark it as out-of-scope-for-recon context.

    All tools must be standalone CLI or GUI apps (no browser DevTools, no F12) per D-09. Note that none require admin to read a DLL or view Device Manager.
  </action>
  <acceptance_criteria>
    - `02-RECON-RUNBOOK.md` exists in the phase directory.
    - It names the modules path: `grep -F '%LocalAppData%\Discord'` (or `LocalAppData` + `Discord` + `modules`) returns ≥ 1 line.
    - It includes the authoritative import check: `grep 'dumpbin /imports'` returns ≥ 1 line.
    - It includes the symbol targets: `grep 'ActivateAudioInterface'` and `grep 'PROCESS_LOOPBACK'` each return ≥ 1 line.
    - It includes the Device Manager check and the build check: `grep -i 'devmgmt'` (or `Device Manager`) ≥ 1, and `grep -i 'winver'` ≥ 1.
    - It contains fillable result slots: `grep -ci 'Result:'` ≥ 6 (one per inspection step).
    - It contains the strings-corroboration warning: `grep -i 'corroborat'` (or `dumpbin` + `before`) ≥ 1.
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && f=.planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-RECON-RUNBOOK.md && test -f "$f" && grep -qi 'modules' "$f" && grep -q 'dumpbin /imports' "$f" && grep -q 'ActivateAudioInterface' "$f" && grep -q 'PROCESS_LOOPBACK' "$f" && grep -qi 'winver' "$f" && [ "$(grep -ci 'Result:' "$f")" -ge 6 ] && echo PASS</automated>
  </verify>
  <done>02-RECON-RUNBOOK.md exists as an ordered copy-pasteable runbook with exact tool commands, exact symbol targets, the strings→dumpbin corroboration rule, a build check, and ≥6 fillable result slots that map to the FINDINGS evidence rows.</done>
</task>

</tasks>

<verification>
- All three artifacts/edits land under `.planning/phases/02-fix-bug-b-windows-loopback-audio-captured/` only.
- Recon-only boundary holds: `git diff --name-only` shows zero changes under `src/` (UPST-01, D-02).
- `02-FINDINGS.md` has the four success-criteria section headings, both legends, an unfilled mechanism verdict, and the desk-research baseline (build 20348, EXCLUDE-tree, public API symbols, Electron multi-process).
- `02-RECON-RUNBOOK.md` is a literal copy-pasteable Windows script with exact symbols/commands and fillable result slots, using only CLI/GUI tools (no DevTools, D-09).
- No hands-on conclusion is pre-asserted; mechanism verdict and clean-room go/no-go verdict are explicit `[depends on 02-02]` slots (Pitfall 1, honesty guardrail).
</verification>

<success_criteria>
- `02-FINDINGS.md` exists with sections mapping 1:1 to the 4 ROADMAP Success Criteria and a provenance+confidence convention applied to every desk-research claim.
- `02-RECON-RUNBOOK.md` exists and gives the developer an exact, ordered inspection script.
- No `src/` changes (recon-only).
- The human (02-02) can run the runbook and drop results straight into the FINDINGS evidence table.
</success_criteria>

<output>
Create `.planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-01-SUMMARY.md` when done.
</output>
