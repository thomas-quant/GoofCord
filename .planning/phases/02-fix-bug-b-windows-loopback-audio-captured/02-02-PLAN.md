---
phase: 02-fix-bug-b-windows-loopback-audio-captured
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - .planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-RECON-OBSERVATIONS.md
  - .planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-FINDINGS.md
autonomous: false
requirements: [AUDIO-01, AUDIO-02]

must_haves:
  truths:
    - "The developer has executed the 02-RECON-RUNBOOK on their physical Windows box and recorded raw observations (Device Manager result, DLL symbol/import results, INCLUDE-vs-EXCLUDE, PIDs, Windows build)"
    - "The findings document records Discord's Windows per-process audio-capture mechanism backed by concrete evidence rows, each tagged with the tool/DLL/symbol that produced it (SC-1)"
    - "The findings document captures the replication parameters: include-vs-exclude, the excluded process tree (accounting for Electron's separate audio process), the minimum Windows build, and the fallback for older builds (SC-2)"
    - "The findings document gives an evidence-based clean-room go/no-go recommendation resting only on the public Microsoft API, never copied Discord code (SC-3, D-05)"
    - "The recon-only boundary is honoured and attested: no GoofCord source changed, no prototype built (SC-4, UPST-01)"
    - "Every conclusion is tagged hands-on-confirmed vs desk-research/inference; no desk-research inference is presented as a hands-on fact (honesty guardrail, Pitfall 1)"
  artifacts:
    - path: ".planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-RECON-OBSERVATIONS.md"
      provides: "The developer's raw on-box observations from running the runbook"
      contains: "Result:"
    - path: ".planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-FINDINGS.md"
      provides: "Finalized findings doc with mechanism verdict, replication parameters, and clean-room go/no-go"
      contains: "Verdict:"
  key_links:
    - from: "02-RECON-OBSERVATIONS.md"
      to: "02-FINDINGS.md"
      via: "each recorded observation becomes an evidence row in section 1"
      pattern: "Verdict|EXCLUDE|INCLUDE|driver"
---

<objective>
Close the Phase 2 recon by (1) having the developer execute `02-RECON-RUNBOOK.md` on their own physical Windows box and record raw observations into `02-RECON-OBSERVATIONS.md`, then (2) synthesising the desk-research baseline (from 02-01) with those human observations to finalize `02-FINDINGS.md` — filling in the mechanism verdict (SC-1), the replication parameters (SC-2), and the clean-room go/no-go recommendation (SC-3), while attesting the recon-only boundary (SC-4).

The AI executor CANNOT inspect the developer's installed Discord and MUST NOT fabricate or infer these observations. The mechanism verdict and clean-room go/no-go rest on what the developer actually observed; where a step was not run, the doc records `inconclusive / needs on-box confirmation`, never a guessed conclusion (Pitfall 1; D-08/D-09 honesty theme).

Purpose: Convert the public baseline into an evidence-backed mechanism conclusion and a clean-room go/no-go that feeds the deferred native-module-vs-workaround decision (D-06) — without writing any GoofCord code (D-02, D-04).
Output: `02-RECON-OBSERVATIONS.md` (raw human observations) and a finalized `02-FINDINGS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-CONTEXT.md
@.planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-RESEARCH.md
@.planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-RECON-RUNBOOK.md
@.planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-FINDINGS.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Developer runs the recon runbook on their Windows box and records raw observations</name>
  <files>.planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-RECON-OBSERVATIONS.md</files>
  <read_first>
    - .planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-RECON-RUNBOOK.md (the exact ordered inspection script to execute)
    - .planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-CONTEXT.md (D-05 clean-room — read the DLL to learn WHICH approach Discord chose, not to lift code; D-09 no DevTools)
  </read_first>
  <action>
    HUMAN-ONLY STEP — the AI cannot probe the developer's machine. The developer runs `02-RECON-RUNBOOK.md` on their physical Windows box (Discord installed) and records every raw result into a new file `02-RECON-OBSERVATIONS.md`. See `<how-to-verify>` for the exact ordered commands and what to paste. The AI executor must NOT fabricate, infer, or pre-fill any of these observations; it only confirms the observations file exists and is populated after the resume signal.
  </action>
  <what-built>
    The AI produced (in 02-01) a copy-pasteable Windows inspection runbook (`02-RECON-RUNBOOK.md`) and a findings-doc skeleton with the desk-research baseline filled in. This checkpoint is the hands-on half that only the developer can do on their physical Windows box with Discord installed — the AI cannot probe that machine.
  </what-built>
  <how-to-verify>
    On your Windows box (Discord installed), open `02-RECON-RUNBOOK.md` and run each step in order, then write your raw observations into a new file `02-RECON-OBSERVATIONS.md` in the same phase directory. For each runbook step record a `Result:` line plus a `Provenance/Confidence:` line. Specifically:

    1. Enumerate Discord's native audio modules: in a terminal run `dir /b /s "%LocalAppData%\Discord\app-*\modules\discord_voice*"` (and `*krisp*`, `*audio*`). Paste the file paths found.
    2. Device Manager (`devmgmt.msc`) → *Sound, video and game controllers* and *Audio inputs and outputs*: record whether a Discord-installed virtual audio device is present (name it if so) or absent.
    3. String pass: `strings -n 8 <discord_voice node/dll> | findstr /I "ActivateAudioInterface PROCESS_LOOPBACK VIRTUAL_AUDIO_DEVICE AUDIOCLIENT_ACTIVATION"`. Paste the matching lines (or "no matches").
    4. Authoritative import check (VS Developer Command Prompt): `dumpbin /imports <discord_voice node/dll> | findstr /I "mmdevapi ActivateAudioInterfaceAsync"`. Paste the result. A `strings` hit only counts as confirmed if `dumpbin /imports` also shows the import — record both.
    5. INCLUDE vs EXCLUDE: note whether `PROCESS_LOOPBACK_MODE_EXCLUDE_TARGET_PROCESS_TREE` or `..._INCLUDE_...` appears (or neither).
    6. PIDs: `tasklist /v | findstr /I discord` and Task Manager Details — list the Discord processes and any Chromium "Audio Service" utility process.
    7. Build: run `winver`; record the build number and whether it is ≥ 20348.

    Honesty rule: record only what you actually saw. Any step you did not run stays `not run / unknown` — do not infer it. If a DLL was locked/in-use, copy it out first and note that.
  </how-to-verify>
  <acceptance_criteria>
    - `02-RECON-OBSERVATIONS.md` exists in the phase directory.
    - It contains a `Result:` line for each runbook step actually run: `grep -ci 'Result:'` ≥ 6 (steps not run are explicitly marked, not omitted).
    - It records the Device Manager driver outcome (present/absent): `grep -i 'device'` returns ≥ 1 line.
    - It records the `dumpbin`/import outcome and the Windows build: `grep -iE 'dumpbin|imports'` ≥ 1 and `grep -E '2[0-9]{4}'` (build number) ≥ 1, OR an explicit `not run / unknown` for any step skipped.
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && f=.planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-RECON-OBSERVATIONS.md && test -f "$f" && [ "$(grep -ci 'Result:' "$f")" -ge 6 ] && echo PASS</automated>
    <human-check>Developer confirms 02-RECON-OBSERVATIONS.md contains their actual on-box results, with any unrun step marked "not run / unknown".</human-check>
  </verify>
  <resume-signal>Type "observations recorded" once 02-RECON-OBSERVATIONS.md is saved, or describe what you could not run (e.g. "no VS Build Tools, dumpbin skipped").</resume-signal>
  <done>02-RECON-OBSERVATIONS.md exists and records the developer's raw on-box results for each runbook step (or an explicit "not run / unknown" marker per skipped step), with provenance/confidence noted.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize 02-FINDINGS.md by synthesising the desk-research baseline with the developer's on-box observations</name>
  <files>.planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-FINDINGS.md</files>
  <read_first>
    - .planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-RECON-OBSERVATIONS.md (the developer's raw on-box results — the ONLY source for the hands-on verdict)
    - .planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-FINDINGS.md (the skeleton + desk-research baseline from 02-01 — the unfilled `[depends on 02-02]` slots)
    - .planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-RESEARCH.md ("Pattern: evidence provenance + confidence tagging"; "Anti-patterns to avoid"; "Common Pitfalls" 1, 2, 3)
    - .planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-CONTEXT.md (D-05 clean-room LOCKED; D-06 the deferred decision this feeds)
  </read_first>
  <action>
    Fill the previously-unfilled `[depends on 02-02]` slots in `02-FINDINGS.md` using ONLY the observations in `02-RECON-OBSERVATIONS.md` for any hands-on claim.

    Section 1 (`## 1. Mechanism + Evidence`): replace the `TO BE FILLED IN 02-02` verdict placeholder with the mechanism verdict (`virtual-device driver` | `public WASAPI process-loopback` | `other` | `inconclusive`) supported by the evidence rows. Populate the Evidence table — one row per runbook step the developer ran — with {claim, tool used, DLL/source, symbol/string, confidence}. Each row's confidence is `hands-on confirmed on my box` ONLY if the developer actually observed it; otherwise `desk-research / inference` or `inconclusive — needs on-box confirmation`. If a `strings` hit was not corroborated by `dumpbin /imports`, do NOT upgrade it to confirmed (Pitfall 3). If the developer could not run a step, the verdict is `inconclusive` for that dimension — never inferred (Pitfall 1).

    Section 2 (`## 2. Replication Parameters`): finalize the INCLUDE-vs-EXCLUDE finding and the "which process tree Discord excludes" sub-point from the observations (accounting for the Electron separate Audio Service process — keep this a process-tree statement, not a single window PID, and flag the exact PID resolution as a future-impl open detail per RESEARCH.md Open Question 2). The min build (20348) and fallback are already desk-confirmed from 02-01 — keep them.

    Section 3 (`## 3. Clean-room Go/No-Go`): write the evidence-based go/no-go — can GoofCord replicate from the public Microsoft API alone (yes/no/conditional)? The recommendation MUST rest only on the public ApplicationLoopback sample and the public symbol surface, NEVER on copied Discord internals (D-05). State explicitly that reading Discord's DLL answered only *which approach* Discord uses; the *how* comes from the Microsoft sample. Surface the inputs to the deferred D-06 decision (native WASAPI module vs. user-side workaround vs. document-only). Do not lift Discord's proprietary symbol layout into the doc as a build recipe (Pitfall 2 / anti-patterns).

    Section 4 (`## 4. Recon-only Boundary`): confirm the attestation — no GoofCord source changed, no prototype built ([UPST-01]); reference the `git diff --name-only` src-boundary check.

    Apply the provenance + confidence tag to every substantive claim throughout. Do not present any desk-research inference as a hands-on-confirmed fact (the honesty guardrail).
  </action>
  <acceptance_criteria>
    - `02-FINDINGS.md` section 1 no longer contains the placeholder: `grep -ci 'TO BE FILLED IN 02-02'` == 0, and a concrete `Verdict:` line is present (`grep -i 'Verdict:'` ≥ 1).
    - No `[depends on 02-02]` slots remain: `grep -ci 'depends on 02-02'` == 0.
    - Section 1 contains at least one evidence row with provenance: `grep -iE 'tool:|file:|symbol:'` ≥ 1 (when the developer ran any inspection step), OR a recorded `inconclusive` verdict if no step was run.
    - Section 3 cites the public clean-room source by name and states the Discord-DLL boundary: `grep -i 'ApplicationLoopback'` (or `Microsoft sample`) ≥ 1 and `grep -i 'clean-room'` ≥ 1.
    - Section 4 attestation present: `grep -i 'no GoofCord source'` (or equivalent) ≥ 1.
    - Honesty guard: every `hands-on confirmed` tag is backed by a corresponding line in `02-RECON-OBSERVATIONS.md` (manual cross-check noted in SUMMARY).
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && f=.planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-FINDINGS.md && test -f "$f" && [ "$(grep -ci 'TO BE FILLED IN 02-02' "$f")" -eq 0 ] && [ "$(grep -ci 'depends on 02-02' "$f")" -eq 0 ] && grep -qi 'Verdict:' "$f" && grep -qi 'clean-room' "$f" && grep -qiE 'ApplicationLoopback|Microsoft sample' "$f" && echo PASS</automated>
  </verify>
  <done>02-FINDINGS.md is finalized: a concrete mechanism verdict backed by hands-on-tagged evidence rows (or an honest `inconclusive`), finalized replication parameters, an evidence-based clean-room go/no-go resting only on the public API, and the recon-only boundary attestation — with no placeholder or `[depends on 02-02]` slots remaining.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Developer reviews the finalized findings doc for honesty and completeness</name>
  <files>.planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-FINDINGS.md</files>
  <action>
    HUMAN VERIFICATION — the developer reviews the finalized `02-FINDINGS.md` against their own observations to confirm the doc does not overclaim and that the clean-room boundary is intact. See `<how-to-verify>` for the exact review checklist. The AI executor does not self-approve; it pauses for the developer's resume signal.
  </action>
  <what-built>
    02-FINDINGS.md has been finalized by synthesising the desk-research baseline with the developer's on-box observations. This checkpoint confirms the doc honestly represents what was observed (no overclaiming) and answers all four success criteria.
  </what-built>
  <how-to-verify>
    Open `02-FINDINGS.md` and confirm:
    1. The mechanism verdict (section 1) matches what you actually observed — if you did not run a step, that dimension reads `inconclusive`, not a guessed conclusion.
    2. No claim tagged `hands-on confirmed` is one you did not actually observe on your box (spot-check 2-3 evidence rows against `02-RECON-OBSERVATIONS.md`).
    3. The clean-room go/no-go (section 3) cites only the public Microsoft ApplicationLoopback sample as the implementation source, never Discord's DLL internals as a build recipe (D-05).
    4. Sections map 1:1 to the 4 ROADMAP Success Criteria (mechanism+evidence; replication params incl. exclude-tree + excluded process tree + build 20348 + fallback; clean-room go/no-go; recon-only boundary).
    5. The recon-only boundary holds — no `src/` was touched (the executor will have run `git diff --name-only` showing no `src/` changes; confirm the attestation in section 4).
  </how-to-verify>
  <acceptance_criteria>
    - Developer confirms the verdict is honest (observed dimensions are `hands-on confirmed`; unrun dimensions are `inconclusive`).
    - Developer confirms no Discord-DLL code/symbol layout is presented as a build recipe (D-05 clean-room intact).
    - `git diff --name-only` shows zero changes under `src/` for this phase (recon-only, UPST-01).
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test -z "$(git diff --name-only -- src/)" && test -z "$(git diff --name-only --cached -- src/)" && echo "PASS: no src/ changes"</automated>
    <human-check>Developer confirms the findings doc honestly reflects observed vs desk-research claims and the clean-room boundary is intact.</human-check>
  </verify>
  <resume-signal>Type "approved" if the findings doc is honest and complete, or describe what to correct (e.g. "row 3 marked hands-on but I never ran dumpbin").</resume-signal>
  <done>Developer has reviewed 02-FINDINGS.md, confirmed no overclaiming (hands-on tags trace to observations), confirmed the clean-room boundary is intact, and `git diff --name-only` shows no src/ changes.</done>
</task>

</tasks>

<verification>
- `02-RECON-OBSERVATIONS.md` exists and captures the developer's raw on-box results (or explicit `not run` markers).
- `02-FINDINGS.md` is finalized: no placeholder/`[depends on 02-02]` slots; concrete mechanism verdict; finalized replication params; clean-room go/no-go on the public API only; recon-only attestation.
- Sections map 1:1 to the 4 ROADMAP Success Criteria.
- `git diff --name-only` shows zero `src/` changes (UPST-01, D-02 recon-only boundary).
- Honesty guardrail upheld: hands-on-tagged claims trace to observations; unobserved dimensions are `inconclusive`, not inferred (Pitfall 1).
</verification>

<success_criteria>
- SC-1: Findings doc records Discord's Windows per-process audio mechanism backed by concrete, tool/DLL/symbol-tagged evidence (or an honest `inconclusive`).
- SC-2: Findings doc captures include-vs-exclude, the excluded process tree (Electron multi-process accounted for), min build 20348, and the < 20348 fallback.
- SC-3: Findings doc gives an evidence-based clean-room go/no-go resting only on the public Microsoft API (D-05).
- SC-4: Recon-only boundary honoured and attested — no GoofCord source changed, no prototype built (UPST-01).
</success_criteria>

<output>
Create `.planning/phases/02-fix-bug-b-windows-loopback-audio-captured/02-02-SUMMARY.md` when done.
</output>
